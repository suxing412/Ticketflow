# 施工令-013 · 异厂评审台（H88 配套，制作人 14:42 定稿：全厂评审+新厂自动纳入）

## 范围（仓：D:\GitHub\Ticketflow，新建 套件/评审台/review.js）

1. **用法**：`node 套件/评审台/review.js --file <方案md绝对路径> [--out <意见合集md路径>]`——供方案单执行会话调用。
2. **评审团组成（自动发现，新厂零改动纳入）**：读 `D:\GitHub\AI-GameStudio\监制台\studio.config.json`——①codex：本机 `codex exec` CLI（--dangerously-bypass-approvals-and-sandbox，stdin 喂提示词）；②每个 `兼容池` 条目（现 deepseek）：Anthropic 兼容 API（base+key+模型名从 config 读取，**密钥只在进程内使用，任何输出/日志/错误信息不得含密钥**）。未来 config 新增兼容池条目即自动入席。
3. **评审提示词**（统一模板）：喂方案全文，要求逐章抬杠——缺失的不变量/路径划分的坑/验证法漏洞/实现风险，输出「章节×意见×严重度」结构化清单；各家独立评审互不可见。
4. **输出**：意见合集 md（按厂商分节，含各家原文与耗时），stdout 输出一行 JSON 摘要 `{"ok":true,"评审团":["codex","deepseek"],"意见数":N,"out":"…"}`。
5. **容错**：某厂超时/失败不拖垮全场——该厂记「缺席（原因）」，其余照常；全场空手才 ok:false。单厂超时默认 5 分钟可调。

## 不要做
- 不碰监制台 apps/studio；不打包不提交；不在任何输出回显密钥（含错误栈）；不动 AI-GameStudio（只读 config）。

## 验收标准
- [ ] 用一份真实 md（可拿 TK-103 协议文档）实测：两厂意见合集落盘、stdout JSON 摘要正确
- [ ] 密钥审计：全部输出与错误路径 grep 无密钥片段（构造 API 失败场景取证）
- [ ] 单厂失败容错实测（伪造 deepseek 坏端点→codex 独撑，缺席记录在案）
- [ ] 新厂自动纳入：config 临时副本加一个假兼容池条目→评审团自动出现第三席（实测后还原）
- [ ] 完工纪要逐条证据
