# 施工令-015 · wiki 知识总库（制作人 2026-08-07 15:29 定稿）

裁决：wiki 升格唯一知识入口，四分区页签；风格库导航退役。「策划案是骨架，设计事实是一个一个的词条」——两者分开为平行分区，词条带源文档溯源链。

## 范围（apps/studio）

1. **wiki 视图四分区页签**（public/app.js viewWiki 改造）：
   - 设计事实：现有 Docs/wiki 词条机制原样（图谱/幽灵链/_待审 全保留）；词条渲染时若 frontmatter 有 `源文档:` 显示溯源链接。
   - 策划案：聚合 `<项目仓>/Docs/SLG/策划文档/`（含子目录）文档列表+阅读视图；只读展示，不提供编辑。
   - 技术方案：聚合 `<项目仓>/Docs/SLG/技术方案/` 同上。
   - 美术标杆：迁入现风格库内容——美术库图集（缩略图静态服务复用 /stylelib-files）+ 策划标杆.md 渲染（更名展示为「标杆公理」，D12 精选制入库按钮随迁本页签，API 不变）。
2. **风格库导航退役**：nav 去「风格库」页签；#/style 路由 301 到 wiki 美术标杆页签（书签不死）。
3. server.js：策划案/技术方案两个聚合列表 API（读项目注册推仓路径，容错缺目录）；wiki 词条源文档字段透传。
4. 文件零迁移（视图聚合制）；美术库/策划标杆数据留在监制台/风格库目录，仅展示位并入。

## 不要做
- 不动 wiki 词条写入/归档机制与 wiki-archival 契约；不动 D12 精选制流程本体（只挪按钮位置）。
- 不打包不提交；不碰 AI-GameStudio（读路径除外——本单视图读它，不写）。

## 验收标准
- [ ] wiki 四页签齐、各区有内容实机取证（含空目录容错）；词条源文档链可点
- [ ] 风格库 nav 消失；#/style 跳转美术标杆页签；美术图集缩略图正常、标杆公理渲染正常、入标杆按钮可用
- [ ] `npm test` 全量 0 失败（当前 30 套件 255 用例）；完工纪要逐条证据
