# 施工令-053 · 机判识别追加轮回执

案源：2026-08-12/13 连续 8 张单机判 0/N 全靠人工裁——回执文件按轮追加（「## 第 N 轮回执」/「完工报告（抢救誊录版）」等结构）后，precheck 只读首份「## 自测结果」，追加轮的逐条应答全灭。写区：`apps/studio/lib/precheck.js`＋测试。

## 要件
1. 机判取**最后一轮**报告体：识别「第 N 轮」「（返修后）」「完工报告」等追加节，以文件中最后一个「## 自测结果」（或等价节）为判卷对象；
2. 誊录版/按语前缀（总监抢救誊录等）不影响解析；
3. 兼容单轮老格式零回归；
4. 测试夹具直接用 TK-144/156/160/168 真实回执文件（多轮/誊录/单轮各态），断言逐条应答识别数与人工判读一致；全量 npm test 零回归。

## 验收
逐条自证+全量绿；回执 工程队/回执-053.md 附四夹具识别前后对比；git 提交本令文件面；不打包。
