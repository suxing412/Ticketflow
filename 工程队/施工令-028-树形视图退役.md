# 施工令-028 · 树形视图退役并入流程页（制作人 2026-08-09 03:00 裁决）

案源：制作人第一性巡检——「树形现在看着几乎没用」。拆解结论：层级只有两层，树状铺陈形式大于信息；唯一不可替代物=批量验收子单+子单层级一览。裁决：整页退役，能力并入流程页/详情页，少一个视图少一份维护。

## 范围（apps/studio）

1. **能力迁移先行（退役的前提，缺一不许删）**：
   - **批量验收子单**（原树形「✓验收子单」批量通过）：迁入**父单详情页** ops 区（专项/战役详情已有按钮族的，加「✓ 批量验收子单」带确认，行为与原树形等价：该父下全部待验收子单批量过）；
   - **子单层级一览**：父单详情页若无子单表格，补一张（子单号/标题/状态/进度/池，行点击进详情）——流程页签字绿框与专项条已可点进父单详情，链路即通；
   - 迁移后两能力实机取证（桩台）。
2. **退役**：路由 `#/tree`、导航页签「树形」、`viewTree` 族函数、专属 CSS、「折叠状态跨会话保持」存储键——整族删除；旧路由访问重定向流程页（书签兜底）。
3. **测试**：删树形相关断言；批量验收迁移后的行为测试补上（含「父下无待验收子单」空态）；`npm test` 全量零回归。
4. 双主题+860px 复查父单详情新增表格。

## 不要做

不动流程页现有结构（022 刚验收）；不动调度语义；批量验收行为不扩权（只动待验收态子单，与原实现逐字对齐）；不打包不 commit；AI-GameStudio 不碰。

## 验收标准

- [ ] 父单详情：子单表格+批量验收按钮实测（桩台截图，含确认门与空态）
- [ ] `#/tree` 重定向流程页；导航无树形页签；`grep -n "viewTree\|#/tree" public/` 仅剩重定向一处
- [ ] `npm test` 全量 0 失败；完工纪要逐条证据
