# 施工令-058 · 专项实体化（H103 落地）

案源：制作人 2026-08-13 18:21 裁决——专项是容器不是工单。现状病灶：TK-146/150 以工单身份混在 待验收，占 TK 号、带 QA/验收方式字段、被状态机与机判骚扰；早期 TK-S3 命名旧例漂移失传（H86 考古注记）。写区：`apps/studio/lib/`（新 专项注册表 module + lifecycle/dispatch/wake 挂钩改绑 + ledger-sync 适配）、`server.js`、`public/`、`test/`。

## 要件

1. **数据模型**：`专项注册表`（对齐 管线注册表 形制）：S 系列号（S-1 递增）、名称、目标、管线、状态机 立项→进行→收口→关账（唯一人闸=关账签字）、子单清单（由子单 fm `专项: S-n` 反向聚合，不在容器里手维护）、聚合视图（子单进度/预算实耗/基线变迁）。回执首节先给模型与迁移方案段再动码。
2. **切单挂钩改绑**：H49「专项定稿→自动切单」迁至「专项立项→切单」；PM 起草专项时产出注册表条目而非工单；候期切单（054 三出口）语义保留。
3. **状态机与机判隔离**：专项实体不进工单目录、不参与机判/QA/派发；052 台账同步器适配（专项批名=专项名，子单挂粒规则不变）。
4. **迁移**：TK-146/TK-150 转 S-1/S-2（含全部子单挂链、收口报告、追溯链），原 TK 号在注册表记 `别名`；迁移幂等可重跑；迁移后两容器停在「收口」态候制作人关账签字（其待验收文件归档不删）。
5. **UI**：专项页/卡按容器呈现（进度聚合条、子单树、收口报告直达、关账按钮=制作人）；工单板不再显示专项伪单。
6. **测试**：注册表状态机/聚合/迁移幂等/切单改绑/052 适配各分支；全量零回归。

## 验收

逐条自证+全量绿；回执 工程队/回执-058.md 附迁移前后对照（S-1/S-2 聚合视图文字誊录）；git 提交本令文件面；不打包（总监统一换装）。
