# 施工令-012 · 巡礼修复批（2026-08-07 夜班豁免件，源=白夜馆/2026-08-07-UI巡礼报告.md）

## 范围（apps/studio，逐条对应报告编号）

1. **P1 prompt 族哑弹根除**：`compatEdit`（app.js:2126 一带）四连 `prompt()` 改自绘输入弹窗（复用 ask() 视觉家族做 askInput()：标题+输入框+确认取消，Promise 化）；全仓 grep `prompt(`/`confirm(`/`alert(` 生产代码清零（ask 家族自身除外）。
2. **P2 在途页项目语境过滤**：viewAgents 对 /api/agents 按当前项目过滤，与页内时间轴同源。
3. **P2 参数卡定高撑破**：.paramcard 定高改 min-height 自适应；执行器 meta 的逐单号列表加收敛（超 3 个显「+N」）。
4. **P2 待定夺卷宗上呈原因字段化**：进待定夺时把原因写进 fm（lifecycle QA裁定/失败分诊/评估回呈 各写 fm.上呈原因 一句话）；详情页优先读字段，流水 grep 降为无字段老单的兜底；二级兜底正则收紧（不再命中滞留告警）。
5. **P2 树形窄窗**：.trow2 固定像素布局改 flex/grid 流式（.mid/.prog 不再绝对定位），860px 宽实测零重叠。
6. **建设性①**：在途页「无会话」态视觉自证——降饱和+虚线边+「已等 N 分钟未起会话」直写。
7. **预检强化**：deploy-ritual 由总监文书线加条；本单在 server 自检 lint（/api/env 或既有 lint 处）加「原生对话框 API 扫描」告警项。

## 不要做
- P3 五条与其余建设性三条不做（列入周六 ui-weekly 议程）；不动调度逻辑；不打包不提交；不碰 AI-GameStudio（读报告除外）。

## 验收标准
- [ ] grep 原生 prompt/confirm/alert 生产零命中；兼容池两按钮实机（Electron 壳或等效取证）可用
- [ ] 在途页过滤与时间轴同源（构造双项目样例取证）
- [ ] 参数卡无下溢（撑破两卡实测）；树形 860px 零重叠（报告同法复测）
- [ ] 新上呈单 fm 有上呈原因且详情页显示；老单兜底不误命中滞留告警（测试或实机）
- [ ] 无会话态视觉三要素齐（实拍）
- [ ] `npm test` 全量 0 失败；完工纪要逐条证据
