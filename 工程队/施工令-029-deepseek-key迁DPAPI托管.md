# 施工令-029 · deepseek key 迁 DPAPI 托管（制作人 2026-08-09 03:14 批准「要的，迁！」）

案源：凭据卡写「尚未托管任何 key」，而 deepseek key 明文躺在 studio.config.json（兼容池配置）——DPAPI 托管（协作者 creds 机制）本为此而生。

## 范围

1. **读码先行**：摸清 ①deepseek key 当前消费链（哪些模块从 config 兼容段读 key：两检 flash 调用/quota/兼容池）②creds.js 托管的写入与读取接口（DPAPI 加解密在谁手里、按什么名字存取）。纪要画出前后数据流。
2. **消费端改造**：所有读 deepseek key 的调用点改「托管优先、config 兼容字段兜底」（兜底保留=迁移失败可回退）；托管命中时 config 明文字段允许为空。
3. **迁移动作**（在本机执行，铁律：**key 明文全程不落日志不落回执不进 git**）：从 config 读出 key → 写入 DPAPI 托管（用 creds 既有接口）→ 验证读回能解密且与原值一致（比对用哈希，不打印原文）→ 把 studio.config.json 兼容段 key 字段清为空串并留注释「已迁 DPAPI 托管」。
4. **凭据卡联动**：迁移后卡上应显示「已托管 1 key（deepseek）」（027 修理后的口径）；桩台截图取证。
5. **测试**：托管优先/兜底回退/托管缺失三态单测；`npm test` 全量零回归。功能实证：迁移后真调一次 deepseek 两检链路（找一张已完成单跑 dry 初检或等价最小调用，一次调用以内，证明 key 从托管读出可用）。

## 不要做

不动 claude/codex 订阅凭据（归 CLI 不归 app）；不改 DPAPI 机制本身（协作者主导件，只消费接口）；config 其他字段不碰；不打包不 commit（0.26.3 攒批）；**任何输出里 key 原文零出现**（含错误信息、diff、测试夹具）。

## 验收标准

- [ ] 数据流前后图 + 消费端改造点清单
- [ ] 迁移实证：托管读回哈希一致 + config 明文已清 + 凭据卡显示已托管（截图）
- [ ] deepseek 真调一发成功（从托管取 key）
- [ ] 三态测试 + `npm test` 全量 0 失败；key 原文零泄漏自查声明
