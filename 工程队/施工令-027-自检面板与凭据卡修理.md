# 施工令-027 · 自检面板与凭据卡修理（制作人 2026-08-09 02:51 巡检点名）

案源：制作人换装后亲巡抓出——①凭据卡 UI 有问题 ②config 完整性误报「技术策划无执行池归属」（数据已修，结构病在码里）③检查器硬编码陈旧。

## 范围（apps/studio）

1. **池归属判定去双账**（结构病根治）：`lib/pool.js` 的 `poolFor` 读 `cfg.执行池.<池>.职能` 老映射，与 H85 编制表（`roster.read`）双账并存——TK-106~108 靠 agent 快照跑通纯属侥幸。改：`poolFor` 委托编制表（职能→池序首位可用池），`cfg.执行池.<池>.职能` 仅作额度闸分组保留（或彻底退役，以考古结论为准，纪要写明取舍）；`server.js:608` 自检同步改走编制口径。补测试覆盖「编制有而老表无」的职能。
2. **检查器硬编码清理**：`server.js:604` charters 数组补「技术策划」（七份）；同类硬编码职能清单全仓 grep 一遍，逐处改活读 `cfg.职能`。
3. **凭据卡 UI 修理**（`public/app.js` 凭据区 + `lib/creds.js` 透出文案）：
   - 「codex codex 登录态见环境探针」标签与文案重复叠字——名称与说明拼接逻辑修干净；
   - 状态圆点语义统一（绿=可用/灰=未知/红=失效），当前 codex 灰点与 claude 绿点判据口径写明并接真值（token 探测/app-server 探测结果）；
   - 行距与按钮对齐按现有卡片体例收拾；「重新登录」「登录」按钮在 Electron 壳内的真实行为核一遍（拉起可见终端？弹说明？），不可用的按钮不许摆着。
4. 双主题+860px 复查涉改区域；`npm test` 全量零回归（基线 35 套件 334 例+新增）。

## 不要做

不动树形视图（另单设计中）；不动调度/派发行为语义（poolFor 改造要求行为等价：现网六职能解析结果逐一对拍旧实现，diff 为零才许过）；不打包不 commit；AI-GameStudio 不碰（config 已由总监修）。

## 验收标准

- [ ] 自检面板：技术策划注册后 config 完整性绿灯（用改后代码+现网 config 桩测）；岗位协议报「7 份齐全」
- [ ] poolFor 新旧对拍表（六职能+技术策划）零差异 + 「编制有老表无」场景新测试
- [ ] 凭据卡截图前后对照（叠字消失/圆点语义表/按钮行为说明）
- [ ] `npm test` 全量 0 失败；完工纪要逐条证据
