# 施工令-057 · OAuth 哨兵二期（Q14+Q15）

案源：055 上线首日实证两处（2026-08-13）：① 拒派日志逐拍刷 journal（16:43-16:44 三连同文）；② 探针 `claude -p` 可触发 refreshToken 自动续期（16:49 实证 +8h），哨兵应先自续、续败才叫人。写区：055 同面（`apps/studio/lib/` 哨兵与 runner 预检）＋测试。

## 要件
1. **拒派日志节流**：同一单同一因的拒派，仅状态变化时记 journal（首次拒派记一条、恢复派发记一条；期间静默计数，恢复条附「期间拒派 N 次」）；
2. **临期自动续期**：哨兵检测剩余 <30 分钟时，先以无头探针触发续期（spawn `claude.exe -p` 短问答，带代理 env，超时 60s）；续成记 journal「OAuth 自续成功 +Xh」不发急件；续败（401/超时）才发急件叫人（沿用既有配方文本）；
3. 探针本身节流：每到期窗口至多尝试 2 次，防循环烧调用；
4. 测试：节流分支（同因去重/恢复附计数）、自续成功/失败/超时三分支（探针可注入桩）；全量零回归。

## 验收
逐条自证+全量绿；回执 工程队/回执-057.md；git 提交本令文件面；不打包（总监统一换装）。
