# 施工令-022 · 流程视图重构：现在线管线甘特（制作人 2026-08-08 22:38 批准）

设计稿定稿：`工程队/设计稿-022-流程甘特.html`（随单入仓，视觉逐稿对齐，色值换语义令牌；「现在线」橙 --now 需新增双主题令牌）。制作人追加：**挂起/归档/废弃/冻结分类折叠，不许堆页面**。

## 范围（apps/studio public/app.js viewFlow 整体重构 + style.css + 必要的 server 字段透传）

1. **结构=行为管线、现在线三分段**：沉淀（折叠）｜现在（橙区）｜接下来（依赖序虚框）。管线=项目管线注册+特殊行（无管线归属的活动单归「散单」行）。
2. **现在区**：在做单实心条+实时百分比（接 /api/runner 进度字段，15s 活体刷新同在途页）；管线无在做→「闲置 N 天」直书（取该管线最近事件时间差）。
3. **接下来区**：待投/草稿/池中单按依赖拓扑排序，`←依赖号` 角标；签字位（保留单待验收/待拍板协同块）绿框 ✍ 常驻；专项终审做里程碑旗 ⚑。
4. **沉淀抽屉（制作人追加重点）**：每管线一行折叠条，分类计数分开列——`▸ 完成 12 · ❄挂起 2 · 废弃 3 · 推翻 1`；点某一类只展开该类（置灰列表，点条进详情）；**默认全折叠**，页面上只活人说话。
5. 点击任何条→详情页；父单/专项条点击进父单。
6. 旧 viewFlow 的泳道/显示历史机制整体退役（006 的签字标语义并入新结构）。

## 不要做
- 不做拖拽排期/时间刻度轴（v2 候选）；不改任何调度逻辑；不打包不提交；AI-GameStudio 不碰。

## 验收标准
- [ ] 三分段渲染+闲置直书+依赖序（构造多管线样例实机取证）
- [ ] 沉淀抽屉分类折叠：四类分计数、单类展开、默认全折、❄/废弃/推翻视觉区分（实机取证）
- [ ] 在做条百分比 15s 活体（桩数据取证）；签字绿框与⚑在位
- [ ] 双主题令牌（含新 --now）；860px 无横向溢出
- [ ] `npm test` 全量 0 失败（基线 35 套件 334 用例）；完工纪要逐条证据
