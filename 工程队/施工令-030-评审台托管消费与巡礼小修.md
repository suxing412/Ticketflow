# 施工令-030 · 评审台接 DPAPI 托管 + W32 补巡三小修（总监夜班立单 2026-08-09 04:25，案源 H94 补巡处置表）

## 范围

1. **评审台失明修复（P1，本单主件）**：`packages/review-panel/review.js` 的评审团自动发现读 `studio.config.json` 兼容段明文 key——key 迁 DPAPI 后 deepseek 失席。改「托管优先、config 兜底」，消费口径与 apps/studio 029 改造完全同构（DPAPI 解密走 `apps/studio/lib/creds.js` 的读取口；review.js 是包，注意消费方式：优先复用 creds 模块——包内 require 相对路径按并排约定处理，且 studio 不在场时（协作者环境无私仓）**优雅回落 config/环境变量，不硬崩**）。测试：托管命中/兜底/双缺失三态（假夹具，零真调）。
2. **报表页 860 溢出**：`div.vin` 宽表加横滚包裹容器（`overflow-x:auto`），页面文档宽=视口宽。
3. **触点地板**：wiki `.wk-l` 词条链接与决策台裸引导链触点高度提到 ≥26px（padding 调整，不改字号布局意图）。
4. **流程页 `%` 字形**：860 下 9px 的 `<small>` 提到 10px 地板。
5. `npm test` 全量零回归（studio 基线 349 + review-panel 16 + 新增）；双主题+860 复查涉改区。

## 不要做

决策台「等你的事」跳板（处置表 #5）是建设性提案，**等制作人批准另办**，本单不做；不打包不 commit；不动 AI-GameStudio；key 明文零出现（同 029 铁律）。

## 验收标准

- [ ] 评审台三态测试全绿 + 本机实证：不带明文 config 跑 `review.js --dry` 能列出 deepseek 在席（从托管取）
- [ ] 860 实测：报表页文档宽=视口宽；wiki/决策台触点 ≥26px；流程页最小字号 ≥10px
- [ ] 全量测试 0 失败；完工纪要逐条证据
