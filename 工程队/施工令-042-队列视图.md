# 施工令-042 · 队列视图（项目内队列页 + 主页工程队队列）

案由：制作人 2026-08-11 02:38 令「我在监制台需要在项目里有队列视图，我现在完全看不到后续的队列里排了什么东西，监制台主页放工程队的队列视图——这俩都要落实」。
前置：施工令-041（三消费接线）已落地；数据源=排程台账（lib/pm/schedule.js，生产账已有 15 粒）。

## 一、项目内「队列」视图（新页签）

- 导航新增「队列」页签（位置：流程与在途之间），路由 `#/queue`。
- 内容=本项目（按管线字段过滤，含无管线的项目相关批）全量计划粒，按 批→序 分组列表：状态徽章（计划/起草中/已成单→链到单号/完成/撤销）、题、预估单元、池衡建议、来源（悬浮显全文）、依赖（有未满足依赖的置灰并标「候：xxx」）。
- 顶部摘要：`N 批 · M 项未完 · 预估合计 X 单元`。
- 已完成批默认折叠。空态：「队列空——批次拍板后由总监/项管登记」。

## 二、主页（hub）工程队队列视图

- hub 的工程队状态卡扩展为**队列卡**：当前施工令（041 直读逻辑保留）+ 其下列出「Q队列」批的计划粒（同上样式精简版，最多 8 行，余数折叠）。
- 制作人开机第一眼即见：监制台自身接下来要修什么。

## 三、验收

1. 纯函数级测试：队列分组/依赖置灰判定/摘要统计/项目过滤，≥6 例；既有测试不回归。
2. 回执 `工程队/回执-042.md` 逐条自证+测试输出。
3. git 提交本令文件面。不打包不换装（总监随后 deploy-ritual 一次换装 040+041+042）。
