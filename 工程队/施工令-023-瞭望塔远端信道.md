# 施工令-023 · 瞭望塔第五信源：远端信道（制作人 2026-08-08 23:01 批准）

## 范围（套件/瞭望塔/watchtower.js + 规则.json 默认规则）

1. **信源⑤远端**：每 N 分钟（默认 5，规则表可调）对配置的仓清单（默认 D:\GitHub\Ticketflow）执行 `git fetch --all --prune`（静默容错：网络断=跳过本轮不报错，恢复即续）；比对上轮快照检测：①远端跟踪分支新提交（含 main）②新分支/删分支③新提交中触及 `docs/` 下 交接/回执/信道 类 md 的单独标记为「信道文书」。
2. **事件与默认规则**：`[远端] 信道文书` → 弹通知+记未读（这是「有信来了」）；`[远端] main 新提交/新分支` → 记未读+流水（不弹）；全部进统一流水（总监监视器即被唤醒）。
3. 快照持久化（`瞭望塔/远端游标.json`），重启不重报；fetch 用 `--quiet` 且工作区零改动（只动 refs）。
4. 测试：假远端仓（本地 bare 仓当 origin）推提交/建分支/信道文件三态各验；断网容错；游标幂等。

## 不要做
不自动 pull/merge（只 fetch 侦察）；不碰工作区文件；不 commit 不打包；AI-GameStudio 只写瞭望塔自己的输出。
