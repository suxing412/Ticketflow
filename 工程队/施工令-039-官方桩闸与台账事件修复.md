# 施工令-039 · 官方桩闸 + 台账事件落盘修复（总监立单 2026-08-09 19:06，案源 037 事故与勘察）

## 范围（apps/studio）

1. **官方桩闸（037 真派发事故的结构根治，021 同族第二案）**：`server.js` 识别环境变量 `STUDIO_STUB=1`——置位时执行器派发面硬关（runner 不 start、tick 空转、/api/runner 报 `桩台:true 运行:false`）、外呼类（quota 真调/netprobe 探测）一并停用，启动日志醒目打「桩台模式：零派发零计费」。工程队桩台从此一个环境变量开闸，不再各自手搓 Module._load 钩子（历史两起事故全因手搓漏面）。测试：置位起服务→派发面全哑断言；不置位行为逐位不变。
2. **台账两类事件落盘修复（037 勘察实锤，制作人 19:17 简报补细）**：`server.js:237`（派单委托）与 `:1028`（定稿放行）`require('./lib/ledger')` 错指——该模块只有 `commitStudio` 无 `.event`，TypeError 被空 catch 吞掉，两类事件从未落盘（现网事件.jsonl 570 行零命中，可复核）。修法：**直接用 server.js 顶部既有的 `pmLedger` 常量**（`lib/pm/ledger` 才是事件属主，有 event/events/read/write/score）；空 catch 至少留一行 journal 或 console.error——错了一个月无人发现，吞异常是帮凶。补测试断言两类事件确实落盘读回；037 的 chain.js 委托事由 30 分钟窗配对随之自动生效，验证一例（起草站从「项管单张起草」套话变成真实委托事由摘要）。语义确认写进纪要：event() 只 append jsonl+可能投信箱，不改状态机、零调度语义影响（本项属 037 明令划出的写入面，独立处置正确）。
3. `npm test` 全量零回归（基线以 038 落库后为准）。

## 不要做

与 038 串行（其落库后开工）；不动调度语义；不打包不 commit；AI-GameStudio 不碰。

## 验收标准

- [ ] STUDIO_STUB 置位/不置位两态测试；桩台起服务实测零派发（/api/runner 恒哑）
- [ ] 两类事件落盘读回+关键汇报链上「受托起草→委托事由」配对实测
- [ ] 全量测试 0 失败；完工纪要逐条证据
