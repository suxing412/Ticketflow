# 施工令-006 · 流程死胡同与签字位可见性

制作人反馈（2026-08-06 21:08，三张实拍）：「监制台本身还是有很多问题」。本令收三处已坐实病灶。

## 范围（apps/studio/public/app.js + style.css）

1. **流程页空态死胡同**（app.js:451 一带）：
   - 全部组被折叠时，空态卡内必须给「显示历史」按钮本体（现在文案让点但按钮不存在）；
   - **待验收/待定夺的专项组不得折入「历史」**——折叠判据从「组内无在途」改为「组内全部为 完成/已归档」；待验收组常驻显示并带「等制作人签字」标记。
2. **树形父单状态徽章**（app.js:645 一带）：父单也渲染状态 pill；待验收态额外亮「等你签字」醒目徽章（okbg 系，不用红）——制作人的签字队列在树形必须一眼可见。
3. **参数页口径两处**：
   - 额度双池卡：阈值标注写明作用窗（如「阈值 70%（管 5h 窗）」），不受阈值管的窗口不并排暗示违规；周窗超沟通保留线时给中性提示（「周 61%·沟通保留线 50% 已越，claude 生产单停拉」——这是 dispatch.js 沟通护城河的真实行为，现在 UI 完全没提示）。
   - 代裁模型下拉：空值显示文案改「跟核查档」，与旁注一致（现显「CLI 默认」自相矛盾）。

## 不要做

- 不碰调度/闸/判官逻辑；不改 dispatch.js 行为本身（只把它已有行为如实呈现）。
- 不打包不提交；不碰 AI-GameStudio。

## 验收标准

- [ ] 构造全折叠态：空态卡内有可点的「显示历史」且点击生效（实机取证）
- [ ] 构造待验收专项：流程页该组常驻+「等制作人签字」标记；不随「折叠已完成」消失
- [ ] 树形父单显状态 pill；待验收父单显「等你签字」徽章（实机取证长短两例）
- [ ] 额度卡阈值窗口标注正确；周窗越保留线时提示语出现、未越时不出现（两态取证）
- [ ] 代裁空值文案=「跟核查档」
- [ ] `npm test` 全量 0 失败；完工纪要含逐条证据
