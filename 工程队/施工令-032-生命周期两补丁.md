# 施工令-032 · 生命周期两补丁：返修掐会话 + 引擎门禁停闸（H97，制作人 2026-08-09 12:59 批准）

## 范围（apps/studio）

1. **返修先掐在飞审检（竞态修补）**：`返修` 动作在移单前调用 `runner.killTicket`（对该单在飞的初检/深检/核查会话一并终止）——与「废弃/收回」既有处理、挂起「先掐会话后冻结」同族。案源：TK-113 返修与在飞初检/核查相撞，孤儿核查会话在旧态上空转。测试：构造在飞审检时返修，断言会话终止、状态迁移干净、无孤儿判词落 fm。
2. **引擎门禁停闸（H97）**：工单验收标准正文命中引擎门禁特征（`enginectl`/`unity-test`/`受控重建` 可配置正则）时，`核查通过` 不再直转完成——停在待验收并盖 `fm.待引擎实证: true`，流水记「核查过·候引擎实证」；新 API 动作 `实证放行`（总监确认审检证据已入回执后调用→转完成）。既有「验收 通过」人闸路径不受影响。测试：门禁单核查通过后停闸、实证放行转完成、非门禁单直转完成不受影响。
3. `npm test` 全量零回归。

## 不要做

与 031 串行施工（同仓 runner/lifecycle 面，031 落库后开工）；不打包不 commit；AI-GameStudio 不碰。

## 验收标准

- [ ] 竞态场景测试（在飞审检被掐+无孤儿判词）
- [ ] 停闸三态测试（门禁停/放行转完/非门禁直通）
- [ ] 全量零回归；完工纪要逐条证据
