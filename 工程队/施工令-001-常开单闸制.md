# 施工令-001 · 常开单闸制（H81）

制作人裁决（2026-08-06）：跑是常态，停是例外。拆除历史包袱开关，执行链默认常开。
案源：2026-08-06 换装后全局暂停闸漏开，四张放行单滞留 9.5 小时零派发无告警（坑档案有案）。

## 范围（仓：D:\GitHub\Ticketflow，工区：apps/studio）

1. **拆除「实弹解锁」开关**：config / 状态 / server 端点 / UI 开关全部删除，执行器只要「运行」即实弹。
2. **拆除生产态「试跑」开关**：从 config、UI、/api/runner 对外状态中删除。**注意**：测试套件靠试跑语义做同步执行（`runner.tick(root, CFG, {durMs})` + `s.执行器.试跑`），这层语义收进测试内部钩子（如 tick 选项或 test-only 标志），不得破坏任何现有测试的同步执行能力。
3. **暂停闸三档收成唯一总闸**：`paused.{global,codex,claude}` 收成单一布尔总闸，默认开（不暂停）；`/api/gate/pause` 收成无 scope 的单开关；主页（hub）一个「停/开」按钮，闸位状态一眼可见（合闸时页面要有醒目常驻提示，不是埋在角落）。额度锁（自动）与编辑器锁（H64 手动）不动——它们职责不同，保留。
4. **零派发告警**：巡检（15 分钟巡检所在处）新增——就绪队列非空（有放行且依赖就绪的待投单）且连续 ≥2 个巡检周期零派发、零执行中 → 写呼叫信箱告警一条（含滞留单号清单）。
5. 迁移：旧 state 文件里遗留的 试跑/实弹解锁/paused 三档字段，读到时静默迁移为新形态，不炸。

## 不要做

- 不碰 额度锁 / 编辑器锁 / 单张放行 / QA·两检判官链逻辑。
- 不碰 studio.config.json 部署实例（D:\GitHub\AI-GameStudio 下的一切）——那是生产数据区；只改仓内代码与仓内样例配置。
- 不打包不换装（攒批，由总监按 deploy-ritual 执行）。
- 不动 DeepSeek 密钥相关代码路径，不在任何输出中回显密钥。
- 不提交 git（总监验收后统一提交）。

## 验收标准

- [ ] 全仓 grep「实弹解锁」生产代码零残留（历史文档/注释案底可留）
- [ ] 生产代码与 /api/runner 对外状态无「试跑」字段；全量测试套件不改断言语义仍全绿
- [ ] /api/gates 返回单一总闸布尔；三档 paused 端点与 UI 拆干净
- [ ] hub 有停/开按钮；合闸时有常驻醒目提示
- [ ] 巡检零派发告警有单元/集成测试覆盖（就绪有单+连续零派发 → 信箱有告警）
- [ ] `npm test`（apps/studio 下）全量 0 失败，逐屏可查
- [ ] 交付一份完工纪要：改动文件清单 + 每条验收标准的证据（测试名/grep 命令）
