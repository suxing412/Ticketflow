# 施工令-051 · 产出速览解析收敛

案源：制作人 2026-08-12 21:28 截图——TK-160 详情页产出速览把「Tools/TK/汉代地图/手修编辑器」（菜单路径）「2.21/2.46 km」「0.00/0.00 km」（数字串）当交付文件去磁盘找，找不到扣红「缺失」，满屏假红误导验收观感（TK-156 亦现一枚）。

写区：产出速览解析所在（`lib/artifacts.js` 或等价）＋前端呈现（`public/app.js`）＋对应测试。

## 要件

1. **只认真路径**：候选串须形如仓内文件路径（含目录分隔且带扩展名，或磁盘实测存在）才进产出清单；纯数字/单位串（`\d+(\.\d+)?\s*(km|ms|%|s|MB)` 等）、Unity 菜单路径（`Tools/`、`SLG/` 开头且无扩展名）、URL 一律不作产出物。
2. **假红降级**：解析不出任何合法产出 → 显示「无产出物声明」中性占位，不显示红「缺失」；真路径但磁盘缺失才红。
3. **测试**：解析函数分支（真路径存在/真路径缺失/菜单路径剔除/数字串剔除/混合清单）；用 TK-156/160 两张真实回执做夹具回归；全量零回归。

## 验收

逐条自证+全量绿；回执 `工程队/回执-051.md` 附 TK-160 回执重解析前后对比（三枚假红应消失）；git 提交本令文件面；不打包（总监统一换装）。
