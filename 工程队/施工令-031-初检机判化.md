# 施工令-031 · 初检机判化（H96，制作人 2026-08-09 12:59 批准）

案源：flash 初检一夜五次形式性误拦（含两次拦截单据明文豁免条款）；TK-102 长回执截断误杀。LLM 干 schema 校验的活=错配。

## 范围（apps/studio）

1. **新建 `lib/precheck.js` 纯代码初检**，逐项可配置（阈值走 config，硬编码为默认值）：
   - 回执存在且非空；**尺寸上限**（默认 2MB）超限即拒并注明「截断风险」；
   - 章节齐备：按单据「验收标准」条目数对照回执逐条应答（✓/✗/或**豁免措辞命中**）；
   - 字段枚举：fm 必填项（初检要求的结构位）；
   - **禁语表**：「In progress/Waiting/等待确认」等空壳标志——但**豁免白名单优先**：单据正文含「总监代劳」「不判失分」「初检不得以占位判缺项」类条款时，对应条目占位不判缺项（豁免命中记入初检备注，透明可审）；
   - 输出与现行初检同构（fm.初检 结论/时间/备注），备注列明逐项判定与豁免记录。
2. **runner 接线**：`初检` kind 从 flash CLI 调用改为进程内调 `precheck.js`（零 token 零会话）；flash 路径保留为 config 开关 `初检.二线LLM`（默认关）——留一条回滚路。
3. **测试**：施工令案例回归——TK-102 截断场景（2MB 尾部藏 ERROR）、TK-112/113 豁免措辞场景（占位+豁免条款→过）、真空壳场景（无豁免条款+占位→拒）、禁语命中、章节缺失；`npm test` 全量零回归。
4. 流水/台账事件文案照旧（「两检初检」标注「机判」）。

## 不要做

不动深检/核查（opus 内容质量层不变）；不动 QA 语义；不打包不 commit；AI-GameStudio 不碰（config 新开关缺省值在代码内兜底）。

## 验收标准

- [ ] 五类场景测试全绿（截断/豁免/空壳/禁语/缺章）+ 全量零回归
- [ ] 桩台实测一单走通机判初检全链（fm 写入同构）
- [ ] 完工纪要逐条证据
