# 施工令-010 · 并发去写死归项管 + 零输出看门狗 + codex 窗口正名（制作人 2026-08-06 23:59 批准）

设定考古（H86）：D17「编制即上限」已随派发制迁移为池并发语义（决议史补记随八站传播落）；H85 编制权归项管——本单把并发调配权同规格下放；硬顶=成本保险丝仅制作人可改（23:29 拍板）。审检并行被制作人明确否决为默认——只允许项管按积压动态调，代码不写死。

## 范围（apps/studio）

1. **审检并发去写死**（lib/runner.js）：`running.has('两检初检')`/`running.has('核查')`/`running.has('仲裁')` 的单槽写死改为读 `config.并发.审检`（默认 1，硬顶 2）——同类判官在跑数 < 配额才开新槽。池并发现有配置维持，全部并发参数聚合进 `config.并发` 段（含硬顶字段 `硬顶` 仅文档声明制作人专属，代码只校验不越硬顶）。
2. **并发调配权归项管**：/api/pm/roster 的 POST 扩一个兄弟端点或同端点扩字段（形态自定，保持记账口径一致）：项管可改 `config.并发` 内各配额（不得越硬顶，越则 400）；journal+台账「并发调配」。brain.js 提示词补一句调配语境（grep 验证）。
3. **零输出看门狗**（lib/pm/patrol.js）：执行会话拉起 ≥N 分钟（`config.并发.零输出分钟` 默认 8）tail 仍为空 → 呼叫信箱急件一条（含单号/池/已历时）+ journal；同一会话只报一次。案源：TK-102 codex 会话零输出挂 48 分钟无人察觉（2026-08-07 00:06 制作人亲自问出）。测试覆盖：零输出超时报/有输出不报/同会话不重复报。
4. **codex 窗口正名**（lib/quota.js + gates 文案 + 额度卡）：codex 现实只有周窗——锁文案不得再写「5小时已用」，按实际窗口 label 呈现；额度卡 codex 只画周条。显示层为主，判定逻辑（拿唯一窗口比阈值）不变。

## 追加范围（总监 00:23 实测定谳，与第 3 条同族）

5. **codex tail 观测盲区修复**（lib/runner.js spawn 段）：codex CLI 的过程输出走 **stderr**，stdout 只在收尾吐最终答案——现 entry.tail 只喂 stdout，导致 codex 会话全程显示「尚无输出」（TK-102 案，或致 00:01 一次冤杀收回）。修法：非 stream 路径的 tail/tail3 兼收 stderr（stdout 优先、无 stdout 时用 stderr 最近行，过滤 ANSI 控制符）；第 3 条零输出看门狗的「活性」判据= stdout∪stderr 任一有新字节。实测参考：runner 式 spawn codex，过程行「codex/tokens used」全在 stderr，stdout 仅 6 字节终答。

## 不要做

- 不改判官逻辑本身（只改槽数来源）；不动 dispatch 池路由（008 刚落）；不打包不提交；不碰 AI-GameStudio。

## 验收标准

- [ ] 审检并发=1 时行为与现状逐位一致（既有测试全绿零改断言）；=2 时两张待验收可并行初检/核查（新测试）
- [ ] 项管调配端点：合法改动生效+记账；越硬顶 400 拒绝（测试）
- [ ] 零输出看门狗三态测试全绿；信箱急件含单号与历时
- [ ] codex 锁文案与额度卡无「5小时」字样（周窗如实），claude 双窗不受影响（实机取证）
- [ ] `npm test` 全量 0 失败；完工纪要逐条证据
