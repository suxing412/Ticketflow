# 施工令-002 · 短题制（H83）+ 工程队状态卡

制作人裁决（2026-08-06 13:23）：工单卡片标题必须短到不点详情也能认单；长标题「每一个都要点进去看很烦」。
排队纪律：本令与施工令-001 共用 app.js/server.js，**等 001 完工合并后再开工**。

## 范围（仓：D:\GitHub\Ticketflow，工区：apps/studio）

1. **短题制·源头**（lib/pm/brain.js）：切单纪律「标题规范 ≤16 字对象+动作」的适用面从子单扩展到**全部单型（含专项/父单/机制单）**；范围枚举（①②③…）一律进正文「范围」章，禁入标题。切单与起草两处提示词同步，改后 grep 验证生效。
2. **短题制·预检**（server.js preflight）：标题 >24 字或含「①」类枚举符 → 预检**警示不拦截**（老单与在途单不受影响）。
3. **短题制·兜底**（public/app.js + style.css）：工单卡片（工单池/总览需你处理/树形/决策台队列）标题统一两行截断（line-clamp），悬停显全文 title 属性；详情页不截断。
4. **动态日志渲染泄漏修复**（public/app.js pmEventLine 一带）：多行 journal 条目（如判官结论正文）泄漏到事件行下方——事件行只取首行，超出截断。案：2026-08-06 13:14 开工事件下方漏出「结论：不过/质量分：1」。
5. **工程队状态卡**（public/app.js hub + server.js）：主页新增只读「工程队」卡片——读 `D:\GitHub\Ticketflow\工程队\状态.json`（若无文件则卡片不渲染）。字段约定：`{"施工令":"001","名称":"…","状态":"施工中|待验收|完工","更新时间":"ISO"}`。监制台只读不写；工程队侧由总监维护该文件。

## 不要做

- 不碰调度/闸/判官链逻辑（001 的领地）。
- 不改历史工单文件的标题。
- 不打包不换装不提交 git（攒批）。
- 不碰 D:\GitHub\AI-GameStudio 生产数据区。

## 验收标准

- [ ] brain.js 切单与起草提示词均含全单型短题条款（grep 命中 2 处）
- [ ] preflight：>24 字或含枚举符的标题产生警示且不拦截（测试覆盖）
- [ ] 四处卡片标题两行截断 + title 悬停全文；详情页完整
- [ ] 动态日志多行条目只显首行（用含换行的 journal 样例测试）
- [ ] 状态卡：有状态文件时渲染、无文件不渲染（测试或截图证据）
- [ ] `npm test` 全量 0 失败
- [ ] 完工纪要：改动清单 + 逐条验收证据
