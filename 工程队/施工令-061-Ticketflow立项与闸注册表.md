# 施工令-061 · Ticketflow 立项 + 闸注册表 + 决策台撤除（末道施工令）

> **本令是施工令通道的最后一道。** 制作人 2026-08-20 00:45 拍板：监制台自维护自立为第二个项目 **Ticketflow（前缀 TF）**，施工令通道整体退役，此后监制台自维护走正规工单同一条生命周期。本令自己就是那次交接——它落地之后，工程队不再出令。

案源链（2026-08-19 18:44 → 08-20 02:34，制作人逐轮拍板，全部已誊入 C-规章/项目管理章程「双队列制与监制台自立项目」章）：

1. 08-19 18:44「流程和队列以及项管页其实都没能做到我想要的效果，就是浏览并管理之后的计划，之前的成果历史，当下在做的东西有在途页在显示，你按照第一性原理合并且重构这三个页签吧」
2. 08-20 00:41「我们始终只维护两个队列」——工单队列＋待办队列（原「计划粒」改名，制作人定名）；排期全权归项管并产甘特图；**待办→工单是人闸，制作人与总监把持**
3. 08-20 00:45「监制台自维护自优化工程可不可以算作监制台开的第二个项目」→ 拍板立项，**不挂管线**（管线是 TK 的命名空间，00:49）
4. 08-20 01:23「报表本身只应该处理消耗的 token 和时间这种资源成本问题」→ 报表纯资源成本面；待办队列落项管页
5. 08-20 02:34 决策台裁决：**撤除**。制作人原话「其实就是应该看着这张单子的详情签这张单子」

## 一、Ticketflow 立项（本令的承重件，先落）

1. `studio.config.json` 项目注册表加第二项：`Ticketflow` → 路径 `D:/GitHub/Ticketflow`，单号前缀 `TF`，无引擎。
2. **派号去 TK 硬编码**：`lib/pm/brain.js:558-561` 现写死 `const px = 'TK'` 与 `/^TK-([0-9]+)$/`，改为按工单/上下文的 `项目` 字段查注册表取前缀，缺省回落项目默认。同处扫齐 `:244/:253/:331`、`lib/pm/ideas.js:51`、`lib/specials.js:197/310`、`server.js:996/1279` 的 `'TK'` 兜底，统一走一个 `前缀Of(cfg, 项目)` 助手。
3. **两项目号段互不相扰**：TF 从 `TF-1` 起派；一条测试断言「注册两项目后各自派号互不串号、不重号」。
4. 施工令历史归档留档，不迁不删（`工程队/` 原样保留作史料）。

## 二、闸注册表 + `等我()`（决策台撤除的前置，必须先建后删）

1. **闸注册表** `监制台/闸注册表.json`：每条 = 闸号｜法源条文｜实体类型｜pending 判据（纯函数名）｜落点页+按钮｜验收方式。**今日应登记 12 条**：①投池放行 ②待定夺拍板 ③保留单终审 ④入库/入标杆 ⑤a撤回 ⑤b废弃 ＋ H103 专项关账 ＋ H49 想法拍板 ＋ 待办放行（待办→工单）＋ H51 管线开线 ＋ H52 wiki 入册 ＋ H64 编辑器锁。
   **注册表不住在任何页面里**——账本独立于渲染器，撤页不损完备性。
2. **服务端唯一谓词 `等我()`**：由注册表驱动，取代 `/api/decisions`。定义域是**闸**不是**工单状态**（今日病根：`server.js:1224` 把队列写死成 待验收∪待定夺，故专项关账这类非工单闸结构上不可见）。内建流程页 `人闸()` 的严判据（剔除委托待验收、剔除有活跃会话，施工令-038 案源）。
   **两条硬约束**：①只读权威状态（工单 fm／专项 json／wiki 待审目录／管线注册表），**严禁以 inbox 通知为数据源**（377 条未读即通知源不可信的实证）；②每笔带 `gateKey = 闸号:实体id`，按 key 幂等去重，签完即消失不重复催。
3. **单一计数**：`server.js:1160` 待签、`lib/pm/schedule-view.js:83` 「等你 K 项」、`lib/pm/chain.js:196` 人闸徽章、项管页待办放行数、专项待签数——全部改为消费 `等我()`；页内子集只许对结果 filter。**lint：任何拼出「等你/待签」却不经 `等我()` 的 `.length` 即违规。**（今天有四把互不引用的尺子。）
4. **超时升格**：任一 gateKey pending 超 **24 小时**（总监默认值，候制作人改）→ 收件箱升「急」＋晨报置顶＋总览红条。**这是整套里唯一会主动找人的机制**——页面不会（实证：TK-180 在决策台躺 26 小时无声、编辑器锁卡 15 天零触发）。
5. **夹具矩阵**（替代观察法，消灭循环论证）：注册表每个 key 各造一条 pending 夹具，断言 `等我()` 全收；**新增人闸未登记、或登记后无夹具 → 构建失败**。不得用「journal 已出现过的事件类型 × 当前 pending」作判据——该判据对从未触发过的闸恒绿，恰好漏掉最该防的一类（H51 是活样本）。

## 三、页签重排 11 → 8

| 页签 | 本性 | 变动 |
|---|---|---|
| 总览 | 门厅 ＋ **人闸唯一召唤面** | 收件箱改渲染 `等我()` 全集，按停摆时长降序，每行带闸号＋已停时长，点击按注册表落点直达并高亮按钮；空态文案改「你不欠任何签字」（改造后**第一次可证**） |
| **工单** | 结构面 | 新页：管线卡片 →（点入）专项卡片层，按编号升序、子单默认折叠、展开就地列子单、点子单直达详情页；直属散单虚线卡末位。**专项页并入此层，H103 关账签字按钮落专项卡** |
| **看板** | 活态流转 | 原「工单」改名；**完成/已归档两列整体移出**（制作人 01:18 定），列数 10→8，落袋即离开看板 |
| 在途 | 执行现场 | 项管页编制快照并入页头只读折叠区；**H64 编辑器锁卡迁入此页**（总监默认，候制作人否决） |
| **项管** | **待办队列＋甘特＋排期**（未来面） | 队列页并入；想法页并入（H49 拍板按钮随入）；**待办→工单那道闸在这一页**，按钮只对制作人与总监开放 |
| 美术 | 常驻设施 | 新页：ComfyUI 实时（当前节点＋步数＋队列＋产出）；地址取 artgen 同源配置，**不得另写死一份** |
| 报表 | **纯资源成本** | token／工时／估时准度／管理费／每日交付 ＋ **复盘时间轴**（默认最近 20 单）。**不收档案检索**（制作人 01:23：报表只应处理消耗的 token 和时间这种资源成本问题） |
| Wiki | 知识 | 不动 |

**撤除**：想法、专项、流程、队列、**决策台**。
**旧书签重定向**（各一条测试）：`#/ideas`→`#/`、`#/specials`→`#/tickets`、`#/flow`→`#/`、`#/queue`→`#/relay`、`#/decisions`→`#/`、`#/tree`→`#/`、`#/board` 保留。

## 四、决策台撤除的次序闸（硬前置，不设日落条款）

删页是销毁性判断（A-治理 二·派生规则「销毁性判断永远归人」），故**必须先建后删**，且不设自动触发：

1. `等我()` ＋ 注册表 ＋ 总览收件箱切换 **只读上线，与现网对拍一个周期**；
2. **工单详情页补单张「通过入库」**（今日全站唯一入口在决策台 `app.js:1165`，删页早于建钮会出现保留单签不了字的空窗——8 月 122 次验收里 36 次是代劳，空窗期只剩代签，直踩 D11 案源）；
3. 三个孤儿闸安家：H103 关账→工单页专项卡、H49 拍板→项管页、H64 锁→在途页；
4. 三条全绿之后，才删 `app.js:1126-1192` 与其余四页。

## 五、双队列制落地

1. **「计划粒」全库改名「待办」**（制作人定名）：`lib/pm/schedule.js`、`schedule-view.js`、UI 文案、协议文本一并改；粒 ID 与数据格式不动（只改称谓，零迁移）。
2. **僵尸清账**：Q·11~15 五粒（对应施工令 054/051/055/057 已交付、回执在册）与项管台账 5 粒孤粒（2026-08-12 报出后无人管）一次性销项。
3. **对齐器补第二路**：`lib/pm/ledger-sync.js` 现只跟 TK 工单跑，补一路扫 TF 工单——立项后 Q 系列自然成为 TF 单，粒进得来也出得去，僵尸病根消失。

## 六、自我修改纪律（制作人 01:18 同意）

涉**派发／闸门／权限／人闸判据**写区的单，`验收方式` 一律取 `保留`（制作人签字），不得委托验收。本令二、四两节全部落在该范围内。

## 不要做

- 不改工单详情页既有行为（只加一颗「通过入库」按钮）；
- 不动工单数据、专项落盘格式、管线注册表格式；
- 不打包换装（总监统一走 deploy-ritual）；
- 不删 `工程队/` 历史（史料保留）。

## 验收

1. 两项目各自派号互不串号、不重号（测试）；全库无 `'TK'` 硬编码兜底残留（grep 判据）。
2. 闸注册表 12 条齐备；夹具矩阵每 key 一条，`等我()` 全收（测试）；**新增闸未登记即构建失败**（负例测试）。
3. 四把尺子全部改吃 `等我()`；lint 命中零违规。
4. 超时升格：造一条 25 小时 pending 夹具，断言升「急」＋进晨报＋总览红条（测试）。
5. 页签 8 个且顺序如三；七条旧书签重定向各一条测试。
6. 次序闸：删页提交必须晚于「详情页通过入库上线」与「三孤儿闸安家」两个提交（git 顺序可判定）。
7. 「计划粒」全库零残留（grep）；Q·11~15 与 5 粒孤粒状态非「计划」。
8. `apps/studio` 全量 `npm test` 50 套件全绿，新增测试计入。
9. **完工判据制**：每条要件登记一条机器可执行验证命令并跑绿，输出摘录入回执。

## 备注 · 执行次序

一（立项与派号）必须最先——它是 TF 工单能存在的前提；此后二～五可拆成 TF-n 系列工单并行推进，本令随一落地即功成身退。
