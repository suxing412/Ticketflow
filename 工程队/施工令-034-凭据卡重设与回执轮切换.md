# 施工令-034 · 凭据卡重设 + 回执轮切换（制作人 2026-08-09 15:24 批准设计稿）

设计稿定稿：`工程队/设计稿-034-凭据卡.html`（视觉逐项对齐，色值换语义令牌）。制作人点名疑现网卡「顶着四个边」——边距列硬指标。

## 范围（apps/studio public/app.js + style.css）

1. **凭据卡按设计稿重构**：三行统一体例（状态点 8px · 名称 86px 列 · 类型胶囊[订阅/key·托管] · 状态一句话带次要小字 · 右侧动作按钮组）；开发者括号黑话全撤，副题一句人话（「订阅登录归各 CLI 保管 · API key 由监制台加密托管，绝不入配置文件」）；图例收右下角「● 状态图例」悬停提示（tooltip 自绘，禁原生 title 之外的原生件）；deepseek 行补端点/模型/迁入日期小字 + **「更换」按钮**（打开添加流程预填池名，成功即覆写托管条目——轮换 key 一步到位）。
2. **边距硬指标（制作人点名）**：卡内边距 ≥20px、行纵向 padding ≥10px、行间分隔线不贴边；**DOM 度量取证**（getComputedStyle 数值表进纪要），并全参数页抽查同类卡是否同病——有就同批修，别只医这一张。
3. **回执轮切换**：详情页回执卡按「## 第 N 轮回执」分段渲染页签（第 1 轮/第 2 轮/…/最新），默认最新轮；单轮回执无页签不变样。数据分段逻辑复用 app.js:1430 既有切分。
4. 双主题+860px 复查；`npm test` 全量零回归（基线 36 套件 391 例）。

## 不要做

不动 creds.js/server 数据面（027/029 刚稳）；「更换」只是前端流程复用添加接口，不新开后端；不打包不 commit；AI-GameStudio 不碰。

## 验收标准

- [ ] 凭据卡与设计稿逐项对齐（桩台 DOM 实录对照表）；边距度量表达标；同类卡抽查结论
- [ ] 轮切换实测（用 TK-114 三轮回执真数据的副本做夹具）；单轮单不变样
- [ ] 双主题/860 复查；全量测试 0 失败；完工纪要逐条证据
