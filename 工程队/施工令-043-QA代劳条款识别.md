# 施工令-043 · QA 提示词识别总监代劳条款（Q8）

案源：TK-138 死锁案（2026-08-11）——QA 以「全量数字缺」打回，但该验收项系总监代劳条款（执行者结构性不可得），自修空转两轮直至总监手术。同族时序摩擦当日再发两起（TK-142 核查、TK-135 初检），均靠人工裁解。

## 改动

`lib/pm/brain.js`（或 QA 提示词所在处）质检卷面规则补一条硬规则：

> 验收标准中凡载明「总监代跑/代劳实测后誊入/不判失分」或指向 enginectl 引擎实测的条目，执行方标注「待代跑」即视为该条**应答完备**——不得以「数字缺/未跑」为由打回或列为缺陷；判词中该条记「待总监代跑」。若执行方对此类条目**冒填数字**（非引用已存在的总监誊入段），按回执失实处理。

同规则同步到核查（代核）提示词。初检（机判 precheck.js）侧已有豁免机制不动。

## 验收

1. 提示词 diff 可指认（两处：质检+代核）；
2. `test/brain.test.js` 补一例：构造含代劳条款的验收标准+标「待代跑」的回执 → 生成的卷面提示词包含上述规则文本（纯文本断言即可）；
3. 全量 npm test 零回归；回执 `工程队/回执-043.md`；git 提交本令文件面。
