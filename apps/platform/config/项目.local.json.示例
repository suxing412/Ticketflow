{
  "_说明": [
    "改名为 项目.local.json 即生效（*.local.json 已被 .gitignore 挡住，不会入库）。",
    "",
    "项目注册表：agent 要改的是**你的仓**，而仓在哪台机器上都不一样，",
    "所以这份天生是机器相关的，入库那份只能是空壳。",
    "",
    "它同时是**写操作的白名单**——不在注册表里的仓，工作区服务一律拒绝往里提交。",
    "这不是「顺便」：`允许写` 打开的是能力，注册表划的是范围，两者缺一不可。",
    "所以别为了图省事把一整个盘根注册进来，那等于把范围划没了。",
    "",
    "路径写**绝对路径**，指向仓的**工作树根**（有 .git 的那一层）。",
    "反斜杠写两道（\\\\）或用正斜杠（/），两种都认。",
    "默认 指的是建单时不写「项目」字段就落到哪个项目，只注册一个时可以省略。",
    "",
    "界面上也能加：设置页「项目注册」（POST /api/setup/project），",
    "登记完当场生效不用重启，注销走 DELETE。用界面加的就是写进这个文件。"
  ],
  "注册": {
    "我的项目": {
      "路径": "D:\\Github\\我的项目"
    },
    "另一个项目": {
      "路径": "D:/Github/another-repo"
    }
  },
  "默认": "我的项目"
}
