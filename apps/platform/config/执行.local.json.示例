{
  "_说明": [
    "把本文件改名为 执行.local.json 即生效（*.local.json 已被 .gitignore 挡住，不会入库）。",
    "它按字段深合并覆盖 platform.config.json 的「执行」段——只写你要改的那几个键即可，",
    "不必把整段抄一遍（抄一遍会随主配置演进而过期，那是另一种分叉）。",
    "允许真跑 只是三闸之一：还需要 ① 请求体显式 干跑:false ② 该池在 预算.池 配了上限。",
    "并发 按 Provider 单独配置；未单独配置的 Provider 使用 默认，最小值为 1。",
    "预算上限写在 config/预算.local.json，见同目录 预算.local.json.示例。",
    "修复职责必须在 权限.放开 内，否则写权矛盾闸会在真跑前拒派。返修达到上限后挂起待人工。"
  ],
  "允许真跑": true,
  "并发": {
    "默认": 1,
    "claude": 2,
    "codex": 1,
    "glm": 1
  },
  "返修上限": 2,
  "权限": {
    "放开": ["backend", "frontend", "integrator", "修复"]
  }
}
