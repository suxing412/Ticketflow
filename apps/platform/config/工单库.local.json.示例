{
  "_说明": [
    "改名为 工单库.local.json 即生效（*.local.json 已被 .gitignore 挡住，不会入库）。",
    "",
    "这是新装一台机器**第一个**要配的东西：没有它，自检直接报「未就绪」，",
    "一句话是「连工单都没地方放——其余都谈不上」。工单接口一律 503。",
    "",
    "为什么不给默认值：工单是**业务数据**，该落你的业务私仓，不是落产品自己的安装目录。",
    "本产品不替你选位置——猜一个地方往里写业务数据，等你发现写错了，",
    "数据已经散在两处了，比直接报错严重得多。",
    "",
    "路径写**绝对路径**。JSON 里的反斜杠要写两道（\\\\），或者干脆用正斜杠（/），两种都认。",
    "目录不存在会自动建；已有工单的目录直接指过去就行，不会动里面的东西。",
    "",
    "也可以不写本文件，改设环境变量 PLATFORM_TICKETS 指向该目录。",
    "⚠ 环境变量**优先级更高**：两边都配了以环境变量为准，而这是最难自查的一种「没反应」。",
    "",
    "界面上也能配：首页工单区有输入框，填完当场生效不用重启（POST /api/setup/tickets）。"
  ],
  "根目录": "D:\\你的私仓\\工单"
}
