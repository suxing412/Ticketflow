{
  "_说明": [
    "改名为 workspace.local.json 即生效（*.local.json 已被 .gitignore 挡住，不会入库）。",
    "",
    "⚠ **本文件的内容就是「workspace」那一段的段体**，不要再套一层 { \"workspace\": {...} }。",
    "文件名即段名——这是所有 *.local.json 的统一规矩（执行.local.json 对应「执行」段，",
    "预算.local.json 对应「预算」段，以此类推），按字段深合并覆盖 platform.config.json。",
    "只写你要改的那几个键即可，不必把整段抄一遍（抄一遍会随主配置演进而过期）。",
    "",
    "允许写 是**独立的授权决定**，不随「服务起来了」顺带获得，所以默认关着。",
    "打开意味着：带令牌的调用方可以在**已注册的项目仓**里建分支、提交、合并。",
    "不开的话提交链一律 403——agent 能干活，但成果回不到你的仓里。",
    "写操作只认 项目.local.json 注册表里的仓，注册表本身就是写操作白名单。"
  ],
  "允许写": true,

  "_可选": [
    "以下都有合理默认值，不配也能跑。列在这里是为了让你知道它们存在。",
    "",
    "模式        direct = 直接在项目工作树里干活；isolated = 每张单一个 Git worktree（默认，推荐）",
    "根目录      worktree 落在哪，默认 workspaces/；若它落在被执行项目自己的工作树内，",
    "            会自动转去系统临时目录——worktree 不能安全地嵌在自己仓里",
    "分支前缀    分支名形如 <前缀>/<项目>/<单号>，默认 platform。",
    "            这个前缀会出现在**你自己的仓**里，改它是为了让人一眼看出分支是谁建的",
    "基线        新 worktree 从哪个 ref 切，默认 HEAD",
    "自动提交    干完自动打检查点，默认 true",
    "装依赖超时毫秒  monorepo 冷装几分钟是常态，默认 15 分钟",
    "port        工作区服务端口，默认 4371"
  ]
}
