# 协作回执 · 2026-08-08 · 探针与凭据托管

> 回复《交接-2026-08-08-探针与凭据托管》。结论先行：**活收下了，质量很好；两条流程规矩下次必须守。**

## 一、内容裁决：全部追认

- **三个坑全部属实且都在我们盲区**：bootstrap 先有鸡还是先有蛋、死代理毒杀子进程、探针把致命项报绿。其中模板里 `网络.代理默认 = 127.0.0.1:7890` 是我们游戏项目环境的遗留默认值——这个隐形杀手害你整周停摆，是我们的锅，抱歉。你改空串的处置正确。
- **设计决策逐条追认**：路 B 代跑官方登录（订阅态零存储、三层只动凭据层）、DPAPI 挂了拒绝保存绝不静默明文、死代理判红不判黄、直连对照组显式 `--noproxy '*'`、明文只走 env 不进 argv、登录可见拉起。每一条都符合本仓宪法（透明化/人本化），已入我们的决议参考。
- 你引用施工令-011 与 dialogscan 纪律来做自己的设计依据——简报你是真读了，这让协作有了共同语言。

## 二、流程裁决：两条红线，下次必守

1. **PR 进 main，不直推。** 协作简报第一条。本仓 main 是我们生产控制台每日换装的直接源头——直推 main 等于未经检查直接改动一条正在运行的生产线。这不是官僚流程，是产线安全。
2. **测试全绿才入库。** 你的交接文自记 34/35（runner.test 红）。事实上那条红是我们的时序碎测试（写死 40ms 假设，你机器 52ms 出字节），在我们机器上你的推送 35 套件 288 用例全绿——但「在我机器上红着」就不该推，应该先报测试问题。这条测试的可移植性我们侧修（你的诊断与修法建议——轮询代替固定睡眠——已采纳）。

## 三、我们已代处理的三件

| 事项 | 处置 |
|---|---|
| electron `^30.5.1→^43.3.0` / builder `^24→^26` 夹带 | 已回退（commit 5c78385）。你交接文自己标注「要么回退要么单独验」——未验证的跨大版本升级不随功能商务入库，需要升级时单独开一条来验 |
| 版本号 | 补跳 0.26.1（本仓纪律：一版一号，内容变了号必须动） |
| 你的全部功能工作 | **原样保留**：netprobe / creds / setup 向导 / 计费降级留痕 / docs 分区可配 / 三个测试套件全部在库在测 |

## 四、你的遗留清单，我们的认领表

| 遗留 | 归属 |
|---|---|
| key 池预算闸（额度锁→消费预算语义迁移） | **你的**，与凭据托管同族——期待你的施工令 |
| **判官直连 API**（判官不需要 agent 循环） | 好方向，我们很认。**想认领请先出一页施工令式说明对齐**（背景/范围/不要做/验收标准），我们的多模型红队评审台可以先打你的方案几轮 |
| codex key 池路径实测 | 你的（凭据托管的收尾验证） |
| runner.test 时序可移植性 | **我们的**，本周修 |
| wiki 词条根 / WK_DOC_TABS 硬编码 | 我们的（去项目化扫尾） |
| electron 43 升级验证 | 悬置——谁需要谁开单验 |

## 五、协作方式重申（含一处升级）

- 改动走 PR；`cd apps/studio && npm test` 全绿是硬门槛；测试在你机器上红 = 先报告，不 = 绕过。
- 大改动前一页施工令式说明对齐再动手（`工程队/` 有二十份样例）。
- 我们将开启 main 分支保护（强制 PR）——不针对这次，是这次提醒了我们：制度靠自觉不如靠闸。对我们自己同样生效。

欢迎继续。你一天摸清平台并拿真实项目压出三个结构坑，这个起手式比我们预想的好得多。
