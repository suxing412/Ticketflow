# 异厂评审意见合集

- 方案：`D:\GitHub\Ticketflow\docs\SLG\技术方案\面板重绘收敛-2026-08-26.md`
- 评审时间：2026-08-25T20:58:20.481Z
- 评审团（自动发现）：codex、deepseek（缺席）
- 在席 1 / 2，意见合计 6 条，击杀 6 条 / 未杀 0 条
- 立场卷派发（红队制，施工令-019）：codex=可行性红队、成本红队；deepseek=不变量红队
- **缺席致落空的立场卷：不变量红队**（该立场本轮无人开火，采信时须自行补位）
- 单厂超时闸：4min

> 各家独立评审、互不可见，各自只领到本席的立场卷（谁领了什么互相不知）；
> 击杀制：每卷须构造具体失败场景，构造不出须明写「未能构造击杀」——该声明即通过证据。
> 以下为各家原文，未做删改（仅做密钥净化与控制符清洗）。

## codex

- 立场卷：可行性红队、成本红队
- 状态：在席 · 模型 `codex 默认档` · 耗时 218.0s · 意见 6 条
- 战果：击杀 6 条 / 未杀 0 条

| 章节 | 意见 | 严重度 | 卷别 | 击杀 |
|---|---|---|---|---|
| §二 G1、病灶 1、§五乙 | 当前 `studio-proj=TK` 且看板打开时，别家项目在下一次 3s 轮询前写入 `TF-999`，前端必须先 `GET /api/pulse?项目=TK&视图=board` 才知道本片未变，因此该拍至少 1 次 fetch 和一次服务端判定，无法同时满足“整拍全部代价归零”与 fetch=0；除非改为服务端推送/订阅，否则应把目标改成“业务数据 fetch=0”。 | 高 | 可行性红队 | 击杀 |
| 病灶 8、§五甲、§六 A13 | 在 `#/t/TK-42` 且 `TK-42` 状态为“完成”时，第二拍数据完全未变，`详情徽章(tg.id)` 仍因未传 `state` 先请求 `/api/ticket` 再请求 `/api/runner`，随后才可能比较“新徽”签名，所以甲阶段所称“+4 行短路”不可能达到 A13 的 fetch=0；必须把版本判定移到网络请求之前或把徽章并入已有详情响应。 | 高 | 可行性红队 | 击杀 |
| §四 病灶 4/5、§五.2、§五.3 | 当 `viewBoard` 把嵌套的 `data-keyed` 卡片容器渲染为空壳、旧 DOM 内已有卡片且本拍仅列头计数变化时，现有 `morphNode` 不认识 `data-keyed`，会递归对齐到空壳并删掉所有旧卡；要让路就必须在 `morphNode` 增加第四道早退，这与 §五.2“morph/morphNode 函数体不改”直接冲突。 | 高 | 可行性红队 | 击杀 |
| 病灶 1/6、§五乙/丁 | 以 50 个常驻客户端、50,000 张单为例，3 秒 pull 一次分片令牌且没有事件索引或共享扫描承诺时，仍会产生 `20 × 50 × 50,000 = 5,000 万` 次 stat/分钟（项目归属还可能要求解析变更文件），前端省下的 fetch 不能阻止服务端 I/O 队列、杀毒扫描或共享盘延迟烧穿；这不是 +30 行 pulse handler 能消掉的成本。 | 高 | 成本红队 | 击杀 |
| G4、病灶 4、附录 A/D1 的序列化计数器 | 当一个普通 board 壳下嵌有 10,000 张 `data-keyed` 卡片且仅壳上的计数文本变化时，父节点的 `o.outerHTML` 比较会先递归序列化全部旧卡片，之后才到达 `data-keyed` 早退；深度守卫只把这笔 O(n) 工作记成“1 次 getter”，故 G4/A6 即使通过也会掩盖真实序列化成本，后续性能取证必然返工。 | 高 | 成本红队 | 击杀 |
| §五.3 职责划分、§六 A4/A14、附录 F.6 | 例如排程执行器在 12:00 直接修改 `_排期/g42`，现有 A4 只遍历 13 个状态目录的 `create/move/update`，A14 又只覆盖 ideas，因此测试可全绿而该源没有进入服务端分片表、前端关心片表和 13 口缓存失效组；每新增一个数据源都要人工同步至少三处实现和多套夹具，漏一次就是静默过期，维护成本呈持续线性扩张。 | 高 | 成本红队 | 击杀 |

第1卷 可行性红队：击杀 3 条  
第2卷 成本红队：击杀 3 条  

总体判断：方案把“前端不重绘”误写成“整拍零成本”，且丙阶段的让路机制与“不改 morph”边界互相否定。先修订目标口径、重绘协议和变更索引，再谈分阶段施工。

## deepseek

- 立场卷：不变量红队
- 状态：**缺席**（原因：应答里没有文本内容（content 为空））—— 上列立场卷本轮落空
- 耗时：134.7s
