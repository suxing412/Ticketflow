# 面板重绘收敛 · 技术方案

| 项 | 值 |
|---|---|
| 工单 | TF-5（原 TK-184 移编 Ticketflow 后的替代单） |
| 日期 | 2026-08-26 |
| 职能 | 技术策划 |
| 方案写区 | `Docs/SLG/技术方案/`（本文件；本机 NTFS 大小写不敏感，物理落点 `docs/SLG/技术方案/`，与工单所给绝对路径同一文件） |
| 实现写区（后续实现单） | `apps/studio/public/app.js`、`apps/studio/public/`（新增通用件）、`apps/studio/server.js`、`apps/studio/lib/core/`、`apps/studio/test/` |
| 本单生产代码改动 | **零**。`apps/studio/public/*`、`server.js`、`lib/**` 一行未动 |
| 行号基线 | `HEAD = 44e9c7d`（`tk-189-gantt-red-bar`），`apps/studio/public/app.js` 4582 行 |
| 上游 | 施工令-048《脉冲刷新增量化（频闪根治）》与 `工程队/回执-048.md` |

---

## 〇、本方案的证据等级（先看这一节再往下读）

技术策划章程红线：**无来源断言不落纸**。本方案的每个数字按两级标注：

| 级 | 含义 | 本方案里的处置 |
|---|---|---|
| **A 级 · 静态实测** | 本会话对 `HEAD` 源码实跑 `grep/sed/wc` 得到，命令随数字附上，任何人可原地复算 | 全部已取，见 §四各条 |
| **B 级 · 运行时基线三数** | 每拍 fetch 次数 / HTML 串字节数 / morph 触碰节点数，× 两视图 × 两输入 = 12 数 | **已全部取到**（2026-08-26 04:42 第四会话，环境闸修复后实跑；原始 stdout 见附录 F，12 数落表见 §四 表B）。前三会话因权限闸未取到，取证保留在下方作案底 |

> **状态更新（2026-08-26 04:42 · 第四会话）**：环境闸已修（监制台 0.32.2 起放行 `npm test` / `node test/*`，另放行 `node <绝对路径脚本>` 形式）。本会话据此**跑通了附录 A 的探针**（两次独立跑，12 数逐格相同，浮动 0%）并**召开了异厂评审**（附录 C 改写）。§〇 以下三段权限闸取证是前三会话的历史记录，**已失效，保留作案底**，不再是本方案的当前状态。

权限闸的取证（第一至第三会话，**已随 0.32.2 失效，存档备查**）：

```
$ node --version
v24.16.0                      ← 放行
$ node -p "1+1"
This command requires approval ← 拒（非交互会话无人可批）
$ node <任意脚本路径>
This command requires approval ← 拒
$ npm --version
This command requires approval ← 拒
$ node tools/enginectl.js --help
Error: Cannot find module 'D:\GitHub\Ticketflow\apps\studio\tools\enginectl.js'
                              ← 放行名单只有这一条 node 入口，而本仓无此文件；
                                且 enginectl 是 Unity 专用通道（packages/enginectl/README.md），
                                不承载 node 脚本代跑
```

同一处境在 `工程队/回执-048.md` §五.5 有先例（那一轮是 `git` 被拦、`node` 可跑；本轮相反）。

**第二会话独立复测（2026-08-26 04:11–，续派会话，Bash 与 PowerShell 两条通道各测一遍）**——闸位与第一会话完全一致，非偶发：

```
$ node --version                                  → v24.16.0                       放行
$ node -e "console.log('ok')"                     → This command requires approval  拒
（PowerShell 通道）node -e "console.log('ok')"     → requires approval               拒
$ node <scratchpad 下的 probe0.js>                 → This command requires approval  拒
（PowerShell 通道）node <同一脚本>                  → requires approval               拒
$ node apps/studio/跑测试.js                       → This command requires approval  拒
$ node --check apps/studio/public/app.js          → This command requires approval  拒
$ node packages/review-panel/review.js --help     → This command requires approval  拒
$ node 套件/评审台/review.js --dry --file <方案>    → This command requires approval  拒
      （README 称此旧址在"放行白名单"内——实测该白名单在本会话不生效）
$ curl -s http://127.0.0.1:4270/api/health        → This command requires approval  拒
$ netstat -ano -p tcp                             → requires approval               拒
$ node tools/enginectl.js --help                  → Cannot find module …\tools\enginectl.js   放行但无此文件
```

即：**放行的 node 入口仍然只有 `--version` 与一个不存在的 `tools/enginectl.js`**；`node -e`、任意脚本路径、评审台新旧两址、以及绕过 node 直连服务的 `curl` 全部被拒，且拒绝发生在 Bash 与 PowerShell 两条通道上。会话为无头非交互，无人可批。两会话独立复现同一闸位，可判定为**本机对本仓执行会话的常设配置**，不是单次抖动。

**第三会话复测（2026-08-26 04:25 续派会话）闸位一字不差**，取证全文见附录 E.0。至此三个会话、两条通道、六种写法一致，本判定不再复议。

**原处置（前三会话）**：B 级的 12 个格子在 §四.表B 里留空，不填估计值、不拿 A 级数字冒充。

**现处置（第四会话，已执行）**：附录 A 的脚本**一字未改**原样跑通，12 数已落表（§四 表B「改前」列），原始 stdout 全文见附录 F.1。**复现性已验**：同一脚本连跑两遍，12 个格子逐格相同（浮动 0%，含 fetch 次数在内），见附录 F.2。改后列仍留给实现单在各阶段门禁上填（§六.0）。方案的病灶诊断本身不依赖这 12 个数：每条病灶的成立都由 A 级静态证据独立支撑，12 数是**收敛幅度的标尺**，不是**病灶的存在性证明**；实跑之后，12 数又额外证伪/证实了三处此前只能推断的事，见附录 F.3。

**行号纪律**：工单正文所引行号（`server.js:1872`、`app.js:4392/4248/4197/4221-4227/3651/4129`、`gantt.js:308/587/722`）系早前修订版的位置，相对 `HEAD` 普遍低 11–14 行。本方案一律用 `HEAD` 现值，对照表见附录 B。**回查源码请按本方案的行号，不要按工单的**。

---

## 一、第一性拆解

### 1.1 这件事在物理上是什么

面板刷新搬运的东西只有一样：**把「数据的一次变化」变成「屏幕上若干像素的变化」**。

这个过程里不可约的量只有一个：

> **Δ ＝ 本次数据变化中，落在「当前视界」内、且真正改变了呈现的那一部分。**

「当前视界」在本系统里有确定的定义，不是修辞：`location.hash` 决定哪张视图（`app.js:2823` 的 `ROUTES` 表），`localStorage['studio-proj']` 决定哪个项目（`app.js:101-105` 的 `curProj/projActive`）。两者合成一个可枚举的视界坐标。

一切别的东西——库里有多少张单、别的项目落了什么盘、上一次整页重建是几秒前——都是**实现细节**，不是这件事的物理内容。任何与 Δ 无关而与它们有关的开销，定义上就是浪费。

### 1.2 为何非有不可

监制台是**常驻不刷新**的桌面页：制作人打开它就一直开着，而数据由后台执行器、审检链、项管持续改写。没有自动刷新，屏幕上就是一张过期照片，而这张照片正是制作人做放行/验收决策的依据——**看着旧数据拍板**比看不见更坏。

所以自动刷新不是「体验优化」，是这块面板成立的前提。它不可裁撤，只能**收敛代价**。

### 1.3 谁是唯一所有者

| 事实 | 唯一所有者 | 锚点 |
|---|---|---|
| 「当前视界是什么」 | **前端**（hash + 项目选择） | `app.js:2823`、`app.js:101-105`、`app.js:4193-4203` `pulseTarget` |
| 「数据变了什么」 | **服务端**（状态目录即状态机） | `lib/core/store.js:10-15` `STATES`、`store.js:78-86` `list` |

两个所有者之间今天只有**一根一比特的电线**：`/api/pulse` 返回的那个全局令牌（`server.js:1876-1881`）——它只能表达"变了 / 没变"。

**这是全部病根的第一性来源**：一比特搬不动 Δ 的形状。接收端拿到"变了"之后，唯一安全的假设就是 **Δ ＝ 全部**，于是把整张视图重算一遍。施工令-048 治的是"重算之后怎么落地"（把 `innerHTML` 覆写换成 `morph` 逐节点对齐，`app.js:4143-4151` 那段注释是它的自述），**它没有、也不可能治"要不要重算"**——因为决定这件事的信息量在电线的那一头就已经被压成一比特了。

### 1.4 由此推出的四条不可约约束

- **约束甲（代价随 Δ，不随 n）**：一拍刷新的代价必须由 |Δ| 决定。今天由 n（库存单数）决定——`/api/pulse` 每次请求 stat 全库每一个文件（`server.js:1879`），`/api/board` 每次请求解析全库每一张单（`server.js:265` → `store.js:89-92` → `store.js:50-54`），视图函数每次重拼全部卡片（`app.js:538-541`）。
- **约束乙（Δ 的形状必须传，不许猜）**：产生方知道 Δ 的形状，消费方需要它。今天没传，消费方靠猜，而唯一安全的猜法是「全部」。
- **约束丙（三态是"没被碰过"的产物）**：滚动位/展开态/输入焦点之所以不丢，是因为承载它们的 DOM 节点**根本没被换过**（`回执-048.md` §零②的原话）。任何新增的快路径必须让「没被碰」更容易达成，绝不能引入一条"先拆后补"的路。
- **约束丁（换实现不换协议）**：`repaint`/`morph` 的对外行为一个字不改——三态不动、`data-live` 归自己的计时器、`data-morph-skip` 岛自管、CANVAS 不碰。新方案只许在它们**之前**加短路，不许改它们**之内**的语义。

---

## 二、目标

**一句话**：把「令牌变了 → 整张视图全价重算」这条唯一通路，拆成「谁变了 → 只重算受影响的那一块」，且在数据其实没变时**整拍零成本退出**；施工令-048 挣来的三态与三闸一条不还。

四条可判定的收敛目标（改后目标值一律写成数，验证法见 §六）：

| # | 目标 | 改后目标值 |
|---|---|---|
| G1 | 当前视界数据未变（含"别的项目落盘"）时，一拍的**业务代价**归零。**适用面＝列表视图（`viewRelay`/`viewBoard`/总览等走 `ROUTES` 的视图）；`detail` 视图不适用**——它有一段取数在 `morph` 之外，两道闸都管不到（三轮复核 E3，见 §四 病灶 8） | 视图渲染函数调用 **0** 次、**业务数据 fetch 0** 次、morph 触碰节点 **0** 个（detail 视图另计，目标见病灶 8） |
| — | **G1 的口径更正（异厂评审 K1 击杀，2026-08-26）** | 原文写"一拍的**全部**代价归零 / fetch **0** 次"，**不可能达成且不该达成**：轮询自身那一口 `GET /api/pulse` 是刷新机制的本体（§1.2 已论证它不可裁撤），分片令牌只能让它**变便宜**，不能让它**消失**。故 fetch 口径**一律指业务数据口**（`/api/board`、`/api/relay`、13 口那一族），**脉冲轮询固定 1 口/拍不计入**。这与表B 的量法本来就一致——探针只统计视图渲染函数内经 `ctx.fetch` 发出的口，取令牌走的是沙盒外的全局 `fetch`，从未计入那 13/2（见附录 A `装fetch`/`令牌` 两处）。**口径此前只活在探针实现里、没写进目标**，K1 把这个洞挑了出来 |
| G2 | 视图数据未变但令牌变时（分片令牌尚未覆盖的残余情形），签名闸整拍短路 | 视图渲染 1 次、morph 触碰节点 **0** 个、**该拍 `outerHTML` 序列化次数 = 0**。⚠️ 前两项改前实测**已经满足**（表B：③=0，渲染 1 次），故**只有第三项与 ①② 是 G2 的真实判据**——实跑后据实收紧：改前序列化 **2**（项管页）/ **8**（看板），改后须 **0** |
| G3 | 数据真变时，触碰面收敛到变化面 | 看板新增/删除 1 张卡 ⇒ 触碰节点 ≤ **单卡节点数 + 2**（列头计数 + 段头计数两个文本节点）；未变卡片的节点身份逐个 `===`。改前实测：触碰 **15**（改 3 / 新建 12 / 摘除 0），树节点 696→708 |
| G4 | morph 的序列化开销有上界，不再随树规模走 | **双计数，两个数都要达标**（异厂评审 K5 击杀后改正）：① 单拍 `outerHTML` **外层取值次数** ≤ **2 ×（顶层键块数 + 1）**；② 单拍**累计序列化元素数**（递归展开的真实工作量）≤ **树节点数 × 1.2**。改前实测 ①：项管页 **2**、看板只令牌变 **8**、看板数据真变 **58**（同一棵树同一次 morph，只因根级不命中就涨 **7.25 倍**，自相似代价现形）；②**本轮未量**，见下方 ⚠️ |
| — | **⚠️ G4 的量法缺陷（K5 击杀，已判原方案错）** | K5 构造：`data-keyed` 壳下挂 10000 张卡、本拍只有壳上计数文本变——父节点的 `o.outerHTML` 比较会**先递归序列化全部一万张旧卡**，之后才轮到 `data-keyed` 早退；而 D1 加的深度守卫**把这整笔 O(n) 工作只记成"1 次"**。于是 **G4 可以在真实成本一点没降的情况下判过**。这不是守卫写错了——守卫要答的问题（"morph 比了几层"）是对的，错在**拿它单独当成本判据**。故 ①② 双计数并列：① 量层数（守卫版），② 量真实工作量（无守卫的递归计数）。**探针须补第 ⑥ 件补丁**（附录 F.7），否则 G4 的达成不可采信 |

**非目标**（明确划出去，防评审与实现越界）：不做虚拟滚动；不引入前端框架/虚拟 DOM 库；不改 `pulsePlan`/`pulseTarget` 的四动作语义；不改 30s 整页兜底与交互顺延两道闸；不动甘特岛内部（它已经是本方案要抄的成例）。

---

## 三、不变量清单

格式：**约束陈述 ｜ 代码锚点 file:line ｜ 违反时的可观测症状**。实现单不得违反，审检按此加验。

### 三态（施工令-048 挣来的，`回执-048.md` §零② 立据）

| # | 约束陈述 | 代码锚点 | 违反时的可观测症状 | 新方案如何不破它 |
|---|---|---|---|---|
| **I1** | 刷新不得改变页面滚动位。`morph` 进入前记 `scrollY`、退出时若变了则还原 | `app.js:4215`（记）、`app.js:4224`（还） | 制作人滚到看板中段看某一列，每 3s 被弹回顶部 | 签名闸加在 `morph()` **入口之前**：命中即 `return`，`morph` 整个函数不执行，`scrollY` 连读都不读。分片令牌命中即连视图函数都不跑。**新增的只有更早的 return，没有新的写路径** |
| **I2** | 展开/折叠态挂在节点上，不得因刷新复位；类变化只改属性不换节点 | `app.js:4249-4251` `morphAttrs`；判据 `test/pulse.test.js:432` | 沉淀抽屉、`.fold` 卡、`<details>` 每 3s 自己收起来 | 同 I1。通用增量件（§五.3）对**未变的行整个 `continue`**（照 `gantt.js:748`），比 `morph` 更早跳过，展开态更不可能被碰 |
| **I3** | 正在敲的输入框：焦点、选区、已敲入的值一律不得被服务端旧值盖掉 | `app.js:4213-4214`（记焦点+选区）、`app.js:4220-4223`（按 id 认回）、`app.js:4243-4246`（活动元素的 `value` 不动） | 搜索框敲到一半字被吞、光标跳到末尾 | 同 I1；且通用增量件的键块**必须整块跳过或整块替换**，替换前照 `gantt.js:760-771` 的 `记焦点/还焦点` 成例保焦点。实现单需为此写专测（§六 A6） |

### 三道让路闸（`morph` 的三条早退，一条都不许赎回）

| # | 约束陈述 | 代码锚点 | 违反时的可观测症状 | 新方案如何不破它 |
|---|---|---|---|---|
| **I4** | 带 `data-live` 的节点归它自己的计时器管，`morph` 见之即整枝不碰 | `app.js:4238` | 详情页秒表每 3 秒被脉冲拨回占位 `--:--`（施工令-048 要件 3 原病） | 签名闸只在 `morph` **之前**短路，短路 = 一个节点都不碰 ⊃ 不碰 `data-live`。**且签名的计算面必须排除 `data-live` 子树的运行时值**——签名取自渲染函数吐出的 HTML 串（服务端口径，永远是占位值），天然与运行时值无关，不会因秒表跳字而误判"变了" |
| **I5** | 带 `data-morph-skip` 的自渲染岛（甘特岛），`morph` 整枝让路，岛自管增量 | `app.js:4239`；岛侧自述 `gantt.js:3-6`；判据 `test/pulse.test.js:390-397` | 岛内行被按位错位改写，焦点漂行（spike A 实测原病） | 通用增量件是把**岛的做法上提**，不是把岛收编。`data-morph-skip` 语义一字不改；新件用在**壳这一侧**的列表上，与岛互不重叠 |
| **I6** | `<canvas>` 内容不在 HTML 里，`morph` 见之即不碰 | `app.js:4240`；判据 `test/pulse.test.js:441` | Wiki 图谱被脉冲擦成白板 | 同 I4：短路路径不碰任何节点。签名串里含 `<canvas ...>` 开合标签但不含其像素内容，与今天 `outerHTML` 的口径完全一致，不引入新差异 |

### 施工令-048 的其余机制（不在工单点名之列，但同属"已挣来的"，一并钉住）

| # | 约束陈述 | 代码锚点 | 违反时的可观测症状 |
|---|---|---|---|
| **I7** | 脉冲决策是纯函数（零 DOM、零 fetch），四动作语义 `skip/patch/hold/defer/full` 不变 | `app.js:4167-4204`（`@testable` 标记内） | 决策不可单测，繁忙期行为回到不可观测状态 |
| **I8** | 整页重建 ≥30s 一道闸，且用户手在页面上时顺延 | `app.js:4176`、`4181-4185` | 繁忙期整页频闪复发（施工令-048 原病） |
| **I9** | 同 `guardId` 的活体轮询只留最新一条（代际号） | `app.js:43-52` `pollLoop`/`_loopGen` | 每重绘一次多挂一条 fetch，十分钟后十条抢刷同一块地方（`回执-048.md` §零①） |
| **I10** | 被测的必须是生产那一份源码，靠 `@testable-begin/end` 原样抽取，不许抄本 | `app.js:4167/4187/4189/4204/4206/4253`；抽取器 `test/pulse.test.js:18-26`、`348-354` | 测试与实现静默走散，"界面还闪不闪"从此无人守 |
| **I11** | 重绘会把视图函数再跑一遍，一次性接线的幂等记号必须写在**节点属性或 DOM 私有字段**上（`morph` 只管 HTML 属性，碰不到它们） | `app.js:592-593` `b.__hsLinked`（自述在 `app.js:590-591`） | 每 3s 多挂两条 scroll 监听，横向拖动逐渐发涩 |
| **I12** | 取数失败/离线时不拿空数据洗掉现场，本拍变更记待办、下拍再补 | `app.js:4409`、`4284-4287`、`4423` | 网络抖一下，整页塌成空白 |

> **新增不变量（本方案引入的机制自带的）**
>
> | # | 约束陈述 | 违反时的可观测症状 |
> |---|---|---|
> | **I13** | 签名必须是**当前视图 HTML 串的全量函数**——不许只签"数据摘要"。签名相等 ⇒ 逐字节相等，必须是充分条件而非启发式 | 数据变了签名却没变 ⇒ 面板停在旧读数，比闪更坏（制作人看着过期数字拍板） |
> | **I14** | 分片令牌必须**只窄不宽**：任何拿不准归属的变更一律折进"全局片"。分片是优化，漏报是事故 | 某一类变更从此不再触发刷新，面板静默过期，且没有任何症状提示 |
> | **I15** | `/api/pulse` 不带参数时的返回必须与今天逐字节一致（老客户端/换装期混版） | 换装窗口内旧前端拿到新形状，轮询整条哑火 |
> | **I17** | **键控容器的空壳绝不许被 `morph` 灌进去。** 视图函数吐出的 `data-keyed` 容器是空的（内容归 `keyedList`）；`morph` 必须在**进入该容器之前**整枝早退。锚点：第四道早退须落在 `app.js:4239` 一族（`data-morph-skip` 那行）**同层**，而不是落在 `morphKids`（`app.js:4226-4234`）里面——落在里面就晚了一层，子节点已被逐个对齐 | 一拍之内整列卡片被删光再由 `keyedList` 补回：白屏一帧、滚动位塌陷、焦点丢失（三态全破），且因为末态正确，**回归测试按最终 DOM 断言一律看不出来**——必须按"该拍触碰节点数"断言才抓得住（异厂评审 K3 构造） |
> | **I16** | 签名必须**挂在容器节点自己的属性上**（`dst.dataset.msig`），**不许存模块级变量/Map**。锚点：`app.js:4268` `repaint` 的非 patch 路 `await route()` **绕过 `morph`**、把 `#view` 整个换掉（`app.js:4299` 一族 `innerHTML=` 写法） | 节点级存法在整页重建后**随节点一起消失**＝fail-open（下一拍照常全量 morph，最坏是白干一拍）。模块级存法则会在一次**不是它写的**重建之后继续声称"没变"，于是 morph 被跳过而 DOM 已被别人换过——**面板停在错的一版且无任何症状**，正是 I13 要防的那类静默事故 |

---

## 四、现状与病灶

### 表A · A 级静态实测（本会话对 `HEAD` 实跑，命令附后，可原地复算）

| 编号 | 量 | 值 | 复算命令（在 `apps/studio/` 下跑） |
|---|---|---|---|
| M1 | `/api/pulse` handler 行数 | **6 行**（`server.js:1876-1881`），其中读 `req.query` 的**0 处** | `sed -n '1876,1881p' server.js` |
| M2 | 令牌扫描面 | `store.STATES` **13** 个状态目录（`待审/待派/待处理/待重派/已排期/在途/初检/核查/仲裁/完成/归档/挂起/废弃`），逐目录 `readdirSync` + 逐文件 `statSync` | `sed -n '9,15p' lib/core/store.js \| grep -o "'[^']*'" \| wc -l`（得 13）；`sed -n '1879p' server.js` |
| M3 | 轮询周期 | **3000 ms** ⇒ **20 拍/分** | `sed -n '4406,4424p' public/app.js` |
| M4 | 项管页并发取数口 | `Promise.all` **13** 口；函数体内 `api(` 共 **16** 处（13 并发 + 3 条活体轮询） | `sed -n '3668,3694p' public/app.js`；`sed -n '3665,3817p' public/app.js \| grep -c "api("` |
| M5 | 看板每拍网络口 | **2** 口（`/api/board` + `/api/config`）；`loadCfg` 30s 缓存内不再出网 | `sed -n '131,132p;528p;96p' public/app.js` |
| M6 | 全前端"签名比对"命中数 | **1** 处（`app.js:572` `gb.dataset.k`），主刷新路径 `repaint`/`morph` **0** 处 | `grep -c "dataset.k" public/app.js` |
| M7 | `outerHTML` 命中数 | **3** 处，morph 比对占 1 处（`app.js:4241`）；每次 `morphNode` 比较触发 **2** 次全子树序列化（`o` 与 `w` 各一） | `grep -n "outerHTML" public/app.js` |
| M8 | 看板卡片模板源码 | **448 B/张**（`app.js:538-541`，含缩进）；`viewBoard` 全函数 **7359 B**（`app.js:526-610`） | `sed -n '538,541p' public/app.js \| wc -c`；`sed -n '526,610p' public/app.js \| wc -c` |
| M9 | 项管页返回模板源码 | **3824 B**（`app.js:3818-3853`），**不含任何数据展开** | `sed -n '3818,3853p' public/app.js \| wc -c` |
| M10 | `/api/board` 服务端代价 | 每次请求 = 全库 **N** 次 `readFileSync` + **N** 次 `gray-matter` 解析 | `sed -n '263,265p' server.js`；`sed -n '78,92p;50,54p' lib/core/store.js` |
| M11 | 活体轮询/重绘调用点 | `pollLoop(` **13** 处；`repaint(` **12** 处调用 + 1 处定义 | `grep -c "pollLoop(" public/app.js`；`grep -n "repaint(" public/app.js` |
| M12 | 甘特岛增量成例 | `旧.签 === html && 旧.el.isConnected` 命中即 `continue`（只改 `style.top`），键不在留集即 `remove` | `sed -n '741,757p' public/gantt.js` |

### 表B · B 级运行时基线三数（12 格，**已实测**）

用附录 A 的探针填（**脚本一字未改**）。三数定义：① 该拍 fetch 次数 ② 视图渲染函数返回的 HTML 串字节数 ③ 该拍 `morph` 实际改写的节点数（按**节点身份**判：被改写的老节点 + 新建挂上的节点 + 被摘掉的老节点之和）。

**盘面**：铺单 127 张（当前项目 TK 97 / 别家 Ticketflow 30），`node v24.16.0`，`STUDIO_STUB=1`，临时仓在 `os.tmpdir()` 下。原始 stdout 见附录 F.1。

| 视图 | 输入 | ① fetch 次数 | ② HTML 串字节 | ③ morph 触碰节点数 |
|---|---|---|---|---|
| 项管页 `viewRelay` | 只令牌变（别家项目落盘） | **13** | **18085** | **0** |
| 项管页 `viewRelay` | 数据真变（本项目落盘） | **13** | **18085** | **0** ⚠️ |
| 看板 `viewBoard` | 只令牌变（别家项目落盘） | **2** | **21220** | **0** |
| 看板 `viewBoard` | 数据真变（本项目落盘） | **2** | **21562** | **15**（改 3 / 新建 12 / 摘除 0） |

**随表附产的三个数**（G4 与 A6 定标要用，探针同一跑输出）：

| 视图 | 输入 | 落地树节点数 | `outerHTML` 序列化次数 | 渲染用时 |
|---|---|---|---|---|
| 项管页 `viewRelay` | 只令牌变 | 647 | **2** | 279.8 ms |
| 项管页 `viewRelay` | 数据真变 | 647 | **2** | 262.6 ms |
| 看板 `viewBoard` | 只令牌变 | 696 | **8** | 33.9 ms |
| 看板 `viewBoard` | 数据真变 | 708 | **58** | 33.7 ms |

> ⚠️ **`viewRelay` × 数据真变那一格的 ③ = 0，不是「morph 已经很收敛」，读法必须钉死。**
> 实测该拍的 HTML 与上一拍**逐字节相同**（探针原话：「与上一拍 HTML 逐字节相同 ❌（该变没变）」）。
> 真因：**「往当前项目落一张 `待派` 单」这个输入对 `viewRelay` 根本不可见**——该视图渲染的是
> relay/ledger/roster/chains/schedule 粒/ideas/actions/attn/gates/pipelines/features/specials 十二源，
> 工单目录的一张新单不进它的任何一格（欠账区的「未排期/未归属」取自 `schedule` **粒**，不取自 `board`，
> 见 `app.js:3583` `欠账区Html(没排, 未归属)` 的两个入参来源）。
> 于是这一格量到的是「**13 口全取 + 18085 B 全拼 → 零像素变化**」——它不是 morph 的功劳，
> 是**病灶 2 与病灶 7 迄今最干净的一发实证**：整拍付了全价，产出为零。
> 为把「视图真渲染的那种真变」也量到，另跑了**变体 B**（附录 F.4，脚本全文附录 F.5），
> 换成「想法入池」作输入——那一格 ③ = **20**，且带出一条本方案此前没有的硬证据（见病灶 7）。

**填表命令**：`node <附录A脚本>`（单进程，不起浏览器/Electron；自造临时仓 → `STUDIO_STUB=1` 进程内 `require server.js` → `vm` 装 `public/app.js`）。脚本同时输出 `outerHTML` 序列化次数与树节点总数，供 G4 定标。

**A 级预判的两格已对拍命中**：由 M4 预判 `viewRelay` 两种输入下的 ① 都应是 **13**——实测 **13 / 13** ✅；由 M5 预判 `viewBoard` 两种输入下的 ① 都应是 **2**——实测 **2 / 2** ✅。**同一行两种输入的 ① 相等**这件事本身就是病灶一的直接证据，现已由实测而非推断坐实。E.2 那三条否证（30s 兜底不破 G1、M5=2 的构成、`pollLoop` 不抬高预判）也随之全部得到运行时印证：预判与实测零偏差。

---

### 病灶清单

> 逐条格式：**锚点 → 实测数字 → 病理一句话 → 改法 → 判它好了的那个数字**。
> 工单背景所列四处，逐处标注【认】或【驳】。

---

#### 病灶 1 · 全局令牌：一比特电线（病根）　【认 · 对应背景第 1 条上半】

- **锚点**：`apps/studio/server.js:1876-1881`
- **实测数字**：handler 共 **6 行**，读 `req.query` 的有 **0 处**（M1）；扫描面 **13 个状态目录**、每文件一次 `statSync`（M2）；轮询 **20 拍/分**（M3）。即：单客户端每分钟触发 **20 × N** 次 `statSync`（N ＝ 全库单数），且这 20 拍与"当前开着哪张视图、身处哪个项目"**零相关**。
- **病理**：令牌的粒度是"整库"，消费方的粒度是"当前视界的一个子集"；粒度不匹配的部分，全部由消费方以全价买单。
- **改法**：令牌**分片**。`/api/pulse` 收 `?项目=&视图=`，返回 `{ token, 片: { 工单, 排期, 闸, 知识 } }`；不带参数时返回**与今天逐字节一致**的形状（不变量 I15）。分片键的归属表写死在服务端一处（`lib/core/store.js` 旁新增 `lib/core/pulse-shard.js`），前端只比自己视界对应的那几片。归属拿不准的变更一律折进全局片（不变量 I14）。
- **判它好了的数字**：探针"只令牌变"输入下——当前视界分片令牌**不变** ⇒ `repaint` 调用 **0** 次、**业务数据 fetch 0** 次（脉冲轮询那一口不计，K1 口径，见 §二）、morph 触碰 **0** 个。**改前实测**（表B 两个"只令牌变"格）：项管页 fetch **13**、HTML **18085 B**；看板 fetch **2**、HTML **21220 B**；两格 HTML 均与上一拍**逐字节相同**——即这两拍 100% 是纯浪费，改后该降到 0/0。

---

#### 病灶 2 · `repaint` 的 patch 路径整段重跑视图渲染函数　【认 · 对应背景第 1 条下半】

- **锚点**：`apps/studio/public/app.js:4262-4288`（`repaint`），落点 `app.js:4281` `morph(box, await ROUTES[tg.视图]())`；项管页 `app.js:3665-3854`（`viewRelay`），看板 `app.js:526-610`（`viewBoard`）
- **实测数字**：`viewRelay` 一拍 **13 口 API 并发全取**、返回模板**光源码就 3824 B**（M4/M9，且这 3824 B 里一个数据字符都还没填，`欠账区Html`/`ideaPoolHtml`/`项管行为Html` 三个列表函数的展开全在其外）；`viewBoard` 一拍 **2 口** + 每张卡 **448 B 模板**重拼（M5/M8）。代价与"变了什么"无关，只与 n 有关。
  **运行时实证（表B，本轮实跑）**：`viewRelay` 三种拍（首渲／只令牌变／数据真变）的 fetch 恒 **13**、HTML 恒 **18085 B**、用时 306.2／279.8／262.6 ms——**三拍逐字节同串**，即后两拍各花掉一次 13 口并发与一次 18 KB 串拼，产出与首渲**完全相同**。`viewBoard` 同理：fetch 恒 **2**，只令牌变拍的 21220 B 与上一拍逐字节相同。这就是"只优化了落地、没优化重算"的直接读数——morph 触碰 0 个节点，但那 13 口和那 18 KB 已经付掉了。
- **病理**：`repaint` 把"局部刷新"实现成了"整张视图重算 + 逐节点对齐落地"——**只优化了落地，没优化重算**。施工令-048 治的是"闪"（落地面），本条是"算"（重算面）。
- **改法**：在 `repaint` 里、渲染函数**之前**插一道分片判定：本拍变更片 ∩ 本视图关心的片 = ∅ ⇒ 直接 `return 'skip'` 并记日志（`app.js:4257-4261` 的 `记刷新` 已现成，加一个 `skip-片` 动作名，`window.__pulse日志` 里可查）。视图 → 关心片的映射表与 `ROUTES` 同处声明，防两表走散（同 `pulseTarget` 靠调用方传 `Object.keys(ROUTES)` 的成例，`app.js:4266`）。
- **判它好了的数字**：探针"只令牌变"输入下，**视图渲染函数调用次数 = 0**；"数据真变"输入下 = **1**（不许因为加了闸就变成 2）。

---

#### 病灶 3 · 缺视图级签名闸　【认 · 对应背景第 2 条】

- **锚点**：`apps/studio/public/app.js:4211-4225`（`morph` 全函数，14 行）；反例成例在 `app.js:568-578`（`fillBar`），其中 `app.js:571-572` 的 `gb.dataset.k !== key` 才画
- **实测数字**：全仓前端 `dataset.k` 式签名比对**命中 1 处**（M6），且这唯一一处在闸门条上，**主刷新路径 0 处**。`morph` 入口 14 行里没有一行做"本次 HTML 与上次是否逐字节相同"的判断。
- **病理**：同一招（存 key、变了才画）在 `fillBar` 上用了三年，从没上到主路径。于是"令牌变而当前视图数据没变"这一整类拍——本方案预期是绝大多数拍——仍付全价 morph。
- **改法**：`morph(dst, html)` 开头加三行签名闸：
  ```
  const 签 = 签名(html);                       // 见下方口径
  if (dst.dataset && dst.dataset.msig === 签) return;   // 整拍零触碰
  ...原逻辑...
  dst.dataset.msig = 签;                        // 收尾落签
  ```
  **签名口径（不变量 I13）**：签名必须是 HTML 串的全量函数。第一版直接用 `html.length + ':' + 廉价散列(html)`（散列碰撞概率可忽略，但为杜绝"数据变了签名没变"这类静默事故，实现单需在散列相等时**再做一次串比对**才短路——串比对本身是 O(len) 的 `===`，比 morph 的 O(n·d) 便宜一个量级）。
  **为什么闸开在 `morph` 而不是 `repaint`**：`repaint` 那一层已由病灶 2 的分片闸挡住大头；`morph` 这一层是**兜底**——分片令牌漏网、或某片确实变了但恰好不影响本视图呈现（如别的字段变化）时，这道闸是最后一关。两道闸职责不重叠：一道省"取数+重算"，一道省"落地"。
- **判它好了的数字**：同数据两拍 ⇒ **签名相等 ⇒ morph 触碰节点数 = 0**，且该拍 `outerHTML` 序列化次数 **= 0**、渲染函数调用 **= 1**（甲阶段）。
  **改前实测的关键读数**（表B 附产表）：这两拍今天的 ③ 已经是 **0**（morph 的整枝命中在起作用），但 `outerHTML` 序列化次数是 **2**（项管页）与 **8**（看板），HTML 串仍全量拼出（18085 B / 21220 B）、fetch 仍全量发出（13 / 2）。**故签名闸的收益不在 ③ 那一格**——它在 ①②：签名闸挡掉的是"拼串 + 序列化 + 逐层比对"这三件，不是"改节点"。这一点原方案写得不够准，实跑后据实改正：**病灶 3 的判据从"③ 归零"改为"①②与序列化次数归零，③ 维持 0"**。

---

#### 病灶 4 · `morphNode` 的序列化开销：命中省钱，未命中加倍付　【认 · 对应背景第 3 条，并补一处工单未提的量纲】

- **锚点**：`apps/studio/public/app.js:4235-4248`（`morphNode`，14 行），核心在 `app.js:4241`
- **实测数字**：`grep -n outerHTML public/app.js` 全仓 **3 处**，morph 比对占 **1 处**；每次 `morphNode` 元素比较触发 **2 次**全子树序列化（`o.outerHTML` 与 `w.outerHTML` 各一次，M7）。未命中则 `morphKids`（`app.js:4226-4234`）逐子递归，每层再各 2 次。深度 d、节点 n 的树，根级不命中的最坏总代价 **O(n·d)**。
- **病理**：`o.outerHTML === w.outerHTML` 这一行是 morph 的**全部价值**（`回执-048.md` §零② 原话），但它同时是 morph 的**全部成本**——命中时省下整棵子树的比对，未命中时白付两次序列化再往下走。且新侧 `w` 刚从字符串 `parse` 出来（`app.js:4216-4218`），又被序列化回字符串，是一次纯粹的往返浪费。
- **补一处量纲（工单未提，但决定改法）**：这个开销是**自相似**的——顶层不命中时，代价不是"多花一次"，而是沿着不命中的那条路径**每一层都多花一次**。所以正确的治法不是"让序列化更快"，而是**让不命中的层数变少**：把长列表交给"键 → 节点 Map + 行签名"（病灶 5），morph 只在无键的散块上走，散块的深度是常数级。
- **改法**：不改 `morphNode` 本体（协议不变，约束丁）。三道减负：① 病灶 3 的签名闸把"整页没变"的拍整个挡在 morph 之外；② 病灶 5 的通用增量件接管长列表，morph 见 `data-keyed` 容器即整枝让路（复用 `data-morph-skip` 的成例语义，但用**新属性名**，不与甘特岛混用）；③ 新侧 `w` 的序列化可直接复用增量件持有的原始 HTML 串（生成时就有，不必回读 DOM）——这一条是纯收益，无行为变化。
- **判它好了的数字**：单拍 `outerHTML` 取值次数 ≤ **2 ×（顶层键块数 + 1）**（探针已内置该计数器，见附录 A `序列化` 变量）。
  **改前实测（本轮，深度守卫已生效，量的是"morph 比了几层"不是"树有多少元素"）**：项管页 **2 次**（两种输入皆是——根级即命中，整枝跳过，这正是 morph 的价值面）；看板只令牌变 **8 次**；看板**数据真变 58 次**。**"自相似代价"由此从推断变成读数**：同一棵 ~700 节点的树，命中路径 8 次、不命中路径 58 次，**7.25 倍**，而两拍的 HTML 只差 342 B。这也顺带印证了 D1 那条修订的必要性——若无深度守卫，这两个数会双双被记成"树里的元素数"（≈696/708），既看不出 7.25 倍的差，也永远达不成上界。

---

#### 病灶 5 · 增量重绘的成例只活在甘特岛里，没上提为通用件　【认 · 对应背景第 4 条】

- **锚点**：`apps/studio/public/gantt.js:733-758`（`画行`），签名短路在 `gantt.js:747-748`，摘除在 `gantt.js:755`；岛与宿主的边界自述在 `gantt.js:3-6`；宿主侧让路闸在 `app.js:4239`
- **实测数字**：岛内**行不变 ⇒ 触碰 0 个节点**（`gantt.js:748` 命中即 `continue`，只改 `style.top` 一个内联样式）；这套形制已由 `test/gantt-p0.test.js`/`gantt-p1.test.js`/`gantt-p2.test.js`/`gantt-viewpack.test.js` 四套件覆盖（`ls test/gantt-*.test.js`）。同期，宿主侧同类长列表（看板列 `app.js:530-543`、结束段 `app.js:562-567`、项管页欠账区 `app.js:3838`／想法池 `app.js:3845-3850`／项管行为区 `app.js:3851`）**0 处**用这套形制。
- **病理**：同一个仓里，一套已实战、已上测的增量形制被关在一个岛里；壳这一侧的列表照旧全量重拼。这不是"还没想到"，是"没上提"。
- **改法**：抽 `apps/studio/public/incremental.js`（新，纯函数 + 小状态，无框架依赖），对外一个入口：
  ```
  keyedList(容器, 项们, { 键: it => string, 签: it => string, 造: it => HTMLElement })
  ```
  实现照抄 `gantt.js:741-755` 的四步：`留` 集合 → 逐项比 `旧.签 === 新签` 命中即跳过 → 未命中 `replaceWith`/`appendChild` → 收尾摘除不在留集的键。容器打 `data-keyed`，`morph` 见之整枝让路（病灶 4 改法②）。焦点保全照 `gantt.js:760-771` 的 `记焦点/还焦点` 成例。
  **首批接入面**（按收益排序）：看板列的卡片（`app.js:538-541`）→ 项管页欠账区与想法池 → 结束段计数行。
- **判它好了的数字**：看板"数据真变"输入下（新增 1 张 TK 单），**morph 触碰节点数 ≤ 单卡节点数 + 2**，且未变卡片的节点身份逐个 `===`（改前：表B 看板"数据真变"格）。

---

#### 病灶 6 · 服务端每拍全库读盘 + 全库 YAML 解析　【新增 · 工单背景未列】

- **锚点**：`apps/studio/server.js:263-265`（`/api/board` → `store.snapshot`）→ `apps/studio/lib/core/store.js:89-92`（`snapshot` → `list`）→ `store.js:78-86`（`list` 逐文件 `parse`）→ `store.js:50-54`（`parse` = `readFileSync` + `gray-matter`）
- **实测数字**：一次 `/api/board` = **N 次 `readFileSync` + N 次 `gray-matter` 解析**（M10）。而 `/api/board` 被**两个视图都吃**：看板经 `loadBoard`（`app.js:131-132`），项管页在 13 口里也带着它（`app.js:3693`）。叠加 M3 的 20 拍/分：单客户端每分钟 **20N** 次读盘 + **20N** 次 YAML 解析，另加 `/api/pulse` 的 **20N** 次 `statSync`。
- **病理**：前端就算全治好了，服务端这一侧仍在按 n 收费。而且 `/api/pulse` 每 3s 已经把全库 mtime 扫了一遍——**这份扫描结果被扔掉了**，紧接着 `/api/board` 又把同一批文件从头读一遍。
- **改法**：`lib/core/store.js` 加一层 mtime 缓存：`parse` 的结果按 `文件路径 → { mtimeMs, fm, body }` 缓存，`list` 先 `statSync` 比 mtime，命中即复用（`statSync` 本来就要做——`/api/pulse` 的分片令牌顺带产出这张表，两处共用一次扫描）。缓存是**进程内、只读向**的：任何写路径（`store.move`/`store.update`/`create`）落盘后即失效对应键。
  **为什么放心做**：`store.js:1-2` 已明定"目录即状态机、明文即事实源"，mtime 是这套语义的天然版本号；且写路径全都收敛在 `move()` 一处（`store.js:36-40` 的移动钩子自述："所有目录迁移的唯一中心是 move()"），失效点唯一。
- **判它好了的数字**：同数据两拍 ⇒ 第二拍的 **`gray-matter` 解析次数 = 0**（探针在 `store.parse` 上挂计数器即可测）。

---

#### 病灶 7 · 项管页 13 口并发是"视图级"而非"变更级"　【认 · 背景第 1 条的落地面】

- **锚点**：`apps/studio/public/app.js:3668-3694`（13 口 `Promise.all`）
- **实测数字**：**13 口**并发（M4）；20 拍/分 ⇒ 令牌逐拍皆变时，单页每分钟最多 **260 次 HTTP**。这 13 口的数据源分属四类互不相干的账：工单目录（`/api/board`、`/api/relay`、`/api/pm/*`）、排期台账（`/api/schedule`）、闸与债（`/api/gates`、`/api/attn`）、结构实体（`/api/pipelines`、`/api/features`、`/api/specials`、`/api/ideas`）。
- **旁证（这一条已经在仓里被承认过一次）**：`app.js:3807-3808` 的注释原文——"3s 脉冲令牌只按工单目录 mtime 算，登记/重排一条待办一个字节都不会动到工单目录——不自己看着账，这页会一直停在打开那一刻的读数"。于是项管页**自己另挂了一条 30s 轮询 + 自算签名**（`app.js:3807-3817`，共 11 行，`ssig` 在 `3809` 逐粒折 `粒ID|状态|版本号`）来补令牌看不见的那一片。**同一页上，令牌既太宽（别家项目的单也触发全取）又太窄（本页关心的排期账它看不见）**——这正是"一比特电线"的两面。
- **实证升级（本轮变体 B 实跑，附录 F.4）**：上面这条"既太宽又太窄"此前只有**源码注释**作旁证，本轮拿到了**运行时实测**，两面各一发：
  - **太宽**：往**别家项目**（Ticketflow）落一张单 ⇒ 令牌 `1792147276941618 → 459608822182815` **变了**，而 `viewRelay` 该拍的 HTML 与上一拍**逐字节相同**（23453 B → 23453 B）。别家的单触发了本页 13 口全取，产出零变化。
  - **太窄**：往**想法池**加一条（`POST /api/ideas`，而想法在池就画在本页 `app.js:3844-3850`）⇒ 令牌 `7777437709708877 → 7777437709708877` **一点没变**，而该拍 HTML **变了 453 B**、morph 触碰 **20 个节点**。**即：真正该刷的这一变，令牌完全漏报**——今天全靠 `app.js:3807-3817` 那条自建 30s 补丁轮询兜着，最坏 30 秒内面板停在旧读数。
  这两发实测把病灶 7 从"注释里承认过一次"抬成了 A/B 双面实证，也直接给不变量 **I14（只窄不宽）** 补上了反面教材：**今天的全局令牌本身就是一个漏报的分片**——它名义上"扫全部状态目录"，而想法池不在状态目录里。乙阶段写归属表时，`_想法`／`_排期` 这类**状态目录之外的数据源**必须一并进片，否则分片令牌会原样继承这个漏报。
- **病理**：13 口捆在一起，任何一片变化都触发全部 13 口重取，同时另有一片变化 13 口全都察觉不到。
- **改法**：13 口按上述四类分组，各组挂到病灶 1 的对应令牌分片上；`repaint` 只重取变了的那一组，其余组复用上一拍结果（结果缓存与分片令牌同期失效）。**副产品**：`app.js:3807-3817` 那条自建的 30s 补丁轮询可以退役（−11 行）——排期账进了分片令牌，它就不再是"令牌看不见的一片"。
- **判它好了的数字**："只令牌变"输入下 fetch = **0**（改前实测 **13**）；"数据真变（工单落盘）"输入下 fetch ≤ **4**（只重取工单片相关的口：`/api/board`、`/api/relay`、`/api/pm/ledger`、`/api/pm/chains`），改前实测 **13**。**另加一条漏报判据**（本轮新增）：想法入池后，本视界分片令牌**必须变**——改前实测**不变**（`7777437709708877 → 7777437709708877`），改后须变；这一条不达标即 I14 破，分片比全局令牌还坏。

---

#### 病灶 8 · 详情页的徽章更新在 `morph` 之外，两道闸都管不到　【新增 · 三轮静态复核发现，工单背景未列】

- **锚点**：`apps/studio/public/app.js:4274-4277`（`repaint` 的 detail 分支，在 `morph(box, await viewDetail(...))` **之后**）→ `apps/studio/public/app.js:4132-4140`（`详情徽章`）
- **实测数字**：`详情徽章(id)` 被调用时**不传 `state`**（`app.js:4274` 实为 `await 详情徽章(tg.id)`，单参），于是 `app.js:4133` 的 `if (state == null)` 必中，**每拍必发一次** `api('/api/ticket?id=…')`；若该单的态落在 `初检/核查/仲裁/完成/待处理` 五态之一（`app.js:4135` 的名单），`app.js:4136` **再发一次** `api('/api/runner')`。即 detail 视图每拍在 `morph` 之外另有 **1～2** 次 fetch；按 M3 的 20 拍/分，**每分钟 20～40 次 HTTP**，与"数据变没变"零相关。另：`app.js:4276` 的 `徽.outerHTML = 新徽` 是 `outerHTML` 的 **setter**（M7 三处中的第三处），命中即**整节点替换**，不是属性对齐。
- **病理**：本方案的两道闸——分片闸开在 `repaint` 取数之前、签名闸开在 `morph` 入口之前——**都在这段代码的上游**。这一段发生在 `morph` **之后**，是一条绕过全部闸门的旁路。不点名的话，实现单会把 G1 写成"全视图零成本"，而 detail 视图的 fetch 下界其实是 1。
- **改法**：把徽章并进 `viewDetail` 的返回串（它本来就在取同一张单，`/api/ticket` 这一口是**重复取数**），让徽章随 morph 一起走闸。
- **⚠️ 过渡改法的破产（异厂评审 K2 击杀，2026-08-26 · 已改正）**：原文写"过渡期的最小改法是给 `app.js:4274-4277` 加一道同款签名短路（`新徽 === 上次徽` 即整段不写、不发第二口）"，并据此把 A13 的甲阶段目标写成 `fetch = 0`。**这在时序上不成立**：`新徽` 只有把 `/api/ticket` 请回来才算得出，签名比对发生在**网络之后**，短路救得下"写不写 DOM"，救不下"发不发第一口"。故甲阶段的 `fetch = 0` **从构造上不可能达成**。改正后的分层改法：
  - **甲阶段（可做，+6 行）**：① `app.js:4274` 改为 `详情徽章(tg.id, 本拍已取到的 state)`——`repaint` 手里本来就有这张单的态，把它传进去即绕开 `app.js:4133` 的 `if (state == null)`，**第一口 `/api/ticket` 直接消失**；② 徽章串签名短路，杀掉 `outerHTML` setter 的整节点替换。**甲阶段目标随之改为 fetch ≤ 1**（残留的是五态名单命中时那口 `/api/runner`，它取的是执行器实况、不是单本身，签名闸管不到）。
  - **丙阶段（原定）**：徽章并进 `viewDetail`，`/api/runner` 随视图走同一道闸，fetch 才真正到 **0**。
- **判它好了的数字**：detail 视图"只令牌变"输入下——**甲阶段 fetch ≤ 1**（改前 1～2），**丙阶段 fetch = 0**；`outerHTML` **setter** 调用次数 **甲阶段即 = 0**（改前每拍 1）。

---

### 背景四处病灶的回应对账

| 背景条目 | 回应 | 落在本方案哪一条 |
|---|---|---|
| ① 全局令牌 × 全量重算（病根） | **认**，且拆成三条分别治：令牌侧（病灶 1）、重算侧（病灶 2）、取数侧（病灶 7） | 病灶 1 / 2 / 7 |
| ② 缺视图级签名闸 | **认**。补一处工单未提的实测：全前端签名比对**只有 1 处命中**（M6），主路径 0 处 | 病灶 3 |
| ③ morph 的序列化开销 | **认**，并补正量纲：成本是**自相似**的（不命中的每一层都多付），所以治法是"减少不命中的层数"而非"加速序列化"；另指出新侧 `w` 的 parse→serialize 往返可直接消掉 | 病灶 4 |
| ④ 改法已有仓内成例（甘特岛），不必新发明 | **认**，且照单全收：`gantt.js:741-755` 四步原样上提为 `keyedList`，`data-morph-skip` 语义一字不改（新容器用新属性名，不与岛混用） | 病灶 5 |

**无驳回条目**——四条经 A 级静态证据逐条复核，全部成立。故本方案不需要出示反证数字。

---

## 五、改动面与路径

### 5.1 分四阶段，每阶段自成一张实现单、自带门禁

阶段按"收益 / 风险"排序，**前一阶段的门禁不过，后一阶段不许开工**。

| 阶段 | 内容 | 改动面 | 预估行数 | 风险 | 依赖 |
|---|---|---|---|---|---|
| **甲** | 视图级签名闸（病灶 3、病灶 4 之①③、病灶 8 的过渡改法） | `public/app.js`（`morph` 入口 +3、收尾 +1、`签名()` 新增 +8、detail 徽章段短路 +4） | **+16 行** | 低（纯前端、纯早退、无协议改动） | 无 |
| **乙** | 令牌分片 + `repaint` 分片判定 + 13 口分组（病灶 1、2、7） | `server.js`（`/api/pulse` +30）、`lib/core/pulse-shard.js`（新，+45）、`public/app.js`（分片判定 +20、13 口分组 +35、轮询带参 +8、退役自建 30s 轮询 −11） | **+127 行** | 中（跨前后端、有兼容期） | 甲 |
| **丙** | 通用增量件上提（病灶 5、病灶 4 之②） | `public/incremental.js`（新，+70）、`public/app.js`（`viewBoard` 接入 +30、`morph` 让路闸 +1） | **+101 行** | 中（触碰三态最密的面，测试面最重） | 甲 |
| **丁** | 服务端 mtime 缓存（病灶 6） | `lib/core/store.js`（+35）、`server.js`（共用扫描 +10） | **+45 行** | 中（缓存失效点必须唯一，否则读到旧单） | 乙（**并须与乙同批上线**，见下方 K4 注） |

> **⚠️ 阶段依赖更正（异厂评审 K4，部分采纳）**：K4 指出令牌分片**只省前端的口，不省服务端的盘**——`/api/pulse` 该扫的 `20 × N` 次 `statSync` 一次不少（M2/M3），前端 fetch 归零并不能阻止服务端 I/O、杀毒扫描或网络盘延迟。**这一条成立**，且原方案的阶段序把它放大了：乙上线、丁未上线的那个窗口里，服务端代价**一点没降**，而前端已经据此宣布"代价归零"。**处置**：丁不再是"乙之后可选的第四阶段"，而是**与乙同批上线的必配件**——分片令牌那次扫描的产物直接喂给 `store` 的 mtime 缓存（§四 病灶 6 改法里"两处共用一次扫描"本就是这么设计的，只是阶段序把它拆开了）。乙单独上线**不许判过**。
>
> **K4 的量级前提予以驳回，附反证数字**：K4 的失败场景设在「50 个常驻客户端 × 50,000 张单 ⇒ 5,000 万次 stat/分钟」。本系统的运行点不是这个量级——监制台是**单制作人的常驻桌面面板**（Electron 壳，`main.js` 一个窗口），常驻客户端数是 **1～2**，不是 50；库存量级见本方案实测盘面 **127 张**（表B，按实盘分布铺设），不是 50,000。按 M2/M3 实算，单客户端实际负载是 `20 拍/分 × 127 ≈ 2540 次 statSync/分`，与 K4 的 5,000 万相差 **约 2 万倍**。故 K4 结论中"这不是 +30 行 pulse handler 能消掉的成本"在**本系统的运行点上不成立**；被采纳的是它的**机理**（分片不降服务端盘），不是它的**量级**。若将来监制台真的多客户端化或库存上万，本条须重开——那时的正解是事件索引/推送，不是继续加缓存，此处存照。

**测试面**（与实现同单交付，程序职能写、审检职能跑）：

| 文件 | 增/新 | 预估行数 |
|---|---|---|
| `test/repaint-converge.test.js` | **新** | ~190 行 |
| `test/pulse.test.js` | 增补（签名闸/让路闸回归） | ~60 行 |
| `test/probe-repaint.js` | **新**（附录 A 转正为常驻量具，不是断言套件） | ~150 行 |
| `test/pulse-shard.test.js` | **新**（乙阶段：分片归属表 + 只窄不宽） | ~90 行 |

**改动面总计**：生产代码 **~289 行**（三轮复核后 +4，病灶 8），测试 **~490 行**。

### 5.2 改哪里 / 不改哪里（边界给死，不留"视情况"）

| 面 | 改 | 说明 |
|---|---|---|
| `morph` / `morphKids` / `morphAttrs` 函数体 | **不改** | 约束丁。只在 `morph()` 的**第一行之前**加闸、**最后一行之后**落签 |
| `morphNode` 函数体 | **改 1 行**（丙阶段，加第四道早退）｜⚠️ **本行系异厂评审 K3 击杀后改正，原文写"不改"** | **原文自相矛盾，已判原文错**：§5.2 声明"`morphNode` 函数体不改"，而病灶 4 改法②要求"`morph` 见 `data-keyed` 容器即整枝让路"、§5.1 丙阶段又已列出"`morph` 让路闸 **+1**"——同一份方案里一处说不改、一处已给改动预算。K3 并同时构造了不改的后果：`viewBoard` 吐出的 `data-keyed` **空壳**遇上装着卡片的旧 DOM，`morphNode` 不认识这个属性，会照常递归对齐到空壳、**把所有旧卡片删光**（一拍白屏，随后由 `keyedList` 补回，正是"先拆后补"——直接违反约束丙）。**处置**：约束丁改为"三道让路闸的**语义**一字不改，允许**并列增设**第四道早退（纯加法，`if (w.hasAttribute('data-keyed')) return;`，位置紧邻 `app.js:4239` 的 `data-morph-skip` 那行）"。**回滚兜底**：若审检判定约束丁不容任何增行，则改走**零改动路线**——键控容器直接复用现成的 `data-morph-skip` 属性（甘特岛今天走的就是这条，`gantt.js:3-6`），代价是与岛共用一个属性名、可观测性变差。两条路线择一，实现单开工前须书面定死，不许边写边选 |
| `pulsePlan` / `pulseTarget` | **不改** | 不变量 I7。分片判定放在 `repaint` 里，不放进这两个纯函数（它们的入参契约不扩） |
| `PULSE.整页最小间隔` / `交互静默` 两道闸 | **不改** | 不变量 I8 |
| `pollLoop` 与 `_loopGen` | **不改** | 不变量 I9 |
| `public/gantt.js` | **不改** | 岛是被抄的成例，不是被改的对象。`keyedList` 是**照它写一份通用的**，岛自己不迁移（迁移是独立议题，不在本方案范围） |
| `data-live` / `data-morph-skip` / `CANVAS` 三闸 | **不改** | 不变量 I4/I5/I6。新增的 `data-keyed` 是**第四道**让路闸，与前三道并列，不替换任何一道 |
| `/api/pulse` 无参数返回形状 | **不改** | 不变量 I15。分片信息只在带参时附加 |
| `repaint` 的 detail 分支 `app.js:4274-4277` | **改**（甲阶段一并） | 三轮复核补入（病灶 8）。这是全 `repaint` 里**唯一一段在 `morph` 之后取数**的代码，两道闸的下游。甲阶段给它加同款签名短路（+4 行）；把徽章并进 `viewDetail` 属**丙阶段**的接入面，不在甲阶段做 |
| `style.css` | **不改** | 同施工令-048 的判断（`回执-048.md` §零③）：这是重算问题，不是样式问题 |
| Electron 主进程 / `main.js` / `preload.js` | **不改** | 与本方案无关 |

### 5.3 职责归哪一层（三个边界问题的明确答案）

**问一：签名算在哪一层？**
答：**算在 `morph` 这一层，不算在视图函数里**。理由：视图函数有十几个，每个都自己算签名 = 十几处口径，必然走散（同 `pulseTarget` 靠调用方传 `ROUTES` 键、不自己抄一份表的道理，`app.js:4190-4191` 有自述）。`morph` 是唯一收口处，签名取自它已经拿到的 `html` 串，天然全量、天然一份。

**问二：分片令牌的归属表放前端还是后端？**
答：**后端一处**（`lib/core/pulse-shard.js`）。理由：它是"数据变了什么"的所有者（§1.3）；放前端就是让消费方去推断产生方的事实，正是今天这一整套病的形状。前端只做一件事：把自己的视界坐标（hash + 项目）翻译成"我关心哪几片"，这张表放前端（它是视界的所有者）。**两张表，各归各家，不许对方抄一份**。

**问三：`keyedList` 与 `morph` 谁先谁后？**
答：**`morph` 先，`keyedList` 后**。视图函数照旧吐一整串 HTML（包括 `data-keyed` 容器的**空壳**），`morph` 把壳对齐好、见 `data-keyed` 整枝让路，随后（同步收尾，照 `app.js:3759-3761` 用 `setTimeout` 不用 `rAF` 的成例）由视图函数注册的 `keyedList` 调用填内容。这与甘特岛今天的接线**完全同构**——壳归 morph、内容归岛（`gantt.js:3-6`），只是把"岛"泛化成"任意键控列表"。

### 5.4 兼容与回滚

- **兼容期**：乙阶段上线后，`/api/pulse` 同时支持"无参 = 旧形状"与"带参 = 分片形状"。换装窗口内新旧前端混跑不会哑火（不变量 I15）。
- **回滚**：甲/丙两阶段是纯前端，回滚 = 回退 `public/` 文件。乙阶段的服务端改动是**加法**（新参数、新字段），回滚前端即可让服务端的新能力静默闲置，不必回退服务端。丁阶段的缓存有开关（环境变量 `STUDIO_NO_PARSE_CACHE=1` 直通旧路径），出事即关。

---

## 六、验证法

### 六.0　门禁 0：基线三数（**实现单开工第一动作**，不是收尾动作）

- **谁跑**：审检职能（测试执行权归审检，见岗位协议职能边界总表）。
- **跑什么**：`node apps/studio/test/probe-repaint.js`（附录 A 脚本转正后的路径）。改前跑一次、每阶段改完各跑一次。
- **什么算过**：脚本正常退出（exit 0）且 12 个格子全部有数。**改前的 12 数不填满，后续任何阶段不许判过**——没有基线就没有"收敛了多少"这句话。
- **回执必须贴**：探针原始 stdout 全文（不许摘要）、表B 的改前列与改后列并排。

> **状态：改前一半已过**（2026-08-26 04:42 第四会话）。附录 A 脚本原样跑通、exit 0、12 格全满（§四 表B），连跑两遍逐格相同，原始 stdout 见附录 F.1/F.2。**改后列仍待实现单各阶段填**。
> 实现单开工时**不必重跑改前基线**——直接引本表即可，但**须核对盘面口径一致**（铺单 127 张 = 分布 12 态 97 张 + 别家 30 张；若按 D.2 第 ④ 件补了 `废弃` 态，铺单数会变，届时改前列须随之重跑，不许拿两个盘面的数对照）。

### 六.1　落点套件

| 套件文件 | 归属 | 职责 |
|---|---|---|
| `apps/studio/test/repaint-converge.test.js` | **新建** | 本方案 G1–G4 的全部断言 |
| `apps/studio/test/pulse.test.js` | 增补 | 三态与三闸在**新增快路径下**的回归（不许因为加了闸就绕过它们） |
| `apps/studio/test/pulse-shard.test.js` | **新建**（乙阶段） | 分片归属表的"只窄不宽" |
| `apps/studio/test/probe-repaint.js` | **新建** | 量具，非断言套件；出数供回执贴 |

夹具沿用现成的三件，不另造：`test/frontend-sandbox.js`（`vm` 装真 `app.js` 直调渲染函数）、`test/minidom.js`（拿节点身份 `===` 验"哪个节点被动过"）、`test/helper.js`（`makeRoot`/`seed`/`收尾`）。`morph` 一律用 `@testable-begin morph` 标记从生产源码**原样抽取**（`test/pulse.test.js:348-354` 的成例），不抄本（不变量 I10）。

### 六.2　逐条断言的形状与改后目标值

> 「触碰节点数」的判法固定为：**被改写的老节点 + 新建挂上的节点 + 被摘掉的老节点**，一律按节点对象身份（`===`）判，不拿字符串比。判法的参考实现见附录 A 的 `快照()`/`触碰()` 两个函数。

| # | 类型 | 断言形状 | 改后目标值 |
|---|---|---|---|
| **A1** | **签名比对类**（工单点名必备） | 同一份数据连拍两次：第二拍进 `morph` 前签名与容器上 `data-msig` 相等 ⇒ `morph` 立即返回 ⇒ 触碰节点数 | **= 0** |
| **A2** | **渲染次数类**（工单点名必备） | 用计数器包装 `ROUTES` 表的视图函数；同一份数据连拍两次（令牌变、数据不变） ⇒ 视图渲染函数调用次数。**两拍必须逐拍 `await`，且须先断言两拍的 `repaint` 返回值都 ≠ `'busy'`**——`app.js:4263` 的 `_重绘中` 再入闸命中即 `return 'busy'` 且**根本不渲染**，并发发两拍的话计数照样得 1，但那是再入闸的功劳、不是签名闸的，断言就证不动它要证的事（三轮复核 E4） | **= 1**（甲阶段）／**= 0**（乙阶段分片令牌生效后）；前置：两拍返回值 ∈ {`patch`}，`busy` 计数 **= 0** |
| **A3** | 分片令牌类 | 往**别的项目**的状态目录落一张单 ⇒ 当前视界（`项目=TK`）的分片令牌 | **不变**；且该拍 `repaint` 调用 **0** 次、fetch **0** 次 |
| **A4** | 分片"只窄不宽"类（**K6 击杀后重写**） | 原文只遍历 `store.STATES` **13 个状态目录** × 写路径——**这正是今天全局令牌的漏报面**（`/api/pulse` 也只扫状态目录，于是想法池的变更它看不见，F.4 实测坐实）。照原文写，分片会**原样继承**这个漏报，而 A4 全绿。改为：**按仓根目录自动枚举全部数据源**（13 个状态目录 + `_排期` + `_想法` + 其余顶层 `_*` 目录），逐源构造一次真实写入，验其落进至少一个分片 | **孤儿数据源 = 0** 且 **孤儿变更数 = 0**。**判据须是「扫出来的源」而不是「表里列的源」**——测试从磁盘枚举、拿归属表比对，**新增一个数据源而没登记 ⇒ 测试自动变红**。这一条是 K6 那笔"每加一个源要人工同步三处"维护成本的正面处置：让漏同步**在测试里立刻爆**，而不是在面板上静默过期 |
| **A5** | 增量类 | 看板：新增 1 张本项目单 ⇒ 触碰节点数；且未变卡片的节点身份 | 触碰 **≤ 单卡节点数 + 2**；未变卡片身份逐个 `===`（**相等数 = 卡片总数 − 1**） |
| **A6** | 序列化上界类（**双计数**，K5 改正后） | 在 `minidom` 的 `El.prototype` 上挂**两个**计数器：①`外层次数`（带深度守卫，量"比了几层"）②`累计元素数`（不带守卫，量递归展开的真实工作量） | ① **≤ 2 ×（顶层键块数 + 1）**；② **≤ 树节点数 × 1.2**。**两个都过才算过**；只报 ① 的回执按不完整退回（K5：只看 ① 时，一次递归序列化一万张卡也记作"1 次"） |
| **A7** | 三态回归（I1/I2/I3） | 签名闸命中的那一拍：容器上预置 `scrollTop=480`、`.fold open` 类、焦点在 `#q` 且已敲入文本 ⇒ 三者 | 全部**原样存活**；且该拍触碰节点数 **= 0** |
| **A8** | 三闸回归（I4/I5/I6） | 签名闸命中拍与增量件重绘拍，各验一次：`data-live` 的运行时值、`data-morph-skip` 岛内行的节点身份、`<canvas>` 节点身份 | 三者身份/值**全部不变**（`=== ` 相等数 3/3） |
| **A9** | 签名充分性（I13） | 构造"数据变了但摘要相同"的对抗样本（同长度、同字段集、值不同） ⇒ 签名 | **必须不等**（若散列相等则串比对必须判不等并放行重绘） |
| **A10** | 服务端解析缓存（病灶 6） | 在 `store.parse` 挂计数器；同数据两次 `/api/board` ⇒ 第二次的 `gray-matter` 解析次数 | **= 0** |
| **A11** | 写后即失效（丁阶段安全性） | `store.move` 一张单后立即 `/api/board` ⇒ 该单的态 | **= 新态**（不许读到缓存里的旧态） |
| **A13** | 闸外旁路类（病灶 8） | 把 hash 置成 `#/t/<某单号>` 走 detail 分支，同一份数据连拍两次 ⇒ 第二拍**整个 `repaint`**（含 `morph` 之后那段徽章代码）发出的 fetch 次数，与 `outerHTML` **setter** 调用次数 | fetch **= 0**（改前 1～2）；setter **= 0**（改前 1）。被测单须挑一张态在 `初检/核查/仲裁/完成/待处理` 五态内的（`app.js:4135`），否则量到的是 1 口而非 2 口的那一档 |
| **A14** | **令牌漏报类**（本轮实测新增，护 I14） | 往想法池加一条（`POST /api/ideas`，`viewRelay` 真渲染这一格）⇒ 当前视界的**分片令牌** | **必须变**。改前实测**不变**（`7777437709708877 → 7777437709708877`，而该拍 HTML 变了 453 B、morph 触碰 20 个节点）。**这条不过，乙阶段一律不许判过**——分片继承了全局令牌的漏报，就是拿 I14 换性能 |
| **A12** | 全量零回归 | `apps/studio` 全量测试链 | **failed = 0**（文件数/断言数照 `test/helper.js:67` `收尾()` 的口径报） |

### 六.3　每档门禁：谁跑、跑什么、什么算过

| 档 | 谁跑 | 跑什么命令 | 什么算过 |
|---|---|---|---|
| 门禁 0（基线） | 审检 | `node apps/studio/test/probe-repaint.js` | exit 0 且表B 12 格全满 —— **改前列已过**（2026-08-26 04:42，见 §六.0） |
| 甲阶段 | 审检 | `node apps/studio/test/repaint-converge.test.js` + `node apps/studio/test/pulse.test.js` | A1/A2(=1)/A6/A7/A8/A9 全绿；A12 failed=0 |
| 乙阶段 | 审检 | 上一档 + `node apps/studio/test/pulse-shard.test.js` | 加验 A2(=0)/A3/A4/**A14** |
| 丙阶段 | 审检 | 同乙档 | 加验 A5；A7/A8 必须**再跑一遍**（本阶段触碰三态最密的面） |
| 丁阶段 | 审检 | 同乙档 | 加验 A10/A11 |
| 收官 | 审检 | `node apps/studio/test/probe-repaint.js` 再跑一次 | 表B 改后列全满，且逐格达 §二 G1–G4 的目标值 |

### 六.4　回执里必须贴的数字（缺一即回执不完整）

1. **表B 两列**：改前 12 数 / 改后 12 数，并排贴，附探针原始 stdout 全文。**改前列已在册**（§四 表B），实现单直接引，不必重跑（盘面口径须一致，见 §六.0）。
0'. **序列化次数四数**：项管页 2/2、看板 8/58 是改前值（表B 附产表），改后四数并排贴——G4 的达成只看这四个数。
0''. **A14 的令牌两值**：想法入池前后的分片令牌串，原样贴（改前实测两值相同即漏报）。
2. **A1 的实测触碰数**：改前 / 改后（目标 0）。
3. **A2 的实测渲染次数**：改前 / 改后（甲后 1、乙后 0）。
4. **A5 的实测触碰数** 与 **单卡节点数**（两个数都要，否则"≤ 单卡 + 2"无从核）。
5. **A6 的实测 `outerHTML` 次数** 与 **顶层键块数**（同上，两个数）。
6. **A10 的实测解析次数**：改前 / 改后（目标 0）。
7. **A12 的三元组**：`文件 N · 断言 M · 失败 0`。

**不可判词禁令**：回执与实现单里不许出现"更快 / 更流畅 / 明显改善 / 基本不卡"这类词。每一句关于性能的话后面必须跟一个来自上表的数。

---

## 附录 A · 探针脚本全文

> 三段式照 `test/frontend-sandbox.js` 抬头那段案由：造临时仓 → `STUDIO_STUB=1` 进程内 `require server.js` 起真服务 → `vm` 装 `public/app.js` 真调渲染函数。单进程 `node`，不起浏览器/Electron。
> 本会话未获准执行（§〇）。转正落点：`apps/studio/test/probe-repaint.js`。

```javascript
// probe-repaint.js — TF-5「面板重绘病灶」基线三数探针（2026-08-26）
//
// 三段式（照 test/frontend-sandbox.js 的成例）：
//   ① 造临时仓（test/helper.makeRoot + seed）→ ② STUDIO_STUB=1 在进程内 require server.js 起真服务
//   → ③ vm 装 public/app.js，真调 viewRelay / viewBoard，量它真吐出来的东西。
// 量三数：① 每拍 fetch 次数 ② 渲染函数吐出的 HTML 串字节数 ③ morph 实际改写的节点数。
// 两种输入各量一遍：
//   「数据真变」= 往当前项目（TK）写一张新单；
//   「只令牌变」= 往**别的项目**（Ticketflow）写一张新单——当前视图根本不展示它，
//                 但 /api/pulse 的全局令牌照变，3s 轮询照样 repaint()。后者是本单要打的靶。
// 不改任何生产代码：本脚本只读 apps/studio 源码，写盘只写 os.tmpdir() 下的临时仓。
'use strict';
const fs = require('fs');
const path = require('path');
const S = 'D:/GitHub/Ticketflow/apps/studio';
const PORT = Number(process.env.PROBE_PORT || 4993);
const 规模 = Number(process.env.PROBE_N || 1); // 1 = 基准盘；2 = 翻倍盘（验「代价只与 n 有关」）

process.env.STUDIO_STUB = '1';
process.env.STUDIO_PORT = String(PORT);

const { makeRoot, seed } = require(S + '/test/helper');
const store = require(S + '/lib/core/store');

// ── ① 临时仓 ───────────────────────────────────────────────
const root = makeRoot();
process.env.STUDIO_ROOT = root;

// 两项目注册：projActive() 要 ≥2 个注册项目 + localStorage 选中，才进「按项目过滤」的真实语境
const cfg = JSON.parse(fs.readFileSync(path.join(root, 'studio.config.json'), 'utf8'));
cfg.项目 = { 默认: 'TK', 注册: { TK: {}, Ticketflow: {} } };
cfg.模型 = { 项管: 'opus' };
fs.writeFileSync(path.join(root, 'studio.config.json'), JSON.stringify(cfg), 'utf8');

// 盘面铺设：按监制台实盘的量级分布铺单（TK 为当前项目，Ticketflow 为「看不见的别家」）
const 分布 = { 待审: 6, 待派: 10, 已排期: 5, 在途: 8, 初检: 3, 核查: 3, 仲裁: 2, 完成: 12, 归档: 40, 待处理: 4, 待重派: 2, 挂起: 2 };
let 铺 = 0;
for (const [st, n] of Object.entries(分布)) {
  for (let i = 0; i < n * 规模; i++) {
    seed(root, st, { id: `TK-${st}-${i}`, title: `TK 的活儿 ${st} 第 ${i} 号 · 标题写长一点更接近实盘`, 项目: 'TK', 职能: ['策划', '程序', 'QA', '美术'][i % 4], 优先级: ['P0', 'P1', 'P2'][i % 3] });
    铺++;
  }
}
for (let i = 0; i < 30 * 规模; i++) { seed(root, '待派', { id: `TF-别家-${i}`, title: `Ticketflow 的活儿 第 ${i} 号`, 项目: 'Ticketflow' }); 铺++; }

// 排程台账（甘特/项管页的数据源），同样两项目对半
const 台账目录 = path.join(root, '_排期');
const 粒 = [];
for (let i = 0; i < 24 * 规模; i++) {
  粒.push({ 粒ID: `g${i}`, 题: `排期粒 ${i}`, 项目: i % 3 === 0 ? 'Ticketflow' : 'TK', 状态: i % 4 === 0 ? '已成单' : '计划',
    单号: i % 4 === 0 ? `TK-待派-${i % 10}` : null, 计划开始: '2026-08-26T09:00', 计划完成: '2026-08-26T17:00', 版本号: 1, 批: 'A', 序: i });
}
try { fs.mkdirSync(台账目录, { recursive: true }); } catch { /* 已存在 */ }

// ── ② 真服务 ───────────────────────────────────────────────
const server = require(S + '/server.js');

// ── 量具 ───────────────────────────────────────────────────
const { 装载前端 } = require(S + '/test/frontend-sandbox');
const { El, doc, win } = require(S + '/test/minidom');
const src = fs.readFileSync(path.join(S, 'public', 'app.js'), 'utf8');
const { morph } = (() => {
  const a = src.indexOf('// @testable-begin morph');
  const b = src.indexOf('// @testable-end morph');
  if (!(a >= 0 && b > a)) throw new Error('morph 抽取标记丢了');
  // eslint-disable-next-line no-new-func
  return new Function('document', 'window', '$', src.slice(a, b) + '\nreturn { morph };')(doc, win, (id) => doc.getElementById(id));
})();

// outerHTML 序列化计数（病灶 4 的量具）：morph 每比一层就序列化一次子树。
// **深度守卫必须有**（2026-08-26 静态复核 D1）：minidom 的 `get outerHTML()` 内部走
// `this.innerHTML` → 逐子 `c.outerHTML`，是**递归**的；不设守卫的话一次顶层取值会把
// 整棵子树的元素数全记进来，量到的就不再是「morph 比了几层」而是「树有多少个元素」，
// G4/A6 的上界 `2 ×（顶层键块数 + 1）` 从定义上就不可能达成。守卫只计**外部发起**的那一次。
let 序列化 = 0, _序深 = 0;
const 原getter = Object.getOwnPropertyDescriptor(El.prototype, 'outerHTML').get;
Object.defineProperty(El.prototype, 'outerHTML', {
  configurable: true,
  get() {
    if (_序深 === 0) 序列化++;
    _序深++;
    try { return 原getter.call(this); } finally { _序深--; }
  },
});

const 全节点 = (n, out = []) => { out.push(n); for (const c of (n.childNodes || [])) 全节点(c, out); return out; };
const 属性串 = (n) => [...n.attrs].map(([k, v]) => k + '=' + v).join('\u0001');
function 快照(box) {
  const m = new Map();
  for (const n of 全节点(box)) {
    if (n.nodeType === 3) m.set(n, { t: 3, v: n.nodeValue });
    else m.set(n, { t: 1, a: 属性串(n), c: (n.childNodes || []).slice(), val: n._value });
  }
  return m;
}
// 触碰 = 被改写的老节点 + 新建挂上的节点 + 被摘掉的老节点。一律按**节点身份**判，不比字符串。
function 触碰(前, box) {
  const 后 = 全节点(box), 后集 = new Set(后);
  let 改 = 0, 新 = 0, 删 = 0;
  for (const n of 后) {
    const p = 前.get(n);
    if (!p) { 新++; continue; }
    if (n.nodeType === 3) { if (p.v !== n.nodeValue) 改++; continue; }
    const c = n.childNodes || [];
    let 变 = 属性串(n) !== p.a || c.length !== p.c.length || p.val !== n._value;
    if (!变) for (let i = 0; i < c.length; i++) if (c[i] !== p.c[i]) { 变 = true; break; }
    if (变) 改++;
  }
  for (const n of 前.keys()) if (!后集.has(n)) 删++;
  return { 触碰: 改 + 新 + 删, 改, 新, 删, 树节点数: 后.length };
}

const B = `http://127.0.0.1:${PORT}`;
let 叫号 = [];
function 装fetch(ctx) {
  ctx.fetch = async (u, opt) => { 叫号.push(String(u)); return fetch(B + String(u), opt); };
}
const 计次 = () => { const n = 叫号.length, 单 = 叫号.slice(); 叫号 = []; return { 次: n, 口: 单 }; };

const 行 = [];
const 出 = (s) => { 行.push(s); console.log(s); };

// 令牌
const 令牌 = async () => (await (await fetch(B + '/api/pulse')).json()).token;

(async () => {
  await server.start();
  const ctx = 装载前端();
  await new Promise((r) => setTimeout(r, 60)); // 让载入时那一发 route() 的异步尾巴走完
  装fetch(ctx);
  ctx.localStorage.setItem('studio-proj', 'TK');
  await ctx.loadCfg(true);
  叫号 = [];

  出(`# TF-5 面板重绘基线探针 · 规模档 ${规模}（铺单 ${铺} 张，其中当前项目 TK ${铺 - 30 * 规模} 张 / 别家 Ticketflow ${30 * 规模} 张）`);
  出(`临时仓：${root}`);
  出(`node ${process.version} · 服务 ${B} · STUDIO_STUB=1`);
  出('');

  // 一拍 = 调一次视图渲染函数，量 fetch 次数 + HTML 字节数
  async function 拍(名, fn) {
    序列化 = 0;
    const t0 = process.hrtime.bigint();
    const html = await fn();
    const 毫 = Number(process.hrtime.bigint() - t0) / 1e6;
    const c = 计次();
    return { 名, html, 字节: Buffer.byteLength(html, 'utf8'), fetch: c.次, 口: c.口, 毫: Math.round(毫 * 10) / 10 };
  }

  async function 量一视图(视图名, fn) {
    出(`## 视图：${视图名}`);
    // 首渲
    const a = await 拍('首渲', fn);
    出(`  [首渲]        fetch=${a.fetch}  HTML=${a.字节} B  用时=${a.毫}ms`);
    出(`                取数口：${a.口.join(' , ')}`);
    const box = new El('div');
    box.innerHTML = a.html;
    doc.body = box; doc.activeElement = null;
    出(`                落地 DOM 节点数=${全节点(box).length}`);
    // 解析保真自检（2026-08-26 静态复核 D2）：minidom 的 parse 自述「不追求容错」，
    // 而这里喂的是生产视图真吐出来的几十 KB HTML（含 <svg>/<canvas>/内联事件/可能的注释）。
    // 解析走样不会报错，只会**静默**地把树建歪——③ 触碰节点数是拿这棵树数的，树歪则数无效。
    // 判法：重序列化后再解析一次，两次的节点数必须相等（幂等即自洽）；不等就别信这一列的数。
    const 重 = new El('div'); 重.innerHTML = box.innerHTML;
    const 保真 = 全节点(重).length === 全节点(box).length;
    出(`                解析保真自检：重解析节点数=${全节点(重).length} · ${保真 ? '幂等 ✅' : '不幂等 ❌（③ 列数字不可用，须先修 minidom 解析或换夹具）'}`);

    // 输入甲：只令牌变（往别家项目写一张单——当前视图根本不展示它）
    const t前 = await 令牌();
    seed(root, '待派', { id: `TF-别家-新-${Date.now()}`, title: '别家项目刚落盘的一张单', 项目: 'Ticketflow' });
    const t后 = await 令牌();
    出(`  [输入甲·只令牌变] /api/pulse 令牌 ${t前} → ${t后} · ${t前 === t后 ? '未变（!!）' : '已变 → 3s 轮询必触发 repaint'}`);
    const b = await 拍('只令牌变', fn);
    出(`                fetch=${b.fetch}  HTML=${b.字节} B  用时=${b.毫}ms`);
    出(`                与上一拍 HTML 逐字节${b.html === a.html ? '相同 ✅（这一整拍是纯浪费）' : '不同 ❌'}${b.html === a.html ? '' : `（差 ${Math.abs(b.字节 - a.字节)} B）`}`);
    序列化 = 0;
    const 前甲 = 快照(box);
    morph(box, b.html);
    const m甲 = 触碰(前甲, box);
    出(`                morph 触碰节点=${m甲.触碰}（改 ${m甲.改} / 新建 ${m甲.新} / 摘除 ${m甲.删}），outerHTML 序列化=${序列化} 次，树节点=${m甲.树节点数}`);

    // 输入乙：数据真变（往当前项目写一张单）
    const c前 = await 令牌();
    seed(root, '待派', { id: `TK-新-${Date.now()}`, title: 'TK 刚落盘的一张单', 项目: 'TK' });
    const c后 = await 令牌();
    出(`  [输入乙·数据真变] /api/pulse 令牌 ${c前} → ${c后} · ${c前 === c后 ? '未变（!!）' : '已变'}`);
    const c = await 拍('数据真变', fn);
    出(`                fetch=${c.fetch}  HTML=${c.字节} B  用时=${c.毫}ms`);
    出(`                与上一拍 HTML 逐字节${c.html === b.html ? '相同 ❌（该变没变）' : `不同 ✅（差 ${Math.abs(c.字节 - b.字节)} B）`}`);
    序列化 = 0;
    const 前乙 = 快照(box);
    morph(box, c.html);
    const m乙 = 触碰(前乙, box);
    出(`                morph 触碰节点=${m乙.触碰}（改 ${m乙.改} / 新建 ${m乙.新} / 摘除 ${m乙.删}），outerHTML 序列化=${序列化} 次，树节点=${m乙.树节点数}`);
    出('');
    return { 视图: 视图名, 首渲: a, 甲: { ...b, morph: m甲 }, 乙: { ...c, morph: m乙 } };
  }

  const r1 = await 量一视图('项管页 viewRelay', () => ctx.viewRelay());
  const r2 = await 量一视图('看板 viewBoard', () => ctx.viewBoard());

  出('## 十二数汇总表（三数 × 两视图 × 两输入）');
  出('| 视图 | 输入 | fetch 次数 | HTML 串字节 | morph 触碰节点数 |');
  出('|---|---|---|---|---|');
  for (const r of [r1, r2]) {
    出(`| ${r.视图} | 只令牌变 | ${r.甲.fetch} | ${r.甲.字节} | ${r.甲.morph.触碰} |`);
    出(`| ${r.视图} | 数据真变 | ${r.乙.fetch} | ${r.乙.字节} | ${r.乙.morph.触碰} |`);
  }
  出('');
  出('## 附：首渲对照（同一渲染函数，代价与「变了什么」无关）');
  for (const r of [r1, r2]) 出(`  ${r.视图}：首渲 fetch=${r.首渲.fetch} HTML=${r.首渲.字节}B ｜ 只令牌变拍 fetch=${r.甲.fetch} HTML=${r.甲.字节}B ｜ 数据真变拍 fetch=${r.乙.fetch} HTML=${r.乙.字节}B`);

  fs.writeFileSync(path.join(__dirname, `probe-out-n${规模}.md`), 行.join('\n') + '\n', 'utf8');
  process.exit(0);
})().catch((e) => { console.error('探针炸了：' + (e && e.stack || e)); process.exit(1); });
```

**跑法**：

```bash
node <脚本路径>              # 基准盘（铺单 127 张）
PROBE_N=2 node <脚本路径>     # 翻倍盘（铺单 254 张）——两档对照即验「代价只与 n 有关」
```

**转正时须补的六件**（本方案不代做，留给实现单；第 ④ 件见附录 D.2，第 ⑤ 件见附录 E.3，**第 ⑥ 件见附录 F.7，系异厂评审 K5 击杀后新增、优先级最高**）：① 排程台账 `_排期` 目录的落盘格式需对齐 `lib/pm/schedule` 的真实写口（当前脚本只建了目录，`/api/schedule` 因此返回空粒，项管页的甘特段数据偏小，**不影响 fetch 次数与 morph 触碰数两项，只影响 HTML 字节数**——转正时补上，并在回执注明补前/补后两组字节数）；② `store.parse` 计数器（A10 用）；③ 视图渲染函数调用计数器（A2 用）。

---

## 附录 B · 行号对照表（工单引用 → `HEAD 44e9c7d` 现值）

| 工单正文所引 | `HEAD` 现值 | 内容 | 偏移 |
|---|---|---|---|
| `server.js:1872` | **`server.js:1876`** | `app.get('/api/pulse', ...)` | +4 |
| `app.js:4392` | **`app.js:4406`**（周期在 `4424`） | 3s 轮询 `setInterval` | +14 |
| `app.js:4248` | **`app.js:4262`** | `async function repaint(因)` | +14 |
| `app.js:4197` | **`app.js:4211`** | `function morph(dst, html)` | +14 |
| `app.js:4221-4227` | **`app.js:4235-4248`** | `function morphNode(parent, o, w)` | +14 |
| `app.js:4225` | **`app.js:4239`** | `data-morph-skip` 让路闸 | +14 |
| `app.js:3651` | **`app.js:3665`** | `async function viewRelay()` | +14 |
| `app.js:526` | **`app.js:526`** | `async function viewBoard()` | 0 |
| `app.js:590` | **`app.js:568`**（`gb.dataset.k` 在 `572`） | `fillBar` 与它的 key 比对 | −22 |
| `app.js:4129` | **`app.js:4143`** | 施工令-048 注释块起始 | +14 |
| `gantt.js:308` | **`gantt.js:316`** | 「行渲染（签名即 HTML）」注释 | +8 |
| `gantt.js:587` | **`gantt.js:598`** | 「DOM 装配与增量重绘」注释 | +11 |
| `gantt.js:722` | **`gantt.js:733`**（签名短路在 `747-748`） | `function 画行(岛)` | +11 |

---

## 附录 C · 异厂评审（**已召开** · 2026-08-26 04:5x 第四会话）

| 项 | 值 |
|---|---|
| 意见合集 md（绝对路径） | `D:\GitHub\Ticketflow\docs\SLG\技术方案\面板重绘收敛-2026-08-26.评审合集.md` |
| 命令 | `node D:/GitHub/Ticketflow/packages/review-panel/review.js --file D:/GitHub/Ticketflow/docs/SLG/技术方案/面板重绘收敛-2026-08-26.md --out <上行> --timeout-min 4` |
| 送审稿 | **69306 字**（已含本轮实跑的表B 12 数与附录 F——评审看到的是有基线数字的稿子，不是空表稿） |
| 评审团（自动发现） | codex、deepseek |
| 在席 / 缺席 | 在席 **1**（codex）／缺席 **1**（deepseek） |
| 立场卷派发 | codex ＝ **可行性红队 + 成本红队**；deepseek ＝ **不变量红队** |
| 战果 | 意见 **6** 条，**击杀 6 / 未杀 0**；按卷：可行性红队 **3 杀**、成本红队 **3 杀**、不变量红队 **落空（缺席）** |
| 单厂闸 | 4 min；codex 耗时 218.0s、deepseek 耗时 134.7s 后判缺席 |

### C.0 席位如实标注（H91）

- **deepseek 缺席**，评审台记录的原因原文：**"应答里没有文本内容（content 为空）"**。其所领的**不变量红队卷本轮落空**——**不是"未能构造击杀"，是无人开火**。采信本方案时，不变量一章须自行补位或另轮补评。
- **与工单定夺方向的预判相反，须点明**：定夺方向（总监 · 2026-08-25）写的是"codex 池当前网络断（TLS 握手 eof），该厂缺席按 H91 额度弃厂条款如实标注即可；deepseek 卷照跑"。**实跑结果恰好反过来**——codex 正常在席并交了 6 条击杀，缺席的是 deepseek。本附录按**实测**记录，不按预判记录。
- 三卷中**无任何一卷出具"未能构造击杀"声明**：codex 领的两卷各构造出 3 条具体失败场景，全部计击杀；第三卷因缺席落空，不适用该声明。

### C.1 各厂意见原文（未删改，仅密钥净化——评审台 `scrub()` 口径）

> 原文出处：意见合集 md（上表路径）。以下为逐字转录。

**codex** · 立场卷：可行性红队、成本红队 · 状态：在席 · 模型 `codex 默认档` · 耗时 218.0s · 意见 6 条 · 战果：击杀 6 / 未杀 0

| 章节 | 意见 | 严重度 | 卷别 | 击杀 |
|---|---|---|---|---|
| §二 G1、病灶 1、§五乙 | 当前 `studio-proj=TK` 且看板打开时，别家项目在下一次 3s 轮询前写入 `TF-999`，前端必须先 `GET /api/pulse?项目=TK&视图=board` 才知道本片未变，因此该拍至少 1 次 fetch 和一次服务端判定，无法同时满足"整拍全部代价归零"与 fetch=0；除非改为服务端推送/订阅，否则应把目标改成"业务数据 fetch=0"。 | 高 | 可行性红队 | 击杀 |
| 病灶 8、§五甲、§六 A13 | 在 `#/t/TK-42` 且 `TK-42` 状态为"完成"时，第二拍数据完全未变，`详情徽章(tg.id)` 仍因未传 `state` 先请求 `/api/ticket` 再请求 `/api/runner`，随后才可能比较"新徽"签名，所以甲阶段所称"+4 行短路"不可能达到 A13 的 fetch=0；必须把版本判定移到网络请求之前或把徽章并入已有详情响应。 | 高 | 可行性红队 | 击杀 |
| §四 病灶 4/5、§五.2、§五.3 | 当 `viewBoard` 把嵌套的 `data-keyed` 卡片容器渲染为空壳、旧 DOM 内已有卡片且本拍仅列头计数变化时，现有 `morphNode` 不认识 `data-keyed`，会递归对齐到空壳并删掉所有旧卡；要让路就必须在 `morphNode` 增加第四道早退，这与 §五.2"morph/morphNode 函数体不改"直接冲突。 | 高 | 可行性红队 | 击杀 |
| 病灶 1/6、§五乙/丁 | 以 50 个常驻客户端、50,000 张单为例，3 秒 pull 一次分片令牌且没有事件索引或共享扫描承诺时，仍会产生 `20 × 50 × 50,000 = 5,000 万` 次 stat/分钟（项目归属还可能要求解析变更文件），前端省下的 fetch 不能阻止服务端 I/O 队列、杀毒扫描或共享盘延迟烧穿；这不是 +30 行 pulse handler 能消掉的成本。 | 高 | 成本红队 | 击杀 |
| G4、病灶 4、附录 A/D1 的序列化计数器 | 当一个普通 board 壳下嵌有 10,000 张 `data-keyed` 卡片且仅壳上的计数文本变化时，父节点的 `o.outerHTML` 比较会先递归序列化全部旧卡片，之后才到达 `data-keyed` 早退；深度守卫只把这笔 O(n) 工作记成"1 次 getter"，故 G4/A6 即使通过也会掩盖真实序列化成本，后续性能取证必然返工。 | 高 | 成本红队 | 击杀 |
| §五.3 职责划分、§六 A4/A14、附录 F.6 | 例如排程执行器在 12:00 直接修改 `_排期/g42`，现有 A4 只遍历 13 个状态目录的 `create/move/update`，A14 又只覆盖 ideas，因此测试可全绿而该源没有进入服务端分片表、前端关心片表和 13 口缓存失效组；每新增一个数据源都要人工同步至少三处实现和多套夹具，漏一次就是静默过期，维护成本呈持续线性扩张。 | 高 | 成本红队 | 击杀 |

```
第1卷 可行性红队：击杀 3 条
第2卷 成本红队：击杀 3 条

总体判断：方案把"前端不重绘"误写成"整拍零成本"，且丙阶段的让路机制与"不改 morph"边界互相否定。
先修订目标口径、重绘协议和变更索引，再谈分阶段施工。
```

**deepseek** · 立场卷：不变量红队 · 状态：**缺席**（原因：应答里没有文本内容（content 为空））· 耗时 134.7s · **该立场卷本轮落空**

### C.2 逐条采纳 / 驳回（驳回必附理由，部分采纳标明采纳哪一半）

| # | 卷 | 击杀要旨 | 裁定 | 理由 |
|---|---|---|---|---|
| **K1** | 可行性 | G1 的"fetch = 0"不可能——轮询自身那一口必须发 | **全采纳** | 成立且切中要害。§1.2 已论证自动刷新不可裁撤，那么它的那一口就不可能归零；原文把"业务口"的目标写成了"全部口"。讽刺的是探针**实现上一直是对的**（取令牌走沙盒外全局 `fetch`，从不计入 13/2），**只是这个口径从没写进目标**——K1 挑出的正是"实现比文档准确"这类最难自查的洞 |
| **K2** | 可行性 | detail 徽章的"签名短路"发生在网络之后，救不下 fetch | **全采纳** | 时序上无可辩驳：`新徽` 必须先请回来才算得出。这一条推翻的是**本方案三轮复核里最新、最得意的那条发现**（E3/病灶 8）的**改法部分**——病灶本身成立，开的药方不成立。已改为分层目标（甲 ≤1 / 丙 = 0），并给出真正能省下第一口的做法（把 `state` 传进 `详情徽章`） |
| **K3** | 可行性 | `data-keyed` 让路必须改 `morphNode`，与"不改"边界冲突 | **全采纳** | 不仅成立，而且指出的是**方案自身的内部矛盾**：§5.2 写"不改"，§5.1 丙阶段却已列"`morph` 让路闸 +1"。三轮自查都没发现同一份文档里这两处打架。K3 附带构造的失败场景（空壳灌进去删光旧卡＝"先拆后补"）直接违反约束丙，已据此新增不变量 **I17** |
| **K4** | 成本 | 分片只省前端口、不省服务端盘；50 客户端 × 5 万单 = 5000 万 stat/分 | **部分采纳**：机理采纳，量级驳回 | **采纳机理**——乙上线、丁未上线的窗口里服务端代价一点没降，而前端已宣布"归零"；已把丁从"第四阶段"改为"与乙同批上线的必配件"。**驳回量级并附反证数字**：本系统是单制作人常驻桌面面板（Electron 单窗口），客户端 1～2 不是 50；实盘量级见表B 铺单 **127 张**不是 50,000。按 M2/M3 实算单客户端 `20 × 127 ≈ 2540 次 statSync/分`，与 5,000 万差 **约 2 万倍**。故"这不是 +30 行能消掉的成本"在本运行点上不成立 |
| **K5** | 成本 | 深度守卫把一次递归 O(n) 序列化记成"1 次"，G4 可虚假通过 | **全采纳** | 成立，且**部分推翻了二轮复核 D1 的处置**。D1 加守卫是对的（不加则量到的是"树里有多少元素"），但**拿守卫版计数单独当成本判据**是错的。已改为双计数：① 层数（守卫版）② 累计元素数（无守卫版），**两个都过才算过**。这条对"改后取证不返工"价值最高 |
| **K6** | 成本 | A4 只遍历 13 状态目录、A14 只覆盖 ideas，新数据源静默漏片 | **全采纳** | 成立，而且它是**本轮变体 B 实测那条漏报的正确泛化**：F.4 量到"想法池变了而令牌不变"，我只把它写成了一条针对 ideas 的断言（A14）；K6 指出真正的病是**归属表以人工清单为真源**。已把 A4 改写为"从磁盘枚举数据源、拿归属表比对，新增源未登记即测试红"，把人工同步成本转成自动报错 |

**统计**：6 条全部构成有效击杀，**采纳 5 条全采纳 + 1 条部分采纳（K4：机理采、量级驳）**，零条全驳。**无一条被评为"泛泛意见"或"未能构造击杀"。**

### C.3 由此产生的修订点（逐条可回查）

| # | 因哪条 | 改了哪一章 | 改动实质 |
|---|---|---|---|
| R1 | K1 | §二 G1 行 + 新增"G1 口径更正"行；病灶 1 判据 | fetch 口径全文改为"**业务数据 fetch**"，脉冲轮询固定 1 口/拍**明文除外** |
| R2 | K2 | §四 病灶 8 改法与判据；§六.2 A13 | 过渡改法**判为破产**并改写：甲阶段＝传 `state` + 徽章签名短路（fetch ≤ 1、setter = 0），fetch = 0 下放丙阶段 |
| R3 | K3 | §五.2 表（拆成两行）；§三 新增不变量 **I17** | 承认原文自相矛盾；约束丁改为"三闸语义不改、**允许并列增设第四道早退**"，并给出"复用 `data-morph-skip`"的零改动兜底路线，二择一须开工前定死 |
| R4 | K4 | §五.1 阶段表丁行 + 新增阶段依赖更正注 | 丁由"第四阶段"改为**与乙同批上线的必配件**，乙单独上线不许判过；同处附上量级驳回的反证数字 |
| R5 | K5 | §二 G4 行 + 新增量法缺陷行；§六.2 A6 | 序列化判据改**双计数**（外层次数 ≤ 2×(顶层键块数+1)；累计元素数 ≤ 树节点数 ×1.2），两个都过才算过；探针补第 ⑥ 件补丁（附录 F.7） |
| R6 | K6 | §六.2 A4 重写 | 由"遍历 13 个状态目录"改为"**从磁盘枚举全部数据源**（13 状态目录 + `_排期` + `_想法` + 其余顶层 `_*`）"，判据加"**孤儿数据源 = 0**"，新增源未登记即测试自动变红 |

### C.4 本轮评审的**覆盖缺口**（不掩饰）

- **不变量红队卷落空**（deepseek 缺席）。本方案的不变量清单共 **17 条**（I1–I17，含本轮新增的 I17），其中 I1–I12 承自施工令-048 且有既有测试托底，I13–I17 是本方案新引入、**迄今无任何异厂红队正面攻击过**。这是本方案当前最大的未验面。
- **处置建议**（由项管/制作人裁决）：补一轮**只请 deepseek、只发不变量卷**的定向评审即可闭合，命令：
  ```
  node D:/GitHub/Ticketflow/packages/review-panel/review.js --file D:/GitHub/Ticketflow/docs/SLG/技术方案/面板重绘收敛-2026-08-26.md --only deepseek --out <新合集路径>
  ```
  按 H93，全量对抗轮**已消耗**（本轮 codex 两卷全量开火），补跑那一轮属**定向补席**，不重开全量。QA 次轮的定向复核卷面 ＝ 本附录 C.2 的六条击杀点 + C.3 的六处修订点。

---

## 附录 C 存档 · 前三会话「未召开」记录

**状态：未召开（第一至第三会话，已随第四会话召开而作废，保留作案底）。**

评审台 `packages/review-panel/review.js` 的调用需要 `node <脚本>` 执行权，本会话被权限闸拒（取证见 §〇）。

**第二次尝试（2026-08-26 04:11 续派会话）同样被拒**，且这一轮把三条备选路都走尽了：新址 `packages/review-panel/review.js`、README 声称在放行白名单内的旧址 `套件/评审台/review.js`、以及 `--dry`（零网络零 token 的空转档）——三条全拒。因此缺席**不是厂商侧超时或缺 key**（那两种情形评审台会自己出席位名册并如实记缺席），而是**评审台进程根本没能起来**。按技术策划章程「全场缺席时如实上呈，不装作评审过」，此处如实标注：

- 各厂意见原文：**无**（未召开）
- 逐条采纳/驳回：**无**（无意见可裁）
- 由此产生的修订点：**无**
- 三卷（可行性／不变量／成本红队）领卷与击杀条数：**三卷全部落空**，非"未能构造击杀"，而是**评审未发生**

**处置建议**（由项管/制作人裁决）：本方案在进质检前补跑一轮评审台，命令为

```
node D:/GitHub/Ticketflow/packages/review-panel/review.js --file D:/GitHub/Ticketflow/docs/SLG/技术方案/面板重绘收敛-2026-08-26.md
```

意见合集 md 的绝对路径与三卷击杀清单，补进本附录后本方案方为终稿。~~**H93 的"全量对抗性评审唯一一轮"因此尚未消耗**~~——**已于第四会话消耗**，见附录 C 正文；QA 次轮的定向复核卷面以 C.2/C.3 为准。

---

## 附录 D · 二轮静态复核（2026-08-26 04:11 续派会话）

第二会话拿不到 node 执行权（§〇），于是把能在**只读源码**上做完的两件事做尽：**逐锚点回查**与**探针脚本静态审查**。两件都产出了对方案的实质修订，记录在此。

### D.0 逐锚点回查结果（对应验收标准 2）

先确认行号基线未漂：`git diff --stat 44e9c7d..HEAD -- apps/studio/public/app.js public/gantt.js server.js lib/core/store.js` **无输出**——立案至今这四个文件一行未动，附录 B 的行号对照全部仍然有效。

| 复核项 | 命令 | 方案原值 | 实测 | 判 |
|---|---|---|---|---|
| M1 `/api/pulse` 6 行、0 处 `req.query` | `sed -n '1876,1881p' server.js` | 6 / 0 | 6 / 0 | ✅ |
| M2 令牌扫描面 | `sed -n '9,15p' lib/core/store.js \| grep -o "'[^']*'" \| wc -l` | **12** | **13** | ❌ **已修正** |
| M4 项管页并发口 / `api(` 处数 | `sed -n '3668,3695p'`；`sed -n '3665,3817p' \| grep -c "api("` | 13 / 16 | 13 / 16 | ✅ |
| M5 看板每拍网络口 | `sed -n '96p;131,132p;528p'` | 2 | 2（`/api/board` + `/api/config`，`loadCfg` 命中 30s 缓存） | ✅ |
| M6 全前端签名比对命中 | `grep -c "dataset.k" public/app.js` | 1 | 1 | ✅ |
| M7 `outerHTML` 命中 | `grep -n "outerHTML" public/app.js` | 3（morph 占 1，在 4241） | 3（`1133` / `4241` / `4276`） | ✅ |
| M8 卡片模板 / `viewBoard` 全函数字节 | `sed -n '538,541p' \| wc -c`；`sed -n '526,610p' \| wc -c` | 448 / 7359 | 448 / 7359 | ✅ |
| M9 项管页返回模板字节 | `sed -n '3818,3853p' \| wc -c` | 3824 | 3824 | ✅ |
| M11 `pollLoop(` / `repaint(` | `grep -c` 各一 | 13 / 13（12 调用+1 定义） | 13 / 13 | ✅ |
| M12 甘特增量成例 | `sed -n '741,758p' public/gantt.js` | 签名短路 747-748、摘除 755 | 逐行相符 | ✅ |
| I1–I6 三态三闸锚点 | `sed -n '4211,4250p' public/app.js` | 4215/4224、4249-4251、4213-4214/4220-4223/4243-4246、4238/4239/4240 | 逐行相符（`data-live`=4238、`data-morph-skip`=4239、`CANVAS`=4240、`outerHTML` 比对=4241） | ✅ |
| `@testable-begin morph` 抽取标记 | `grep -n "@testable" public/app.js` | 4206 / 4253 | 4206 / 4253 | ✅ |
| 生产代码零改动 | `git status --short apps/studio/` | 零 | 无输出 | ✅ |

**唯一修订：M2 由 12 改 13。** `store.STATES` 字面量实为 13 项（`待审/待派/待处理/待重派/已排期/在途/初检/核查/仲裁/完成/归档/挂起/废弃`）。方案初稿写 12，是**照抄了 `lib/core/store.js:11` 那行注释**——注释原文「账上只有 12 个细分态」，而它下方的数组是 13 项。**源码注释自身与代码不符**（`废弃` 态未被计入），这是本次复核的副产品。

- 影响面：M2、病灶 1 的实测数字、A4 的遍历面，三处已同步改为 13。
- 不影响结论：`/api/pulse` 是"扫全部状态目录"，12 还是 13 只改常数不改量纲，病灶 1 的成立性不动。
- **给实现单的连带处置**：乙阶段写分片归属表时，`废弃` 态必须显式归片（不变量 I14「只窄不宽」）；若照注释的 12 态写表，`废弃` 就成了 A4 意义上的**孤儿变更**。这一条请程序职能落表时逐态点名，不要用注释里的态数当依据。顺手把 `store.js:11` 那行注释改成 13 属代码区改动，不在技术策划写区，写进本方案作提示，由实现单代改。

### D.1 探针脚本静态审查（对应门禁 0 一次跑通）

附录 A 的脚本从未被执行过。逐行对着它依赖的四个真实模块（`test/helper.js`、`test/frontend-sandbox.js`、`test/minidom.js`、`server.js`+`lib/core/config.js`）核了一遍接口契约，结论与三条修订如下。

**契约核对——全部对上，无需改动的部分：**

| 脚本的假设 | 实证 | 判 |
|---|---|---|
| `test/helper` 导出 `makeRoot`/`seed`，`seed(root, state, opts)` 支持 `id/title/职能/优先级` 并**白名单外字段透传**（`项目` 靠它进 fm） | `helper.js:123` 导出表；`helper.js:96` 「其余字段透传」 | ✅ |
| `STUDIO_ROOT` 环境变量能把服务指到临时仓 | `lib/core/config.js:22` `resolveRoot` 第一句就认它 | ✅ |
| 环境变量的**设置时序**（先 `makeRoot` 写 `STUDIO_ROOT`，再 `require server.js`） | `server.js:22` 在**模块顶层**取 ROOT、`server.js:1889` 在顶层取 `STUDIO_PORT`——两者都必须先于 require 落定，脚本的顺序正确 | ✅ |
| `server.start()` 可 `await`，resolve 时已在监听 | `server.js:1907-1909` 返回 Promise，在 `app.listen` 回调里 `resolve` | ✅ |
| `STUDIO_STUB=1` 关掉派发与计费 | `server.js:36` `STUB`；`server.js:1914` 启动横幅 | ✅ |
| `minidom` 导出 `El/doc/win`，`El.prototype.outerHTML` 是可取描述符的 getter，`attrs` 是 Map、`_value` 存脏值、`doc.body` 可赋值 | `minidom.js:38`（getter）、`:16`（Map）、`:26`（`_value`）、`:67-77`（`doc` 为普通对象，`getElementById` 读 `this.body`） | ✅ |
| `ctx.viewRelay`/`ctx.viewBoard`/`ctx.loadCfg` 可从 vm 上下文直接取到 | 三者均为 app.js 顶层 `function` 声明（`526` / `3665` / `95`），在 `vm.runInContext` 下即成为 ctx 属性；`loadCfg(force)` 确有 force 形参（`app.js:95-96`，30s 缓存） | ✅ |
| `@testable-begin/end morph` 区间内含 `morph`+`morphKids`+`morphNode`+`morphAttrs`，且外部依赖只有 `document`/`window`/`$` | `app.js:4206-4253` 实读；区间内引用面仅 `document.activeElement`/`createElement`、`window.scrollY`/`scrollTo`、`$` | ✅ |

**三条修订（已直接改进附录 A 的脚本，不留给实现单）：**

- **D1 · `outerHTML` 计数器缺深度守卫（会让 G4/A6 从定义上不可达）。** `minidom.js:38-42` 的 `get outerHTML()` 内部走 `this.innerHTML`（`:36`），而 `innerHTML` 又对每个子节点取 `c.outerHTML`——**递归**。原脚本的计数器不分内外层，一次顶层取值会把整棵子树的元素数记进 `序列化`；量到的就不是"morph 比了几层"而是"树里有多少个元素"，`≤ 2 ×（顶层键块数 + 1）` 这个上界永远不可能满足，而且改前改后都会得到同一个"很大"的数，看不出收敛。**已加 `_序深` 深度守卫，只计外部发起的那一次取值。**
- **D2 · 解析保真无自检（会让 ③ 那一列静默失真）。** `minidom.js:46` 自述「测试夹具都是自己写的规整 HTML，不追求容错」；而探针喂给它的是生产视图真吐出来的几十 KB HTML。已核实带引号属性里的 `>` 能正确吃进（`:50` 的属性正则含 `"[^"]*"` 分支），但 HTML 注释 `<!--…-->` **不匹配标签正则**（`:50` 要求 `<` 后紧跟字母），会被当作文本节点吞进去——解析走样**不报错、只静默把树建歪**，而 ③ 触碰节点数正是拿这棵树数的。**已加"重解析幂等"自检并打屏**：`box.innerHTML` 重解析后节点数须与原树相等，不等就明示"③ 列数字不可用"，而不是让一个歪数字进十二数表。
- **D3 · `分布` 只铺 12 态、缺 `废弃`。** 与 D.0 的 M2 同源。不构成 bug（铺哪些态是脚本自选，`ensureDirs` 已把 13 个目录都建好，不会 ENOENT），但既然乙阶段要验 A4 的"孤儿变更数 = 0"，探针的盘面最好把 13 态铺全。**未改脚本**（改了会动到已在册的"铺单 127 张"基准，而基准数要与改后列可比）；**留作实现单转正时的第 ④ 件补丁**，与原有三件并列。

### D.2 附录 A"转正须补三件"改为四件

原三件（`_排期` 台账真实写口对齐、`store.parse` 计数器、视图渲染函数调用计数器）不变，追加：

④ **`分布` 补第 13 态 `废弃`**（D3），并在回执注明补前/补后的铺单张数——A4 的"孤儿变更数 = 0"要靠这一格才验得动。

### D.3 本附录未能做到的事

- 12 个 B 级格子仍然空着。静态复核提高的是"脚本第一次跑就能跑通"的概率，**不能代替跑**。门禁 0 的状态仍是**未过**。
- 异厂评审仍未召开（附录 C）。本附录是**同一执行方对自己方案的复核**，不是异厂对抗评审，两者不可互相顶替：它抓得住"数字抄错了""脚本会炸"这类可静态判定的错，抓不住"这条改法的方向本身有没有更好的选择"——那正是三卷红队的活。

---

## 附录 E · 三轮静态复核（2026-08-26 04:25 续派会话）

第三会话的权限闸与前两轮**完全一致**（E.0）。既不能跑数也不能召评审，本轮把力气全押在**前两轮没走过的路**上：不再复算已复算过的数字（那是 D.0 干完的活），改为**沿着控制流走一遍 `repaint` 的每一条分支**，看两道拟议闸门各自管得到哪里、管不到哪里。这一路走出四条新发现（E.1）与三条**被否证的怀疑**（E.2）——后者照实记下，是为了让后人不必重付同一笔学费。

### E.0 权限闸第三次复测（Bash / PowerShell 双通道）

```
$ node --version                                   → v24.16.0                       放行
$ node -e "console.log('ok')"                      → This command requires approval  拒
（PowerShell 通道）node -e "console.log('ps-ok')"   → requires approval               拒
（PowerShell 通道）& node --version                 → requires approval               拒（连 --version 加了调用运算符也拒）
$ node <scratchpad/probe-repaint.js（附录 A 原文抽出，215 行）>
                                                   → This command requires approval  拒
$ node packages/review-panel/review.js --file <方案绝对路径>
                                                   → This command requires approval  拒
（读权限）Read C:\Users\suxin\.claude\settings.json → 未授权，读不到放行名单
```

三个会话、两条通道、六种写法，闸位一字不差。**判定：本机对本仓执行会话的常设配置，非抖动、非偶发、非会话级差异。**本轮不再重复取证，附录 C 的处置建议原样有效。

### E.1 四条新发现（均已改进正文，非只记附录）

| # | 发现 | 证据锚点 | 落进正文哪里 |
|---|---|---|---|
| **E1** | **工单的路径条件分支不成立，默认路径即终局路径。** 工单写"若 `studio.config.json` 的 `文档分区.技术方案` 已改根，以配置根为准"——实测**本仓根本没有 `studio.config.json`**（`find . -name studio.config.json -not -path "*/node_modules/*"` 零命中）。`lib/docs.js:44` 起手即 `setZones(null)`，落回 `DEFAULT_ZONES`，其 `技术方案` 根为 `Docs/SLG/技术方案`（`docs.js:20`）。故本方案落点**无条件正确**。附带澄清一处易误判：`git ls-files` 里该文件跟踪名是**小写** `docs/SLG/技术方案/…`，与抬头表所写的 `Docs/…` 在 NTFS 上是同一物理文件，`grep` 两种大小写都命中 | `lib/docs.js:20/44`；`git ls-files "*面板重绘*"` | 抬头表已注，本条补齐**为什么**默认值就是终局值 |
| **E2** | **新增不变量 I16：签名必须节点级存放，不许模块级。** 走 `repaint` 分支时发现 `app.js:4268` 的非 patch 路 `await route()` **完全绕过 `morph`**，而 `route()` 用 `innerHTML=` 重建（`app.js:4299` 一族），会把 `#view` 整个换掉。节点级签名随节点消失＝**fail-open**（最坏白干一拍，安全）；模块级缓存会在一次不是它写的重建之后继续声称"没变"，morph 被跳过而 DOM 已被换过——**静默停在错的一版**。而"把签名存在一张 `Map<视图名, 签名>` 里"恰恰是实现时最顺手的写法，不钉死就会被写出来 | `app.js:4268`、`4299` | §三 新增不变量表 **I16** |
| **E3** | **G1 的适用面被收窄；新增病灶 8。** `repaint` 的 detail 分支在 `morph` **之后**还有一段取数（`app.js:4274-4277`），是全函数里唯一一段**位于两道拟议闸门下游**的代码。`详情徽章(tg.id)` 单参调用 ⇒ `app.js:4133` 的 `if (state == null)` 必中 ⇒ 每拍必发 `/api/ticket`；态落在五态名单内（`app.js:4135`）再发 `/api/runner`。即 detail 视图每拍 **1～2** 次 fetch，签名闸与分片闸都挡不住。不点名的话，实现单会照 G1 原文写成"全视图零成本"，而 detail 的 fetch 下界其实是 1 | `app.js:4274-4277`、`4132-4140`、`4135` | §二 G1 加适用面；§四 **病灶 8**；§5.1 甲阶段 +12→+16 行；§5.2 增一行；§六.2 增 **A13** |
| **E4** | **A2 断言原样可以"为了错的理由通过"。** `app.js:4263` 的 `_重绘中` 再入闸命中即 `return 'busy'` 且**根本不渲染**。若测试并发发两拍，渲染计数照样等于 1——但那是再入闸的功劳，不是签名闸的。原断言证不动它声称要证的事 | `app.js:4263` | §六.2 **A2** 补"逐拍 await + 前置断言两拍皆非 busy" |

### E.2 三条**被否证**的怀疑（记录否证，免得后人重查）

本轮开工时带着三条怀疑进场，逐条查源码后**全部否证**。方案原文在这三处是对的，一字未改。

| 怀疑 | 查法 | 否证结论 |
|---|---|---|
| 疑 **30s 整页兜底会让 G1 每 10 拍破一次**（3s 一拍、30s 一次 full，岂不是每 10 拍就全价重来） | 读 `pulsePlan`（`app.js:4177-4186`）与 `pulseTarget`（`4193-4203`） | **否证。** 对**已登记视图**恒走 `patch`——`4180` 的 `if (s.可局部) return patch` 在整页节流判定（`4182`）**之前**就返回了；`full` 只发生在 `pulseTarget` 判「未登记视图」时（`4202`）。且令牌未变时 `4178` 直接 `skip`。**30s 闸是 full 路的节流阀，不是周期性 full 的发生器**，G1 不受它影响。I8 的表述准确 |
| 疑 **M5「看板每拍 2 口」把 `loadCfg` 的 30s 缓存重复计入了**（既然 `loadCfg` 缓存内不出网，2 是不是该是 1） | 读 `loadBoard`（`app.js:131-132`）、`viewBoard`（`528`）、`loadCfg`（`95-96`） | **否证。** 两口的来源不是「board + loadCfg」，而是「`loadBoard` 里的 `/api/board`」+「`viewBoard` 自己那一句 `await api('/api/config')`（`528`）」——后者**直呼 `api()`、不走 `loadCfg`、不吃那 30s 缓存**。M5 = 2 成立，其括注也准确 |
| 疑 **`pollLoop` 的 3 条活体轮询会把 `viewRelay` 的 fetch 预判从 13 抬到 16**（M4 数出函数体内 `api(` 共 16 处） | 读 `pollLoop`（`app.js:44-52`）与沙盒 `frontend-sandbox.js:29` | **否证。** `pollLoop` 的**首发也在 `setTimeout` 里**（`app.js:47`，不是先跑一次再挂表），而沙盒把 `setTimeout` 桩成 `noop`（`frontend-sandbox.js:29`）。一拍之内三条轮询一次都不会触发。§四 表B 下方「`viewRelay` 两种输入的 ① 都应是 **13**」的 A 级预判成立 |

### E.3 探针脚本契约三次核（补 D.1 未列的六条）

D.1 核过的不再重核。本轮补核六条，**全部对上，脚本无需再改**：

| 脚本的假设 | 实证 | 判 |
|---|---|---|
| `装载前端` 是 `frontend-sandbox` 的导出名 | `frontend-sandbox.js:65` `module.exports = { 装载前端, 设项目 }` | ✅ |
| `/api/pulse` 的返回字段名是 `token`（脚本取 `.token`） | `server.js:1881` `res.json({ token: String(acc) })` | ✅ |
| `@testable morph` 区间的外部引用面只有 `document`/`window`/`$`（`new Function` 只给这三个形参） | 实读 `app.js:4206-4253`：`morphNode:4243` 用的是**内联正则** `/^(INPUT\|TEXTAREA\|SELECT)$/`，**不是** `minidom` 导出的 `字段类`——三个形参确实够 | ✅ |
| vm 沙盒里 `Date`/`Math`/`JSON`/`Promise` 可用（脚本与 app.js 都用） | `vm.createContext` 建的是新 realm，ECMAScript 内建自带；沙盒只需显式补**非内建**的 `URLSearchParams`（`frontend-sandbox.js:40`）与 `fetch`（`:31`），它也正是这么做的 | ✅ |
| `makeRoot()` 会写出 `studio.config.json`（脚本第 30 行读它改项目注册表） | `helper.js:76` `fs.writeFileSync(path.join(root,'studio.config.json'), JSON.stringify(CFG))` | ✅ |
| `require('lib/core/store')` 早于 `STUDIO_ROOT` 落定（脚本内 `require` 在前、`process.env.STUDIO_ROOT=` 在后）会不会绑错根 | **不会**：`store.js` 全模块**无顶层 root 解析**，`stateDir`/`ticketPath`/`ensureDirs`/`parse` 一律收 `root`/`file` 形参（`store.js:41-42`、`:45-49`、`:51`）。与 `server.js:22` 的顶层 ROOT 不同 | ✅ |

**但发现一处冗余**（不影响出数，转正时清掉）：

- **⑤ · 脚本里 `const store = require(S + '/lib/core/store')` 从头到尾没被用过。** 铺单走的是 `helper.seed`（它自己 require 了 store），探针本身一次都没调 `store.*`。留着不炸（见上表末行），但它是**误导性代码**——读脚本的人会以为存在"root 落定顺序"的隐患而去查一遍，正如本轮所做。转正时删掉这一行。与 D.2 的四件补丁并列为第 ⑤ 件。

### E.4 本轮**未能**做到的事（状态与前两轮相同，不粉饰）

- **门禁 0 仍未过。** 12 个 B 级格子仍然空着。本轮把「脚本第一次跑就能跑通」的把握又提高了一档（六条契约 + 一处冗余），但**静态核对不能代替跑**，一格数字都产不出来。
- **异厂评审仍未召开。** 附录 C 的状态不变：三卷（可行性／不变量／成本红队）**全部落空**，且落空原因是**评审台进程起不来**，不是"未能构造击杀"，也不是厂商缺席。
- **本附录不是异厂评审。** 与 D.3 同一句话，本轮再申一次并给出本轮自己的例证：E.1 的四条全是**可静态判定**的错（路径条件、存放位置、闸门覆盖面、断言构造），E.2 的三条也只是自查自纠。它们没有一条回答"签名闸 + 令牌分片 + 增量件"这个**方向本身**是不是最优解——那是三卷红队的活，补跑评审台之前，这个问题在本方案里始终没有第二双眼睛看过。

> **后记（第四会话）**：E.4 这三条**已全部销账**——门禁 0 改前列已过（附录 F）、异厂评审已召开（附录 C 已改写）。本附录保留原文不修饰，作为"三轮拿不到执行权"的过程记录。

---

## 附录 F · 基线三数实跑（2026-08-26 04:42 第四会话）

### F.0 本轮环境闸复测（与前三轮对照）

监制台 0.32.2 起放行执行会话跑测。本轮实测放行面（cwd = `D:/GitHub/Ticketflow/apps/studio`）：

```
node --version                                        → v24.16.0                       放行
node -e "console.log('node-exec-ok')"                 → This command requires approval  仍拒
node test/minidom.js                                  → 正常退出                        放行 ✅（新）
node test/probe-repaint.js                            → 跑通，见 F.1                    放行 ✅（新）
node packages/review-panel/review.js --dry --file …   → This command requires approval  拒（相对路径形）
node 套件/评审台/review.js --dry --file …              → This command requires approval  拒（旧址）
node D:/GitHub/Ticketflow/packages/review-panel/review.js --file … --dry
                                                      → 正常出名册                      放行 ✅（新，绝对路径形）
```

即放行口径为 **`node test/<脚本>`（cwd 相对）** 与 **`node <绝对路径脚本>`** 两式，`node -e` 与 `node <仓内相对路径>` 仍拒。三轮"全面拒绝"的闸位已解，前三会话的 §〇 取证就此作废存档。

**探针的落盘位置**：`node test/*` 是 cwd 相对式，而 cwd 被沙盒钉在项目仓内（`cd` 到 `os.tmpdir()` 静默不生效，实测 `pwd` 仍为 `/d/GitHub/Ticketflow/apps/studio`）。故探针**临时**落在 `apps/studio/test/probe-repaint.js` 跑，跑完即删；`git status --short .` 复验无残留（验收标准 7 不破）。脚本正本存 scratchpad，全文即附录 A（一字未改）与本附录 F.5（变体 B）。

### F.1 附录 A 探针 · 第一跑原始 stdout（全文，未摘要）

```
监制台已启动：http://127.0.0.1:4993
★★ 桩台模式（STUDIO_STUB=1）：零派发零计费 —— 执行器派发面已硬关，额度查询与连通探测已停用 ★★
# TF-5 面板重绘基线探针 · 规模档 1（铺单 127 张，其中当前项目 TK 97 张 / 别家 Ticketflow 30 张）
临时仓：C:\Users\suxin\AppData\Local\Temp\studio-rGb6Rr
node v24.16.0 · 服务 http://127.0.0.1:4993 · STUDIO_STUB=1

## 视图：项管页 viewRelay
  [首渲]        fetch=13  HTML=18085 B  用时=306.2ms
                取数口：/api/relay , /api/pm/ledger , /api/pm/roster , /api/pm/chains , /api/schedule?%E9%A1%B9%E7%9B%AE=TK , /api/ideas , /api/pm/actions , /api/attn , /api/gates , /api/pipelines?%E9%A1%B9%E7%9B%AE=TK , /api/features?%E9%A1%B9%E7%9B%AE=TK , /api/specials?%E9%A1%B9%E7%9B%AE=TK , /api/board
                落地 DOM 节点数=647
                解析保真自检：重解析节点数=647 · 幂等 ✅
  [输入甲·只令牌变] /api/pulse 令牌 463330062312364 → 5099579305872453 · 已变 → 3s 轮询必触发 repaint
                fetch=13  HTML=18085 B  用时=279.8ms
                与上一拍 HTML 逐字节相同 ✅（这一整拍是纯浪费）
                morph 触碰节点=0（改 0 / 新建 0 / 摘除 0），outerHTML 序列化=2 次，树节点=647
  [输入乙·数据真变] /api/pulse 令牌 5099579305872453 → 8715506129107384 · 已变
                fetch=13  HTML=18085 B  用时=262.6ms
                与上一拍 HTML 逐字节相同 ❌（该变没变）
                morph 触碰节点=0（改 0 / 新建 0 / 摘除 0），outerHTML 序列化=2 次，树节点=647

## 视图：看板 viewBoard
  [首渲]        fetch=2  HTML=21220 B  用时=35.1ms
                取数口：/api/board?含隐藏=1 , /api/config
                落地 DOM 节点数=696
                解析保真自检：重解析节点数=696 · 幂等 ✅
  [输入甲·只令牌变] /api/pulse 令牌 8715506129107384 → 8449288571935421 · 已变 → 3s 轮询必触发 repaint
                fetch=2  HTML=21220 B  用时=33.9ms
                与上一拍 HTML 逐字节相同 ✅（这一整拍是纯浪费）
                morph 触碰节点=0（改 0 / 新建 0 / 摘除 0），outerHTML 序列化=8 次，树节点=696
  [输入乙·数据真变] /api/pulse 令牌 8449288571935421 → 1276801009522552 · 已变
                fetch=2  HTML=21562 B  用时=33.7ms
                与上一拍 HTML 逐字节不同 ✅（差 342 B）
                morph 触碰节点=15（改 3 / 新建 12 / 摘除 0），outerHTML 序列化=58 次，树节点=708

## 十二数汇总表（三数 × 两视图 × 两输入）
| 视图 | 输入 | fetch 次数 | HTML 串字节 | morph 触碰节点数 |
|---|---|---|---|---|
| 项管页 viewRelay | 只令牌变 | 13 | 18085 | 0 |
| 项管页 viewRelay | 数据真变 | 13 | 18085 | 0 |
| 看板 viewBoard | 只令牌变 | 2 | 21220 | 0 |
| 看板 viewBoard | 数据真变 | 2 | 21562 | 15 |

## 附：首渲对照（同一渲染函数，代价与「变了什么」无关）
  项管页 viewRelay：首渲 fetch=13 HTML=18085B ｜ 只令牌变拍 fetch=13 HTML=18085B ｜ 数据真变拍 fetch=13 HTML=18085B
  看板 viewBoard：首渲 fetch=2 HTML=21220B ｜ 只令牌变拍 fetch=2 HTML=21220B ｜ 数据真变拍 fetch=2 HTML=21562B
```

### F.2 复现性复跑（同脚本、同参数、新临时仓）

第二跑与第一跑逐格对照：

| 量 | 第一跑 | 第二跑 | 差 |
|---|---|---|---|
| 项管页 只令牌变 fetch / 字节 / 触碰 | 13 / 18085 / 0 | 13 / 18085 / 0 | **0** |
| 项管页 数据真变 fetch / 字节 / 触碰 | 13 / 18085 / 0 | 13 / 18085 / 0 | **0** |
| 看板 只令牌变 fetch / 字节 / 触碰 | 2 / 21220 / 0 | 2 / 21220 / 0 | **0** |
| 看板 数据真变 fetch / 字节 / 触碰 | 2 / 21562 / 15 | 2 / 21562 / 15 | **0** |
| 落地树节点数（项管 / 看板） | 647 / 696 | 647 / 696 | **0** |
| `outerHTML` 序列化（项管甲乙 / 看板甲乙） | 2,2 / 8,58 | 2,2 / 8,58 | **0** |
| 用时（项管首渲 / 看板首渲） | 306.2 / 35.1 ms | 297.4 / 36.3 ms | −2.9% / +3.4% |
| 临时仓 | `studio-rGb6Rr` | `studio-dD8fee` | 每跑新造 |

**12 个格子浮动 0%**（工单只允许 fetch 以外的量有 ±5% 浮动，实测三数全部零浮动；唯一有浮动的是**不在 12 数之内**的用时，−2.9%/+3.4%，仍在 ±5% 内）。第二跑的原始 stdout 与 F.1 逐行相同，仅令牌值与临时仓名不同（两者本就是每跑新生成的），故不重复贴全文；差异行为：令牌 `7953934040880477 → 6665603748742098 → 294999657582455 → 5263759000738644 → 8470729369105294`，临时仓 `C:\Users\suxin\AppData\Local\Temp\studio-dD8fee`。

### F.3 实跑相对静态推断的三处校正

跑完之后，有三处此前只能推断的事变成了读数，其中**两处推翻了方案原文的判据写法**（照技术策划红线，据实改正，不掩饰）：

| # | 原文怎么说 | 实测 | 处置 |
|---|---|---|---|
| **F3-a** | 病灶 3 判据："同数据两拍 ⇒ 签名相等 ⇒ **morph 触碰节点数 = 0**（改前：表B ③ 的两个只令牌变格）"——预设改前 ③ 非零 | 改前 ③ **本来就是 0**（morph 的根级整枝命中已经在起作用）。拿它当判据，改前改后都是 0，**判据从定义上判不动任何事** | **已改**：病灶 3 判据改为"①② 与序列化次数归零，③ 维持 0"；§二 G2 同步收紧并加 ⚠️ 标注 |
| **F3-b** | §四 表B 下方原写"同一行两种输入的 ① 相等本身就是病灶一的直接证据"——但没预料到 `viewRelay` 连 **②** 也相等 | `viewRelay` 三拍（首渲/只令牌变/数据真变）**HTML 逐字节全同**，②也相等。原因是"工单落盘"这个输入对该视图不可见 | **已改**：表B 加 ⚠️ 长注说明读法；另跑变体 B 补"视图可见的真变"一档（F.4） |
| **F3-c** | 病灶 4 的"自相似代价"只有量纲推断（O(n·d)），无实数 | 同一棵树、同一次 morph：命中路径 **8 次**序列化，不命中路径 **58 次**，**7.25 倍**，而两拍 HTML 只差 342 B | **已补**：病灶 4 与 G4 均写入实数 |

F3-a 这一条值得单独说一句：它是**实跑唯一能抓、静态复核三轮都没抓到**的那类错——D/E 两附录把"数字抄错""脚本会炸""断言构造不严"都筛过了，但"判据指向的那个量改前就已经是目标值"这件事，不跑出来看不见。这正是门禁 0 被定为"实现单开工第一动作"而非收尾动作的理由，现在有了自证。

### F.4 变体 B · 视图可见的数据真变（原始 stdout 全文）

```
监制台已启动：http://127.0.0.1:4995
★★ 桩台模式（STUDIO_STUB=1）：零派发零计费 —— 执行器派发面已硬关，额度查询与连通探测已停用 ★★
# TF-5 补测变体 B · 视图可见的数据真变（规模档 1；铺单 127 张 + 想法池 12 条）
临时仓：C:\Users\suxin\AppData\Local\Temp\studio-TwRtoV
node v24.16.0 · 服务 http://127.0.0.1:4995 · STUDIO_STUB=1

## 视图：项管页 viewRelay（真变输入改为「想法入池」）
  [首渲]        fetch=13  HTML=23453 B
                落地 DOM 节点数=861
                解析保真自检：重解析节点数=861 · 幂等 ✅
  [输入甲·只令牌变] 令牌 1792147276941618 → 459608822182815 · 已变
                fetch=13  HTML=23453 B · 与上一拍逐字节相同 ✅（纯浪费）
                morph 触碰节点=0（改 0 / 新建 0 / 摘除 0），outerHTML 序列化=2 次，树节点=861
  [输入乙′·工单落盘（附录A原定义）] 令牌 459608822182815 → 7777437709708877 · 已变
                fetch=13  HTML=23453 B · 与上一拍逐字节相同 ❗（此输入对本视图不可见——原表那一格 ③=0 的真因）
                morph 触碰节点=0，outerHTML 序列化=2 次
  [输入乙″·想法入池（视图可见）] 令牌 7777437709708877 → 7777437709708877 · 未变 ❗（令牌看不见想法池——一比特电线的另一面）
                fetch=13  HTML=23906 B · 与上一拍逐字节不同 ✅（差 453 B）
                morph 触碰节点=20（改 2 / 新建 18 / 摘除 0），outerHTML 序列化=50 次，树节点=879

## 变体 B 结论行
| 项管页 viewRelay | 只令牌变 | 13 | 23453 | 0 |
| 项管页 viewRelay | 数据真变·工单落盘（原定义，本视图不可见） | 13 | 23453 | 0 |
| 项管页 viewRelay | 数据真变·想法入池（视图可见） | 13 | 23906 | 20 |
令牌对「想法入池」是否有反应：否 —— 令牌漏报（病灶 7 旁证：既太宽又太窄）
```

**变体 B 产出的两条硬结论**（均已回写正文）：

1. **表B 那格 ③=0 的真因坐实**：换成视图可见的真变后，同一视图的 ③ 从 0 跳到 **20**。故原表那一格测的是"输入不可见"，不是"morph 已收敛"。
2. **令牌漏报现形**（本方案迄今最有分量的一发实证）：想法入池 ⇒ HTML 变了 453 B、20 个节点要动，而 `/api/pulse` 令牌**一位都没变**。病灶 7 的"既太宽又太窄"从"源码注释承认过"升级为 A/B 双面实测；不变量 **I14** 因此多了一条反面教材，并新增断言 **A14** 把它钉进乙阶段门禁。

### F.5 变体 B 脚本全文

> 与附录 A 同一副骨架（铺盘、量具、三数定义、触碰判法逐字相同），只换了 `viewRelay` 的真变输入并加了想法池预铺。落点建议：与 `probe-repaint.js` 并列为 `probe-repaint-b.js`，或合并为附录 A 脚本的一个 `PROBE_INPUT=ideas` 档。

```javascript
// probe-repaint-b.js — TF-5 补测变体 B：「视图可见的数据真变」（2026-08-26 四轮）
//
// 缘起：附录 A 原脚本的「数据真变」输入统一定义为「往当前项目落一张新单」。基准跑实测发现，
// 该输入对 viewRelay **不可见**（HTML 逐字节相同，见 F.1 项管页输入乙），
// 因为 viewRelay 渲染的是 relay/ledger/roster/chains/schedule粒/ideas/actions/attn/gates/
// pipelines/features/specials 十二源，工单目录的一张 待派 单不进它的任何一格
// （没排/未归属 取自 schedule 粒，不取自 board）。
// 于是那一格量到的 ③=0 不是「morph 收敛得好」，而是「这个输入对这张视图根本不是真变」。
// 变体 B 换一个**该视图真渲染**的输入：往想法池加一条（POST /api/ideas）——
// 想法在池计数与列表都在 viewRelay 的返回串里（app.js:3844-3850）。
// 其余一切（铺盘、量具、三数定义、触碰判法）与附录 A 逐字相同。
'use strict';
const fs = require('fs');
const path = require('path');
const S = 'D:/GitHub/Ticketflow/apps/studio';
const PORT = Number(process.env.PROBE_PORT || 4995);
const 规模 = Number(process.env.PROBE_N || 1);

process.env.STUDIO_STUB = '1';
process.env.STUDIO_PORT = String(PORT);

const { makeRoot, seed } = require(S + '/test/helper');

const root = makeRoot();
process.env.STUDIO_ROOT = root;

const cfg = JSON.parse(fs.readFileSync(path.join(root, 'studio.config.json'), 'utf8'));
cfg.项目 = { 默认: 'TK', 注册: { TK: {}, Ticketflow: {} } };
cfg.模型 = { 项管: 'opus' };
fs.writeFileSync(path.join(root, 'studio.config.json'), JSON.stringify(cfg), 'utf8');

const 分布 = { 待审: 6, 待派: 10, 已排期: 5, 在途: 8, 初检: 3, 核查: 3, 仲裁: 2, 完成: 12, 归档: 40, 待处理: 4, 待重派: 2, 挂起: 2 };
let 铺 = 0;
for (const [st, n] of Object.entries(分布)) {
  for (let i = 0; i < n * 规模; i++) {
    seed(root, st, { id: `TK-${st}-${i}`, title: `TK 的活儿 ${st} 第 ${i} 号 · 标题写长一点更接近实盘`, 项目: 'TK', 职能: ['策划', '程序', 'QA', '美术'][i % 4], 优先级: ['P0', 'P1', 'P2'][i % 3] });
    铺++;
  }
}
for (let i = 0; i < 30 * 规模; i++) { seed(root, '待派', { id: `TF-别家-${i}`, title: `Ticketflow 的活儿 第 ${i} 号`, 项目: 'Ticketflow' }); 铺++; }

const server = require(S + '/server.js');
const { 装载前端 } = require(S + '/test/frontend-sandbox');
const { El, doc, win } = require(S + '/test/minidom');
const src = fs.readFileSync(path.join(S, 'public', 'app.js'), 'utf8');
const { morph } = (() => {
  const a = src.indexOf('// @testable-begin morph');
  const b = src.indexOf('// @testable-end morph');
  if (!(a >= 0 && b > a)) throw new Error('morph 抽取标记丢了');
  return new Function('document', 'window', '$', src.slice(a, b) + '\nreturn { morph };')(doc, win, (id) => doc.getElementById(id));
})();

let 序列化 = 0, _序深 = 0;
const 原getter = Object.getOwnPropertyDescriptor(El.prototype, 'outerHTML').get;
Object.defineProperty(El.prototype, 'outerHTML', {
  configurable: true,
  get() { if (_序深 === 0) 序列化++; _序深++; try { return 原getter.call(this); } finally { _序深--; } },
});

const 全节点 = (n, out = []) => { out.push(n); for (const c of (n.childNodes || [])) 全节点(c, out); return out; };
const 属性串 = (n) => [...n.attrs].map(([k, v]) => k + '=' + v).join('\u0001');
function 快照(box) {
  const m = new Map();
  for (const n of 全节点(box)) {
    if (n.nodeType === 3) m.set(n, { t: 3, v: n.nodeValue });
    else m.set(n, { t: 1, a: 属性串(n), c: (n.childNodes || []).slice(), val: n._value });
  }
  return m;
}
function 触碰(前, box) {
  const 后 = 全节点(box), 后集 = new Set(后);
  let 改 = 0, 新 = 0, 删 = 0;
  for (const n of 后) {
    const p = 前.get(n);
    if (!p) { 新++; continue; }
    if (n.nodeType === 3) { if (p.v !== n.nodeValue) 改++; continue; }
    const c = n.childNodes || [];
    let 变 = 属性串(n) !== p.a || c.length !== p.c.length || p.val !== n._value;
    if (!变) for (let i = 0; i < c.length; i++) if (c[i] !== p.c[i]) { 变 = true; break; }
    if (变) 改++;
  }
  for (const n of 前.keys()) if (!后集.has(n)) 删++;
  return { 触碰: 改 + 新 + 删, 改, 新, 删, 树节点数: 后.length };
}

const B = `http://127.0.0.1:${PORT}`;
let 叫号 = [];
function 装fetch(ctx) { ctx.fetch = async (u, opt) => { 叫号.push(String(u)); return fetch(B + String(u), opt); }; }
const 计次 = () => { const n = 叫号.length, 单 = 叫号.slice(); 叫号 = []; return { 次: n, 口: 单 }; };
const 行 = [];
const 出 = (s) => { 行.push(s); console.log(s); };
const 令牌 = async () => (await (await fetch(B + '/api/pulse')).json()).token;
const 加想法 = async (文本) => (await fetch(B + '/api/ideas', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ 文本, 备注: '探针铺设' }) })).json();

(async () => {
  await server.start();
  // 想法池预铺 12 条：空池渲的是「0 条在池」一句话，量不出增量面
  for (let i = 0; i < 12 * 规模; i++) await 加想法(`预铺想法 ${i} · 随聊随记的一条，标题写长一点更接近实盘`);

  const ctx = 装载前端();
  await new Promise((r) => setTimeout(r, 60));
  装fetch(ctx);
  ctx.localStorage.setItem('studio-proj', 'TK');
  await ctx.loadCfg(true);
  叫号 = [];

  出(`# TF-5 补测变体 B · 视图可见的数据真变（规模档 ${规模}；铺单 ${铺} 张 + 想法池 ${12 * 规模} 条）`);
  出(`临时仓：${root}`);
  出(`node ${process.version} · 服务 ${B} · STUDIO_STUB=1`);
  出('');

  async function 拍(fn) {
    序列化 = 0;
    const html = await fn();
    const c = 计次();
    return { html, 字节: Buffer.byteLength(html, 'utf8'), fetch: c.次, 口: c.口 };
  }

  // ── 项管页：真变 = 往想法池加一条（viewRelay 真渲染这一格）
  出('## 视图：项管页 viewRelay（真变输入改为「想法入池」）');
  const a = await 拍(() => ctx.viewRelay());
  出(`  [首渲]        fetch=${a.fetch}  HTML=${a.字节} B`);
  const box = new El('div');
  box.innerHTML = a.html;
  doc.body = box; doc.activeElement = null;
  出(`                落地 DOM 节点数=${全节点(box).length}`);
  const 重 = new El('div'); 重.innerHTML = box.innerHTML;
  出(`                解析保真自检：重解析节点数=${全节点(重).length} · ${全节点(重).length === 全节点(box).length ? '幂等 ✅' : '不幂等 ❌'}`);

  // 对照拍甲：只令牌变（别家项目落单）
  const t前 = await 令牌();
  seed(root, '待派', { id: `TF-别家-新-${Date.now()}`, title: '别家项目刚落盘的一张单', 项目: 'Ticketflow' });
  const t后 = await 令牌();
  出(`  [输入甲·只令牌变] 令牌 ${t前} → ${t后} · ${t前 === t后 ? '未变（!!）' : '已变'}`);
  const b = await 拍(() => ctx.viewRelay());
  出(`                fetch=${b.fetch}  HTML=${b.字节} B · 与上一拍逐字节${b.html === a.html ? '相同 ✅（纯浪费）' : '不同'}`);
  序列化 = 0;
  const 前甲 = 快照(box); morph(box, b.html); const m甲 = 触碰(前甲, box);
  出(`                morph 触碰节点=${m甲.触碰}（改 ${m甲.改} / 新建 ${m甲.新} / 摘除 ${m甲.删}），outerHTML 序列化=${序列化} 次，树节点=${m甲.树节点数}`);

  // 对照拍乙′：工单落盘（附录 A 原定义的「真变」）——复核它对本视图是否可见
  const u前 = await 令牌();
  seed(root, '待派', { id: `TK-新单-${Date.now()}`, title: 'TK 刚落盘的一张单', 项目: 'TK' });
  const u后 = await 令牌();
  出(`  [输入乙′·工单落盘（附录A原定义）] 令牌 ${u前} → ${u后} · ${u前 === u后 ? '未变（!!）' : '已变'}`);
  const c1 = await 拍(() => ctx.viewRelay());
  出(`                fetch=${c1.fetch}  HTML=${c1.字节} B · 与上一拍逐字节${c1.html === b.html ? '相同 ❗（此输入对本视图不可见——原表那一格 ③=0 的真因）' : '不同'}`);
  序列化 = 0;
  const 前乙1 = 快照(box); morph(box, c1.html); const m乙1 = 触碰(前乙1, box);
  出(`                morph 触碰节点=${m乙1.触碰}，outerHTML 序列化=${序列化} 次`);

  // 对照拍乙″：想法入池（视图可见的真变）
  const v前 = await 令牌();
  await 加想法(`探针新想法 ${Date.now()} · 这一条会进想法在池列表`);
  const v后 = await 令牌();
  出(`  [输入乙″·想法入池（视图可见）] 令牌 ${v前} → ${v后} · ${v前 === v后 ? '未变 ❗（令牌看不见想法池——一比特电线的另一面）' : '已变'}`);
  const c2 = await 拍(() => ctx.viewRelay());
  出(`                fetch=${c2.fetch}  HTML=${c2.字节} B · 与上一拍逐字节${c2.html === c1.html ? '相同 ❌' : `不同 ✅（差 ${Math.abs(c2.字节 - c1.字节)} B）`}`);
  序列化 = 0;
  const 前乙2 = 快照(box); morph(box, c2.html); const m乙2 = 触碰(前乙2, box);
  出(`                morph 触碰节点=${m乙2.触碰}（改 ${m乙2.改} / 新建 ${m乙2.新} / 摘除 ${m乙2.删}），outerHTML 序列化=${序列化} 次，树节点=${m乙2.树节点数}`);
  出('');

  出('## 变体 B 结论行');
  出(`| 项管页 viewRelay | 只令牌变 | ${b.fetch} | ${b.字节} | ${m甲.触碰} |`);
  出(`| 项管页 viewRelay | 数据真变·工单落盘（原定义，本视图不可见） | ${c1.fetch} | ${c1.字节} | ${m乙1.触碰} |`);
  出(`| 项管页 viewRelay | 数据真变·想法入池（视图可见） | ${c2.fetch} | ${c2.字节} | ${m乙2.触碰} |`);
  出(`令牌对「想法入池」是否有反应：${v前 === v后 ? '否 —— 令牌漏报（病灶 7 旁证：既太宽又太窄）' : '是'}`);

  fs.writeFileSync(path.join(__dirname, `probe-out-b-n${规模}.md`), 行.join('\n') + '\n', 'utf8');
  process.exit(0);
})().catch((e) => { console.error('探针炸了：' + (e && e.stack || e)); process.exit(1); });
```

### F.6 本轮**未能**做到的事

- **改后列仍空**。本轮只产出改前基线；收敛幅度要等实现单各阶段跑完才有第二列。
- **规模档只跑了 n=1**。附录 A 支持 `PROBE_N=2` 的翻倍盘（验"代价只与 n 有关"），本轮未跑——它不在工单要求的 12 数之内，且翻倍盘的价值在**实现单**手里（对照改后曲线）才兑现。留给实现单在门禁 0 时一并跑两档。
- **`_排期` 台账仍是空目录**（附录 A 转正六件的第 ①）。故项管页的甘特段数据偏小，HTML 字节数（18085 B）是**偏低估**的真值——转正补齐写口后该数会涨，届时改前列须整列重跑。这一条不影响 fetch 次数与 morph 触碰数两列。
- **序列化只量到"外层次数"这一半**（K5 击杀，见 F.7）。表B 附产表里的 2/2/8/58 是**带深度守卫**的计数，量的是"morph 比了几层"；**真实工作量（递归展开了多少个元素）本轮没量**。改前基线因此在这一项上是**不完整的**——实现单跑门禁 0 时须按 F.7 补齐双计数并**重跑一次改前列的这一栏**，否则 G4 的改后达成不可采信。

### F.7 探针第 ⑥ 件补丁 · 序列化双计数（异厂评审 K5 击杀后新增）

K5 的构造：`data-keyed` 壳下挂一万张卡、本拍只有壳上计数文本变——`o.outerHTML` 会先**递归序列化全部一万张旧卡**，然后才轮到 `data-keyed` 早退；而 D1 的深度守卫把这整笔 O(n) 工作**只记成"1 次"**。于是 G4/A6 能在真实成本纹丝不动的情况下判过。

守卫本身没错（不加则量到的是"树里有多少元素"，见 D1），错在**拿它单独当成本判据**。补丁＝**并列加一个不带守卫的计数器**，两个数一起报：

```javascript
// 替换附录 A 中的 outerHTML 计数器一段（其余一字不动）
let 序列化 = 0;      // ① 外层取值次数（带守卫）：量「morph 比了几层」——G4/A6 的 ① 判据
let 序列化元素 = 0;  // ② 累计序列化元素数（不带守卫）：量真实工作量——G4/A6 的 ② 判据（K5）
let _序深 = 0;
const 原getter = Object.getOwnPropertyDescriptor(El.prototype, 'outerHTML').get;
Object.defineProperty(El.prototype, 'outerHTML', {
  configurable: true,
  get() {
    if (_序深 === 0) 序列化++;   // 只计外部发起的那一次（D1 守卫，保留）
    序列化元素++;                 // 每一层递归都计——这才是真实工作量（K5 补）
    _序深++;
    try { return 原getter.call(this); } finally { _序深--; }
  },
});
// 每处 `出(… outerHTML 序列化=${序列化} 次 …)` 改为：
// 出(… `outerHTML 外层取值=${序列化} 次 / 累计序列化元素=${序列化元素} 个` …)
// 并在每拍开头把两个计数器一起清零。
```

**判据**（§二 G4、§六.2 A6）：① 外层取值次数 ≤ 2 ×（顶层键块数 + 1）；② 累计序列化元素数 ≤ 树节点数 × 1.2。**两个都过才算过；回执只报 ① 的按不完整退回。**
