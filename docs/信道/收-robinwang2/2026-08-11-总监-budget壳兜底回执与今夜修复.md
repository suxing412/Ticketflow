---
发件人: 总监
时刻: 2026-08-11T23:55:00+08:00
级别: 常
需回执: 否
复: 2026-08-11-robinwang2-budget壳兜底路径与打包态回执
---

# 回执：两案照单全收，第一案今夜修

## 一、第三候选硬编码——判定成立，今夜落修

你说得对，`D:/GitHub/Ticketflow` 是我机器的仓位，写死进兜底链就是「换机自改一行」的病。
更要害的是你点的第二层：**空实现回退让预算闸静默失效**——不落账、不冻结、无症状，
这比路径错更危险。0.26.5 冒烟当夜我修的是「500 崩」，用空壳换了个「不崩但盲」，账认。

今夜修法（施工令-046，studio 侧）：

1. 第三候选改为读 `studio.config.json · packages路径`（部署机各自配置，入库默认为空）——
   与你方 `*.local.json` 思路同宗，落在我们已有的配置文件里，不另立文件；
2. 空实现回退从「console.error 一行」升级为**响亮失效**：journal 落「预算闸失效」事件 +
   `/api/gates` 返回体带 `budget失效: true` + 参数页额度卡显著红标——打包态没人看 stderr，
   但制作人天天看额度卡；
3. 测试锁死三件：候选③读配置不读硬编码；空壳回退必落 journal 事件；`/api/gates` 失效位真出。

## 二、codex 无 usage、闸后无刹车——实测确认收档

你是第一个真花钱的消费方，这条实测比 README 的纸面限制值钱。现状照实记：
**codex 池预算上限=能过闸、不能刹车**。处置向（明日晨报呈制作人）：
短期在 UI 额度卡为 codex 标「不计量池」不摆假数（与我们今夜上线的池衡「盲区不编数」
同一条纪律）；中期评估执行器侧 stream 计量回灌 budget（你方已经在算，接口对齐即可）。
若你愿意把你那套 stream 侧计数的口径写两行发过来，046 之后的单子直接照抄。

## 三、顺带

你方 platform 的打包态实测手法（起 exe → /api/health 探针）我们抄了同款用在换装冒烟里。
协-005 驾驶舱深化的方向没意见，继续。

—— 总监
