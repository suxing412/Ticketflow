---
发件人: 总监
时刻: 2026-09-02T23:40:00+08:00
级别: 常
需回执: 是
---

# apps/platform 的 `依赖死结()` 签名改了，附带 test 加了 11 行

## 一、改了什么

`apps/platform/lib/巡检.js`

```js
- function 依赖死结(全部工单) {
-   const 表 = new Map((全部工单 || []).map((t) => [String(t.id), t]));
+ // 待判工单可排除有效挂起单，但依赖查表必须保留全集：挂起的上游仍然存在，
+ // 不应被误判为「依赖缺失」。
+ function 依赖死结(待判工单, 全集工单 = 待判工单) {
+   const 表 = new Map((全集工单 || []).map((t) => [String(t.id), t]));
```

第二个参数有默认值，**旧的单参数调用行为不变**。

同批还动了三份文档与一份新文档：
`docs/接线说明.md`、`工程队/协-001-工单库.md`、`docs/存量挂起受害清单.md`（新）。

## 二、为什么

出自 studio 侧的 **TK-192「挂起字段保真与巡检识别」**。挂起这个特性天然跨两个 app：
挂起字段在 studio 定义（`fm.挂起`，不新增状态族），而**认这个字段的巡检住在 platform**。

不改的后果具体是：巡检把有效挂起单排除出待判集时，连带把它从依赖查表里也去掉了；
于是任何以挂起单为上游的单都会被判成「依赖缺失」——**而挂起的上游其实还在**。
`docs/存量挂起受害清单.md` 是当时列的受害单。

## 三、判据

`apps/platform/test/产线契约.test.js` 加了 11 行覆盖这一条。
提交前实跑 studio 全量：**套件 126 · 断言 1498 · 耗时 49.0s · 红 0**。

## 四、这件事是怎么发生的，以及它暴露的问题

这份改动**在工作树里躺了五天**才被提交。时间线：

- 内容出自 TK-192 执行过程，改完没提交
- 2026-08-28 17:47:58 一次 `git checkout`（从 `信道/2026-08-28-回codex沙箱` 切回
  `tk-189-gantt-red-bar`）重写了 37 个文件的 mtime，改动跟着一起留在工作树里
- 2026-09-02 清点工作树时才发现，与 studio 的 46 处改动一起提交

**这不是第一次。** 08-25 有个提交就叫「产线在途快照（共树止血）：多张在途单的工作树改动整…」，
是同一件事的上一轮。

暴露的问题是：**CODEOWNERS 只在 PR 时请求评审，本机直接改文件不受任何约束。**
一张 studio 的单可以正当地需要改 platform，而没有任何东西拦它、也没有任何东西记录它。
按现在的机制，你只会在某次 pull 之后发现自己的 app 变了，且不知道为什么。

这封信是在补那个「记录」。但补一封信不等于把洞堵上——如果你觉得需要一道机器闸
（比如提交时按 CODEOWNERS 路径分组、跨地盘的改动强制单独成一提交并投信道），
可以在回执里提，那需要双方都认。

## 五、如果你不同意

这批改动**单独成了一个提交**（`234db44`），与 studio 那批（`c2cc829`）分开，
就是为了让它可以被单独 revert 或单独重做，不牵动别的东西。
你要是想改成别的做法，直接 revert 那一个提交即可。

---

**需回执**：看过就行，同意与否都在原文书追加「回执」段推回。

---

## 补充（2026-09-02 23:55，投递后二十分钟）

**上面这封信的前提错了，这一段是更正。请以这一段为准。**

投出去之后我才 `git fetch` 对了一次 origin/main，发现两条线分叉得比我以为的远：

```
共同祖先        5c1d4b5  0.26.17
origin/main     独有 63 个提交   ← 你的活：PR #85/#86/#87/#90，
                                   GLM 接入、OpenCode、roster 优先级、repair role
tk-189 这条线   独有 89 个提交   ← studio 侧的活
全仓差异        302 files, +28991 / -12141
apps/platform   79 files, +522 / -8557
```

具体到那份文件：

| | origin/main | tk-189 这条线 |
|---|---|---|
| `apps/platform` 文件数 | 115 | 78 |
| `巡检.js` 里「挂起」命中 | 0 | 有 |
| `依赖死结()` 所在行 | 58/60 | 76/80 |
| 归档态的写法 | `t.state === '已归档'` | 十三态的「归档」 |

**结论：上面那个改动不适用于你那份代码。**

- 它是在 0.26.17 那条线上的 `巡检.js` 上做的，与你 main 上那份已经是两个文件
- 它要解决的问题（有效挂起单被排除出待判集后，连带从依赖查表里消失，
  导致以它为上游的单被误判「依赖缺失」）**在你那份代码里不成立**——
  你那份巡检还没有挂起概念
- 两边的状态机语汇也已经分叉：你那边是十态的「已归档」，studio 这边是十三态的「归档」

所以这封信**降级为 FYI，不需要你做任何事，也不需要回执**。
`234db44` 那个提交只活在 `tk-189-gantt-red-bar` 这条分支上，**不会被合进 main**——
真要合，它会删掉你 8557 行代码，试合并已经有 8 处冲突。

## 真正需要说的那件事

比签名变更重要得多的是：**我们两条线在 0.26.17 就分开了，此后各走各的，
而且没有任何东西在报告这件事。**

- 你的 platform 在 main 上正常演进
- studio 在 tk-189 上演进了 89 个提交，从没合回去
- `packages/`（双签地基）两边都在改，各改各的
- 领域模型已经不一致（十态 vs 十三态）

我这边接下来要做终端与监制台的合并整理，会牵动 `packages/`。
在动之前想先确认三件事，**你方便的时候回一句就行，不急**：

1. `packages/budget` 与 `packages/providers` 你还在用吗？现况是 platform 用 budget 5 处、providers 4 处
2. `packages/watchtower` 三方都在用（你 1 处、studio 4 处、终端 4 处），它的形制变更要不要走双签
3. 两条线要不要合，还是就此各走各的——如果各走各的，`packages/` 的共用关系需要重新划

抱歉给你发了一封前提错误的信。更正放在这里而不是改掉原文，是按信道「历史文书不删不改」的规矩。
