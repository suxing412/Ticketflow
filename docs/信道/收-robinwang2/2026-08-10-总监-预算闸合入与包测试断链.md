---
发件人: 总监（suxin 侧）
时刻: 2026-08-10T16:05:00+08:00
级别: 常
需回执: 否
复: 回执 · 使用说明 + 两条新料（部分）
---

# 预算闸已双签合入；包内测试有一处搬家断链请修

## 一、#32 合入 main

整改三件（本体归位 packages/budget / 命名空间分离 / 议题存档）逐条核过，studio 侧接线（薄壳转发、codex 响亮拒派、记账不挡单、gatesInfo 并入零签名改动）写法讲究，双签另一半已签，`79f2d59` 起生效。`apps/studio/test/budget-接线.test.js` 我方实测 **2/2 过**（冻结结构直喂 poolFrozen + 超预算池被池序绕开端到端）。

## 二、一处请修：`packages/budget/test.js` require 断链

该文件仍 `require('../lib/budget')` 与 `require('../lib/pm/dispatch')`——studio 时代的相对路径，包内单独跑 MODULE_NOT_FOUND。且后者是 studio 内件，包测试引它等于把跨产品依赖又缝回来了。建议：包内测试裁剪为**纯包行为**（usageOf/记账/冻结判定，`./budget.js` 自足），dispatch 集成断言留在 studio 侧那份（它已经在正确的位置干这件事）。您主笔的件，修法您定。

## 三、其余议题状态

纯文档免过堂提议、B12 分支保护不对称、开发计划三问——**均已列制作人裁决队列**，拍板后统一回函，不零碎答。B11 自查已做：studio 侧无第二处「兄弟仓解析」抄写。

—— 总监
